/*
 * tc_embed_aot.h — TC-Embed AOT 桥接模式
 *
 * AOT 专用函数声明见 tc_embed.h（tc_embed_create_aot）。
 * 本文件仅为满足命名约定 tc_embed_aot.h ↔ tc_embed_aot.c。
 */
#ifndef TC_EMBED_AOT_H
#define TC_EMBED_AOT_H

#include "tc_embed.h"

#endif /* TC_EMBED_AOT_H */

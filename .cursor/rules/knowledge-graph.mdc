---
description: >-
  TC-Compiler cross-module index — load ONLY when adding/changing TcRhsKind or
  TcStmtKind, or editing parser+analyzer+executor/aot together. Read exactly ONE
  kg-*.md. Skip for single-file fixes, docs, or test lookup. Dispatch details
  in kg-dispatch.md; verify with check_rhs_coverage.py.
globs:
alwaysApply: false
---

# TC-Compiler 知识图谱（索引）

> **v0.0.42** · 加载条件见 [.cursor/README.md](../README.md) § `@knowledge-graph`  
> **本 rule → 恰好一个 `kg-*.md`** · 单文件 → `compiler-src` + `rg`

## 任务 → 读哪一份（恰好一个）

| 任务 | 读 |
|------|-----|
| 新运算符 / 新 RHS / 浮点·cast·bitcast / 复合 RHS | [kg-dispatch.md](../skills/tc-architecture/kg-dispatch.md) |
| if / while / goto / CFG / uninit / 块作用域 | [kg-cfg.md](../skills/tc-architecture/kg-cfg.md) |
| let / 短路 / OOM / AOT 语义 / 复合运行时 | [kg-eval.md](../skills/tc-architecture/kg-eval.md) |
| `#program` / import / Self / 导入 struct | [kg-module.md](../skills/tc-architecture/kg-module.md) |
| func / funcall / 调用图 / static / 调用帧 | [kg-func.md](../skills/tc-architecture/kg-func.md) |
| TC-Embed / value_bridge / `--embed` | [kg-embed.md](../skills/tc-architecture/kg-embed.md) |
| I/O / format | 只改 `src/vm/runtime/tc_io.c`；路由 [features/scalar.md](../skills/tc-architecture/features/scalar.md) § I/O |
| 新/改错误码 | [errors.md](../skills/tc-architecture/errors.md) + 语言标准附录 B |
| 测试/CI | Skill `run-tests` — **勿**加载 kg |

## 短工作流

**新 RHS** — `rg "TC_RHS_" src/` → 8 分发点（[kg-dispatch.md](../skills/tc-architecture/kg-dispatch.md)）→ `check_rhs_coverage.py` → `.tc` + `run_tests.sh`

**新 STMT** — `rg "TC_STMT_" src/` → **`src/vm/analyzer/tc_cfg.c` 必改** → [kg-cfg.md](../skills/tc-architecture/kg-cfg.md) → `test_cfg` + uninit `.tc`

**let** — `src/vm/analyzer/tc_const_eval.c` → [kg-eval.md](../skills/tc-architecture/kg-eval.md)

**模块** — `tc_module.c` / `tc_scope.c` → [kg-module.md](../skills/tc-architecture/kg-module.md)

**OOM** — `TC_ERR_OUT_OF_MEMORY` + `"memory allocation failed"` → [kg-eval.md](../skills/tc-architecture/kg-eval.md)

## 分发点入口（细节不在此重复）

| Kind | 8 函数入口 | 权威 |
|------|------------|------|
| `TcRhsKind` | parse_rhs · parse_const_rhs · rhs_free · check_rhs · eval_const_rhs · eval_rhs · aot_emit_rhs · aot_rt shim | [kg-dispatch.md](../skills/tc-architecture/kg-dispatch.md) |
| `TcStmtKind` | parser · pass1 · pass2 · **cfg** · executor · aot_emit_stmt | [kg-cfg.md](../skills/tc-architecture/kg-cfg.md) |

`FUNCALL_EXPR` kind 在 `src/vm/parser/tc_parser_stmt.c` 赋值；其余 RHS 多在 `tc_parser_rhs.c`。

验证：`python3 scripts/sync/check_rhs_coverage.py [--fix]`

## 维护

1. 分发点路径以 `check_rhs_coverage.py` 为准；细节在各 `kg-*.md`
2. 路由表：`skills/tc-architecture/SKILL.md`；导航：`.cursor/README.md`

---
description: >-
  TC-Compiler documentation editing — sync with code, tests, and .cursor skills.
  Loads when editing docs/**/*.md. Language spec is docs/TC语言标准设计说明书-0.0.44.md.
globs: docs/**/*
alwaysApply: false
---

# 文档编辑

Agent 导航：[.cursor/README.md](../README.md) · 语法摘要同步 [syntax.md](../skills/tc-architecture/syntax.md)

## 版本状态

- **设计目标**：TC **0.0.44**（语言标准附录 B 86 码）
- **实现代码**：`src/vm/runtime/tc_version.h`（**v0.0.44**）；Phase 1–7 已落地（含 struct 运行时与 Embed v0.0.44）
- **Embed 规格**：`docs/TC-Embed详细设计说明书-0.0.44.md`

## 文档职责

| 文件 | 内容 |
|------|------|
| `docs/TC语言标准设计说明书-0.0.44.md` | 0.0.44 语法/语义唯一权威来源（附录 B 86 码）；语言特性变更**必更** |
| `docs/TC编译器标准设计说明书-0.0.44.md` | 13 阶段管线、诊断优先级、调用图等编译器规范 |
| `docs/TC-VM详细设计说明书-0.0.44.md` | VM 架构、执行器、诊断（无 REPL） |
| `docs/TC-AOT详细设计说明书-0.0.44.md` | AOT 代码生成、shim 层、差分测试 |
| `docs/libtc设计说明书-0.0.44.md` | libtc 架构设计与嵌入 API（含 §15 调用者 API 速查，原 libtc-api 已并入） |
| `docs/TC-VM命令行参考-0.0.44.md` | tc-vm / tc-aot CLI（含 `-I`、无 REPL） |
| `docs/TC-Embed详细设计说明书-0.0.44.md` | TC-Embed API、VM/AOT 双模式、非致命 abort |
| `docs/TC语言标准设计说明书-0.0.45.md` | **0.0.45 冻结候选语言标准**：`ref<T>`（托管引用）/ `addr<T>`（线性地址）双空间、公共 IR 文本形态；尚未落地实现 |
| `docs/TC编译器标准设计说明书-0.0.45.md` | **0.0.45 冻结候选编译器标准**：检查规则与语言标准 0.0.45 对齐；含降级契约（§1.4）、局部可判定（§1.5）、调用深度上界（§8.10）；尚未落地实现 |

上表为**设计/规范文档**（7 份 0.0.44 现行 + 2 份 0.0.45 冻结候选）。它们只引用同类设计文档与 `src/` / `tests/`，**不得**引用过程性文档（符合性审计报告、整改进度台账、CHANGELOG、release-checklist、`docs/README` 地图、AGENTS / `.cursor/**`）。分层检查：`python3 scripts/sync/check_doc_layering.py`。查某条规范是否落地：对 `src/` 与测试，勿回写过程记录。

## 同步检查

改语言特性时确认：

- [ ] 语法摘要与 `tc-architecture/syntax.md` 一致（实现未到的特性以语言标准为准，syntax 可暂注「待落地」）
- [ ] 新错误种类写入编译器标准 §11.4 + `errors.md` + VM 详设 + `tc_error_kind_name()`（语言标准仅当变更可观察语义时）
- [ ] OOM 路径使用 `TC_ERR_OUT_OF_MEMORY`（非语法类错误码）
- [ ] 特性实现写入 `features/*.md` 或 Embed→`kg-embed.md`（经 [features.md](../skills/tc-architecture/features.md) 路由）+ Rule `knowledge-graph` / 对应 `kg-*.md`
- [ ] Agent 导航同步 [AGENTS.md](../../AGENTS.md) · [.cursor/README.md](../README.md)；易踩坑写入 [gotchas.md](../skills/tc-architecture/gotchas.md)（大改架构时）
- [ ] 诊断阶段与编译器标准 / `errors.md` 一致（现网多阶段编排见 pipeline.md）
- [ ] 版本号：`src/vm/runtime/tc_version.h`（及 AOT `TC_AOT_VERSION`）
- [ ] 测试规模：当前注册见 `scripts/*/run_tests.sh` / `test-map.md`
- [ ] 新模块同步 `locations.md` / `features/*.md` / `compiler-src.mdc` / `unit-tests-c.mdc`（Embed → `embed-src.mdc` / `kg-embed.md`）
- [ ] 工程布局与 `tc_<module>.h` ↔ `tc_<module>.c` 命名一致；`check_source_naming.py`
- [ ] 设计文档未引用过程性文档；`check_doc_layering.py`
- [ ] 固定点入口写作 `tc_analyze_definite_init`（`tc_cfg.c`），勿写旧符号名
- [ ] 无 REPL；skill/rule 中不得引入 `tc_repl` / `tc_analyze_statement`

Agent 导航：[AGENTS.md](../../AGENTS.md) · [.cursor/README.md](../README.md)（加载分级）· Skill `tc-architecture`（路由）· [gotchas.md](../skills/tc-architecture/gotchas.md)。用户文档保持完整叙述，勿复制进 rule。

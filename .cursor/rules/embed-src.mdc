---
description: TC-Embed 编辑 — C 宿主 API、VM/AOT 双模式、非致命 abort；编辑 src/vm/embed/ 时加载
globs: src/vm/embed/**/*
alwaysApply: false
---

# Embed 编辑

> **v0.0.39 / Phase 7**。跨模块细节：[kg-embed.md](../skills/tc-architecture/kg-embed.md)。规格：`docs/TC-Embed详细设计说明书-0.0.39.md`。

## 文件

| 文件 | 职责 |
|------|------|
| `tc_embed.h` / `.c` | 公共 API；VM 模式创建与调用 |
| `tc_embed_aot.h` / `.c` | AOT 模式装配 |
| `tc_embed_internal.h` | 内部上下文 |
| `tc_value_bridge.h` | 标量值桥接（static inline） |

协作：AOT 侧 `src/aot/tc_aot_embed_rt.h` + `tc_aot_codegen.c`（`--embed` / 函数表）；Executor `tc_exec_call_function_public`。

## 必守

1. VM / AOT 模式对外 API 兼容（同一套 call/slot/ptr）
2. AOT 运行时错误走 `tc_aot_embed_abort`（非致命），经 `tc_embed_had_error` / `get_error` 可见
3. 扩展值桥接时保持 header-only 标量路径；堆对象勿 silently memcpy
4. 改完：`cmake --build build --target check-embed check-embed-aot`
5. 文档：`features.md` § TC-Embed · [test-map.md](../skills/tc-architecture/test-map.md) Phase 7

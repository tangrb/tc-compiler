#program
; 观察-⑥ 负例：`<模块名>.<static let> = rhs` → 常量赋值（不得降级为 undefined variable）
; 期望：cannot assign to constant（ConstantAssignmentError）
import MemberLib
var v: int32 = 0
MemberLib.K = 9
writeln(int32, v)

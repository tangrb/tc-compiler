#program
; §6.8.9 操作数表：dst_idx / src_idx / length 须为整数类型 operand。
; 指针类型作下标（此处 dst_idx = q）→ TC_CE_TYPE_MISMATCH，不得静默接受。
var a: int32 = 1
var b: int32 = 2
var p: ptr<int32> = ptr_address(int32, a)
var q: ptr<int32> = ptr_address(int32, b)
memcopy_unsafe(int32, p, q, q, 0, 1)
writeln(int32, a)

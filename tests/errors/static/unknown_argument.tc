#lib
; 期望：实参个数多于形参，但「未知」检查在前 → UnknownArgument，定位最早未知名称
public func sum(a: int32) int32 then
    return a
end
public func caller() void then
    var x: int32 = funcall(Self.sum, z: 1, y: 2)
    return
end

#lib
; 期望：cannot jump to label in another function（TC_CE_CROSS_CONTROL_FLOW_JUMP）
; §7.3 / 编译器标准 §7.5：函数 a 内无标签 y，但函数 b 定义了 label y → 跨控制流域错误。
public func a() void then
    goto y
    return
end
public func b() void then
    label y:
    return
end

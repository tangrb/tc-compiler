; 期望：float underflow → 结果为 0.0 时静默接受
; strict 模式下，float64 乘法下溢（结果为 0.0 时不报错）
writeln(float64, 1.0)
var a: float64 = 1e-200
var b: float64 = 1e-200
var c: float64 = mul(float64, a, b)
writeln(float64, c)

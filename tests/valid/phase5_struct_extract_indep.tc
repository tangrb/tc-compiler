#program
; 嵌套结构体提取后独立修改
struct Inner then
    var x: int32
end
struct Outer then
    var inner: Inner
end
var o: Outer = Outer(inner: Inner(x: 5))
var i: Inner = o.inner
var ox: int32 = 0
var ix: int32 = 0
i.x = 8
ox = o.inner.x
ix = i.x
writeln(int32, ox)
writeln(int32, ix)

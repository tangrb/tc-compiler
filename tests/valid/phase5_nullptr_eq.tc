#program
; Phase 5：nullptr 等值比较合法
var p: ptr<int32> = nullptr
var q: ptr<int32> = nullptr
var same: bool = ptr_eq(int32, p, q)
var is_ne: bool = ptr_ne(int32, p, q)
writeln(bool, same)
writeln(bool, is_ne)

#program
; §6.8.9 memcopy_unsafe：ptr<struct> 元素拷贝（堆对象区）
struct B then
    var x: int32
end
var s: B = B(x: 5)
var t: B = B(x: 0)
var ps: ptr<B> = ptr_address(B, s)
var pt: ptr<B> = ptr_address(B, t)
var w: int32 = 0
memcopy_unsafe(B, pt, 0, ps, 0, 1)
w = t.x
writeln(int32, w)

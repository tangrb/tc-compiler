#program
; 常量 struct 整值经 `let` 复制后再写入运行时 `var`（值语义深拷贝）。
struct Pair then
    var x: int32
    var y: int32
end
let a: Pair = Pair(x: 3, y: 4)
let b: Pair = a
var p: Pair = b
var s: int32 = add(int32, p.x, p.y)
writeln(int32, s)

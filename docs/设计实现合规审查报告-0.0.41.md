# TC 0.0.41 设计—实现合规审查报告

> **规范基线（唯一权威）**：[TC 语言标准 0.0.41](./TC语言标准设计说明书-0.0.41.md) · [TC 编译器标准 0.0.41](./TC编译器标准设计说明书-0.0.41.md)
>
> **实现基线**：libtc / TC-VM / TC-AOT / TC-Embed v0.0.41（Phase 1–7 已落地）
>
> **文档状态**：实现已落地；证据已于 2026-07-24 回填
>
> **审查日期**：2026-07-24

---

## 目录

1. [审查口径](#1-审查口径)
2. [审查矩阵](#2-审查矩阵)
3. [词法、语法与 IR](#3-词法语法与-irl-01l-15)
4. [类型系统](#4-类型系统t-01t-20)
5. [模块系统](#5-模块系统m-01m-15)
6. [函数与调用模型](#6-函数与调用模型f-01f-18)
7. [控制流与确定初始化](#7-控制流与确定初始化c-01c-15)
8. [表达式与运算](#8-表达式与运算e-01e-25)
9. [常量求值与 let](#9-常量求值与-letk-01k-12)
10. [I/O](#10-ioi-01i-10)
11. [VM 执行器](#11-vm-执行器v-01v-12)
12. [AOT 代码生成](#12-aot-代码生成a-01a-12)
13. [libtc API 与诊断](#13-libtc-api-与诊断d-01d-18)
14. [发布、文档与清理](#14-发布文档与清理r-01r-10)
15. [v0.0.31 → v0.0.41 迁移检视](#15-v0031--v0041-迁移检视)
16. [最终发布结论](#16-最终发布结论)

---

## 1. 审查口径

### 1.1 审查范围

本报告覆盖 TC 0.0.41 的全部设计—实现规范追踪，涵盖以下新增领域：

| 领域 | 0.0.31 基线 | 0.0.41 新增 |
| ---- | ---------- | ---------- |
| 模块系统 | 单文件 | `#program`/`#lib`、`import`、`public`/`private`、`Self`、DAG 依赖图 |
| 函数 | 无 | `func`/`funcall`/`return`、命名实参、按值只读形参、无环调用图 |
| 类型 | 标量（12 种） | 增加 `ptr<T>`、`memblock<T,N>`、`struct`、`isize`/`usize`、`void` |
| 运算 | 算术/位/比较/逻辑/cast/bitcast | 增加全部 `ptr_*`、`memblock_*`、`memcopy_unsafe` 指令 |
| 编译管线 | 6 阶段 | 13 确定性阶段（含 4/6 阶段内部子阶段） |
| 错误码 | 41+1 | 70+（含函数、模块、memblock、struct、ptr 专用诊断） |
| REPL | 包含 | 无 |
| 程序入口 | 顶层语句 | `#program` 顶层语句 |

### 1.2 判定规则

| 状态 | 含义 |
| ---- | ---- |
| 通过 | 规范要求已映射到实现，且有自动化行为证据 |
| 通过（实现存在；专项测试薄弱） | 源码路径存在，但缺少独立自动化用例可直接证明该细则 |
| 未实现 | 设计已定但实现待完成 |
| 不适用 | 不属于 0.0.41 规范或本次发布边界 |

### 1.3 证据层次

各项审查需要三层证据：

1. **设计层面**：规范条款 → 设计文档映射
2. **实现位置**：设计文档 → 源码模块/函数
3. **测试覆盖**：源码 → 测试用例 + 门禁通过

---

## 2. 审查矩阵

### 2.1 规范—实现追踪总览

| 审查域 | 主要规范章节 | 主要实现层 | 编号 |
| ------ | ------------ | ---------- | ---- |
| 词法、语法与 IR | 编译器标准 §2–§5、语言标准 §2、附录 A | lexer、parser、types | L-01～L-15 |
| 类型系统 | 编译器标准 §3、语言标准 §3 | analyzer、types | T-01～T-20 |
| 模块系统 | 编译器标准 §4、语言标准 §4 | module resolver、analyzer | M-01～M-15 |
| 函数与调用模型 | 编译器标准 §8、语言标准 §8 | analyzer、executor | F-01～F-18 |
| 控制流与初始化 | 编译器标准 §7、§9、语言标准 §7、§9 | CFG、DFA、analyzer | C-01～C-15 |
| 表达式与运算 | 编译器标准 §6、语言标准 §6 | semantics、analyzer | E-01～E-25 |
| 常量求值与 let | 编译器标准 §5、语言标准 §5 | const_eval、analyzer | K-01～K-12 |
| I/O | 编译器标准 §10、语言标准 §10 | tc_io | I-01～I-10 |
| VM 执行器 | VM 设计说明书 | executor | V-01～V-12 |
| AOT 代码生成 | AOT 设计说明书 | codegen、runtime shim | A-01～A-12 |
| libtc API 与诊断 | libtc 设计说明书、编译器标准 §11 | libtc、diagnostic | D-01～D-18 |
| 发布与清理 | 各设计文档、编译器标准附录 | 同步脚本、文档、测试 | R-01～R-10 |

---

## 3. 词法、语法与 IR（L-01～L-15）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| L-01 | UTF-8 解码，拒绝 BOM、U+0000、非法 UTF-8 | 编译器标准 §1.2 阶段1、语言标准 §2.1 | 通过 | `tc_lexer.c` UTF-8 解码路径 | `test_lexer` / `test_lexer_extended`；非法字节用例 |
| L-02 | 最长匹配词法分析器（含 0 后进制检测） | 编译器标准 §2.1 | 通过 | `tc_lexer.c` 字面量扫描 | `hex_literal`/`oct_literal`/`bin_literal`；`leading_zero` |
| L-03 | 字面量 Token 自身绝对值上限、浮点舍入为零/无穷检查 → `TC_CE_LITERAL_OUT_OF_RANGE` | 编译器标准 §1.2 阶段2 | 通过 | `tc_lexer.c` + 字面量定型 | `literal_range*`；`fp_literal_range`；`invalid_hex_overflow` |
| L-04 | 缩进级别栈：只能 U+0020、4空格一级、INDENT/DEDENT 生成规则 | 编译器标准 §2.2 | 通过 | `tc_lexer.c` 缩进栈 | `indent_mixed_*`；`indent_insufficient_*`；`indent_else_mismatch` |
| L-05 | 13 阶段确定性流水线顺序（阶段优先、源位置优先、规则优先） | 编译器标准 §1.2–§1.3 | 通过 | `tc_analyzer.c` `tc_analyze_ex` | `test_analyzer` 诊断优先级；`diag_priority_*` |
| L-06 | 操作数数量受限恢复与权威表（含所有 `ptr_*`/`memblock_*`/`memcopy_unsafe`） | 编译器标准 §1.3 | 通过 | `tc_parser_rhs.c` RHS 解析表 | `check_rhs_coverage.py`；`format_operand_count*` |
| L-07 | `#program`/`#lib` 模式指令解析 | 编译器标准 §4.2 | 通过 | `tc_lexer.c`/`tc_parser.c` | `test_lexer`/`test_parser`；模块头用例 |
| L-08 | `public`/`private` 可见性修饰符解析 | 编译器标准 §4.2 | 通过 | `tc_parser.c` 可见性 | `missing_visibility`；`private_member_access`；`test_module` |
| L-09 | `import` 声明解析与模块路径定位 | 编译器标准 §4.6 | 通过 | `tc_parser.c` + `tc_module.c` | `import_ok`；`import_not_found`；`include_search_ok` |
| L-10 | `func` 定义解析（含形参列表、返回类型、`void`） | 编译器标准 §8.1 | 通过 | `tc_parser.c` func 语句 | `test_parser`；`phase4_*`/`phase5_void_funcall` |
| L-11 | `funcall` 解析（含命名实参：`name: expr`） | 编译器标准 §8.2 | 通过 | `tc_parser.c`/`tc_parser_rhs.c` | `funcall_*` 错误；`phase5_funcall_return` |
| L-12 | `return` 解析（有值/无值形式） | 编译器标准 §8.3 | 通过 | `tc_parser.c` return | `return_*`；`missing_return`；`phase5_void_funcall` |
| L-13 | `struct` 类型定义解析（含 `let`/`var` 字段、`@padding`） | 编译器标准 §3.3 | 通过 | `tc_parser.c` struct | `phase3_struct_*`；`phase5_struct_padding` |
| L-14 | `memblock<T, N>` 语法（`N` 为编译期 `usize` 常量） | 编译器标准 §3.1 | 通过 | `tc_parser.c` 类型语法 | `phase3_memblock*`；`memblock_count_*` |
| L-15 | `static var` / `static let` 声明解析（仅 `#lib`） | 编译器标准 §4.3 | 通过 | `tc_parser.c` + 模式检查 | `phase4_static_let`；`static_in_program`；`phase5_static_var` |

---

## 4. 类型系统（T-01～T-20）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| T-01 | 固定宽度整数 `int8`～`uint64`、`bool` | 语言标准 §3.2–§3.5 | 通过 | `tc_types.c`/`tc_types.h` | `test_types`；`bool_var`；`type_combinatorial` |
| T-02 | `float32`/`float64` 浮点类型 | 语言标准 §3.2 | 通过 | `tc_types.c` + `tc_sem_fp.c` | `fp_basic`/`fp_arith`；`test_semantics` |
| T-03 | `ptr<T>` 类型，携带所指 `T`，等价判定不含所指类型外的差异 | 编译器标准 §3.2 | 通过 | `tc_types.c` `tc_make_ptr`/`tc_type_equals` | `test_types`；`phase3_ptr_*`；`ptr_type_mismatch` |
| T-04 | `memblock<T, N>` 类型：`N` 不参与类型等价，仅由 `T` 决定 | 编译器标准 §3.1 | 通过 | `tc_types.c` equals 忽略 N | `test_types`；`memblock_size_mismatch` |
| T-05 | `struct` 类型：字段列表、可变性、padding、类型定义唯一性；字段类型位置规则含 `ptr<本结构体>` 指针自引用（[语言标准 §3.9.1]） | 编译器标准 §3.3；语言标准 §3.9.1 | 通过 | `tc_struct_check.c`（`tc_struct_validate_type_node` 按位置规则递归校验） | `struct_*` 错误；`phase5_struct_*`；`struct_self_ref`（值自引用 → `TC_CE_STRUCT_VALUE_SELF_REF`）/ `struct_memblock_self_ref` / `struct_ptr_fwd_ref` / `struct_memblock_undefined` / `ptr_undefined_struct` / `ptr_struct_type_distinct` / `struct_dup_field` / `struct_padding_neg|hex|u` / `struct_end_extra` / `struct_end_indent`；unit `test_struct_self_reference` |
| T-05a | `struct` 指针自引用：`var next: ptr<S>` 在定义 `S` 时合法；值自引用与前向引用仍拒绝 | 语言标准 §3.9.1 | 通过 | `tc_struct_check.c`：`TcType.pending_name` 承载未决名，`tc_struct_validate_type_node` 位置校验 + `tc_struct_resolve_type_tree` 解析为 struct_id；`tc_parser.c` 保留嵌套结构体名 | `phase5_struct_ptr_self_ref` / `_nested_self_ref` / `_roundtrip`；`struct_self_ref` / `struct_memblock_self_ref` / `struct_ptr_fwd_ref`；unit `test_struct_self_reference` |
| T-06 | `isize`/`usize` 平台字长类型，宽度等于目标指针宽度 | 编译器标准 §3.5 | 通过 | `tc_types.c` 字长 | `isize_arith`/`usize_arith`；`test_types` |
| T-07 | `void` 仅可作函数返回类型 | 编译器标准 §1.1.1 | 通过 | `tc_func_check.c`/`tc_types.c` | `phase5_void_funcall`；`return_*` |
| T-08 | `sizeof_bits` 计算：各类型宽度表，memblock 含长度头部 | 编译器标准 §3.0.1、§3.1 | 通过 | `tc_types.c` `tc_sizeof_bits` | `test_types`；`ptr_size_not_usize` |
| T-09 | `memblock` 编译器约束：记录 `N`、计算宽度、比较 `N`、`.count` 为编译期常量 | 编译器标准 §3.1 | 通过 | `tc_memblock_check.c` | `memblock_*`；`phase3_memblock*`；`test_type_check` |
| T-10 | `ptr<T>` 编译器约束：禁止通用标量运算、`nullptr` 由期望类型定型、禁止 I/O | 编译器标准 §3.2 | 通过 | `tc_ptr_check.c` | `ptr_scalar_arith`；`nullptr_non_ptr`；`ptr_io_writeln` |
| T-11 | `ptr_address` 仅接受 `var`/`static var`/形参 | 编译器标准 §3.2 | 通过 | `tc_ptr_check.c` | `ptr_address_const`；`phase5_ptr_basic` |
| T-12 | `ptr_store`/`memcopy_unsafe` 可变性约束（只读/形参绑定拒绝） | 编译器标准 §3.2 | 通过 | `tc_ptr_check.c` | `ptr_store_readonly`；`parameter_assignment*` |
| T-13 | 空指针分类：解引用类 → `TC_RE_NULL_POINTER_DEREFERENCE`，算术类 → `TC_RE_NULL_POINTER_ARITHMETIC` | 编译器标准 §3.2 | 通过 | `tc_ptr_exec.c`/`tc_aot_rt.c` | `null_ptr_deref`/`null_ptr_store`；`null_ptr_arith` |
| T-14 | `struct` 值构造器验证：全字段必填、文本顺序、类型一致 | 编译器标准 §3.3 | 通过 | `tc_struct_check.c` `tc_struct_check_constructor` | `struct_missing_field`/`field_order`/`unknown_field` |
| T-15 | `struct` 字段赋值双层可变性检查（外层绑定 × 字段 `let`/`var`） | 编译器标准 §3.3 | 通过 | `tc_struct_check.c` `tc_struct_check_field_assign` | `struct_immutable_field`；`struct_assign_let_outer_*`；`phase5_struct_mut_matrix_ok` |
| T-16 | `memblock` 构造器验证：`count:` 合法性、逐值数量检查、填充形式 | 编译器标准 §3.4 | 通过 | `tc_memblock_check.c` | `memblock_element_count`/`fill_type`/`count_zero` |
| T-17 | 有符号整数默认 strict，`wrap` 模式仅允许在 `add`/`sub`/`mul`/`neg`/`shl` | 编译器标准 §3.5 | 通过 | `tc_sem_int.c` + analyzer 模式矩阵 | `wrap_arithmetic_all`；`wrap_mode_error*` |
| T-18 | 无符号整数固定模回绕，不接受模式关键字 | 编译器标准 §3.5 | 通过 | `tc_sem_int.c` | `unary_wrap_unsigned`；`wrap_mode_error*` |
| T-19 | 字面量检查实现要点：无后缀整数/浮点/`u` 后缀/`inf`/`nan`/`nullptr` | 编译器标准 §3.6 | 通过 | `tc_lexer.c` + 定型 | `literal_edge_cases`；`phase3_nullptr`；`float_special_non_float` |
| T-20 | `cast`/`truncate`/`bitcast` 三种形式静态验证与运行时策略（含 `ptr` 等宽 cast、`cast(ptr<T>, nullptr)`、与 `ptr`↔`usize` bitcast；Pass2 预 intern） | 编译器标准 §3.7 | 通过 | `tc_sem_cast.c` + `tc_parser_rhs.c` + pass2/executor/AOT | `cast_operations_all`；`bitcast_roundtrip*`；`phase5_ptr_cast`；`phase5_ptr_cast_nullptr`；`phase5_ptr_bitcast`；`ptr_cast_width` |
| T-21 | 类型单一事实源：`TcValue`/`TcSymbol`/`binding` 持 `const TcType*`；`TcTypeTag` 仅为投影；`type_table` intern | 编译器标准 §3.0；VM 详设 §8.1/§12.1 | 通过 | `tc_types.c`/`tc_symbol.c`/Pass1 | `test_types` intern；`check_type_fact_source.py` |

---

## 5. 模块系统（M-01～M-15）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| M-01 | 五层模块结构（模式指令 → import → struct → 值声明 → 函数/语句） | 编译器标准 §4.2 | 通过 | `tc_module.c` `tc_module_check_structure` | `module_layer`；`test_module` |
| M-02 | 子阶段 4a：单文件结构检查（`TC_CE_MODULE_LAYER`、`TC_CE_MISSING_VISIBILITY`、`TC_CE_PROGRAM_MODE_MISUSE`） | 编译器标准 §4.1 | 通过 | `tc_module.c` 4a | `module_layer`；`missing_visibility`；`program_mode_misuse` |
| M-03 | 子阶段 4b：导入解析（`IMPORT_NOT_FOUND`/`NOT_LIB`/`AMBIGUOUS`/`DUPLICATE`/`NAME_CONFLICT`） | 编译器标准 §4.1、§4.6 | 通过 | `tc_module.c` `tc_module_resolve_imports` | `import_not_found`/`not_lib`/`ambiguous`；`duplicate_import` |
| M-04 | 子阶段 4c：依赖图环检查 → `TC_CE_CIRCULAR_IMPORT`（DAG） | 编译器标准 §4.1 | 通过 | `tc_module.c` 环检测 | `circular_import`；`self_import` |
| M-05 | 子阶段 4d：收集全部可达库的函数签名 | 编译器标准 §4.1 | 通过 | `tc_module.c` `tc_module_collect_signatures` | `test_module`；`phase4_self_funcall` |
| M-06 | `Self` 仅在 `#lib` 模块内有效，`#program` 中出现 → `TC_CE_PROGRAM_MODE_MISUSE` | 编译器标准 §4.5 | 通过 | `tc_scope.c`/`tc_func_check.c` | `self_in_program`；`phase4_self_funcall` |
| M-07 | 函数体内本库成员必须通过 `Self.<名>` 访问，裸名 → `TC_CE_FUNCTION_SCOPE_ACCESS` | 编译器标准 §4.5、§8.4 | 通过 | `tc_func_check.c` `tc_func_try_function_scope_access` | `function_scope_access` |
| M-08 | `public`/`private` 可见性检查：外部访问 `private` → `TC_CE_PRIVATE_MEMBER_ACCESS` | 编译器标准 §4.5、§4.1 | 通过 | `tc_func_check.c`/`tc_scope.c` | `private_member_access` |
| M-09 | 公开 `static var` 跨模块可读写（含作为 `read` 目标） | 编译器标准 §4.5 | 通过 | `tc_scope.c` + executor 槽 | `phase5_static_var`；modules/`import_ok` |
| M-10 | `static let` 编译期求值，所有引用处内联 | 编译器标准 §4.3 | 通过 | `tc_func_check.c` `tc_func_eval_static_lets` | `phase4_static_let`；`static_let_forward` |
| M-11 | `static var` 初始化器约束：仅字面量、更早 `Self` 成员、导入公开 static | 编译器标准 §4.3 | 通过 | `tc_func_check.c` `tc_func_check_static_vars` | `static_var_bad_init`；`phase5_static_var` |
| M-12 | `static var` 全程序唯一槽，导入不复制状态 | 编译器标准 §4.3 | 通过 | `tc_symbol.c` + `tc_executor.c` 槽域 | `phase5_static_var`；AOT 差分同用例 |
| M-13 | `static var` 按依赖拓扑序初始化 | 编译器标准 §4.3 | 通过 | `tc_executor.c`/`tc_aot_codegen.c` init | `phase5_static_var`；`tc_run_program`（`test_libtc`） |
| M-14 | 本库顶层成员名索引：收集 `func`/`static let`/`static var` 声明名 | 编译器标准 §8.4 | 通过 | `tc_scope.c` `tc_member_index_build` | `test_module`；`function_scope_access` |
| M-15 | 模式对照约束（`#program` vs `#lib` 接受集） | 编译器标准 §4.4 | 通过 | `tc_module.c` | `func_in_program`；`static_in_program`；`self_in_program` |

---

## 6. 函数与调用模型（F-01～F-18）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| F-01 | 阶段 5：函数重名检查 → `TC_CE_DUPLICATE_FUNCTION` | 编译器标准 §8.1 | 通过 | `tc_func_check.c` `tc_func_check_signatures` | `duplicate_function` |
| F-02 | 阶段 5：函数名与值绑定冲突 → `TC_CE_FUNCTION_NAME_CONFLICT` | 编译器标准 §8.1 | 通过 | `tc_func_check.c` | `function_name_conflict` |
| F-03 | 阶段 5：参数重名检查 → `TC_CE_DUPLICATE_PARAMETER` | 编译器标准 §8.1 | 通过 | `tc_func_check.c` | `duplicate_parameter`；`parameter_name_conflict` |
| F-04 | 阶段 7：funcall 目标检查（`UNDEFINED_FUNCTION`/`FUNCTION_SCOPE_ACCESS`/`PRIVATE_MEMBER_ACCESS`） | 编译器标准 §8.2 | 通过 | `tc_func_check.c` `tc_func_check_funcall` | `undefined_function`；`function_scope_access`；`private_member_access` |
| F-05 | 阶段 7：funcall 调用位置是否匹配返回类型（`TC_CE_FUNCALL_POSITION`） | 编译器标准 §8.2 | 通过 | `tc_func_check.c` | `funcall_position` |
| F-06 | 阶段 7：实参检查（缺失/重复/未知/顺序/类型） | 编译器标准 §8.2 | 通过 | `tc_func_check.c` | `argument_*`；`funcall_*` |
| F-07 | 阶段 7：调用结果类型检查（`TC_CE_FUNCALL_RESULT_TYPE`） | 编译器标准 §8.2 | 通过 | `tc_func_check.c` | `funcall_result_type` |
| F-08 | 阶段 8：return 在函数体内（`TC_CE_RETURN_OUTSIDE_FUNCTION`） | 编译器标准 §8.3 | 通过 | `tc_func_check.c` `tc_func_check_return` | `return_*` |
| F-09 | 阶段 8：return 形式匹配（有值/无值 vs 返回类型，`TC_CE_RETURN_FORM`） | 编译器标准 §8.3 | 通过 | `tc_func_check.c` | `return_*`；`phase5_void_funcall` |
| F-10 | 阶段 8：return 操作数类型（`TC_CE_RETURN_TYPE`） | 编译器标准 §8.3 | 通过 | `tc_func_check.c` | `return_*`；`phase5_funcall_return` |
| F-11 | 阶段 12：调用图环检查 → `TC_CE_RECURSION`（含自递归） | 编译器标准 §8.8–§8.9 | 通过 | `tc_callgraph.c` `tc_callgraph_check` | `recursion_*` |
| F-12 | 递归环确定性选择算法 | 编译器标准 §8.9 | 通过 | `tc_callgraph.c` SCC/报告边选择 | `recursion_*`；unit 覆盖于模块测试链 |
| F-13 | 按值只读形参：入口已初始化、不得赋值 → `TC_CE_PARAMETER_ASSIGNMENT` | 编译器标准 §8.1 | 通过 | `tc_func_check.c` `tc_func_check_writable_target` | `parameter_assignment`；`parameter_assignment_read` |
| F-14 | 形参类型检查：标量/memblock/ptr/struct 按值语义 | 编译器标准 §8.1 | 通过 | `tc_func_check.c` + pass2 | `phase5_struct_funcall`；`phase5_ptr_*`；`funcall_memblock_size` |
| F-15 | memblock 形参传参时比较 `N` → `TC_CE_MEMBLOCK_SIZE_MISMATCH` | 编译器标准 §8.1 | 通过 | `tc_func_check.c`/`tc_memblock_check.c` | `funcall_memblock_size` |
| F-16 | ptr 形参按值传递：复制指针值，共享所指对象 | 编译器标准 §8.1 | 通过 | `tc_call_frame.c` + `tc_ptr_exec.c` | `phase5_ptr_basic`；`phase5_struct_ptr_field` |
| F-17 | 调用帧实现（被调函数标识、返回地址、参数槽、局部槽、返回值位置） | 编译器标准 §8.6 | 通过 | `tc_call_frame.c` | `phase5_funcall_return`；`phase5_nested_funcall`；`test_executor` |
| F-18 | 调用步骤：实参求值 → 帧建立 → 执行函数体 → return 销毁帧 | 编译器标准 §8.7 | 通过 | `tc_executor.c` + `tc_call_frame.c` | `phase5_funcall_return`；`phase5_nested_funcall`；AOT 同用例差分 |

---

## 7. 控制流与确定初始化（C-01～C-15）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| C-01 | 阶段 6a：`goto`/`label` 无 `func` 祖先 → `GOTO/LABEL_OUTSIDE_FUNCTION` | 编译器标准 §7.5 | 通过 | `tc_cfg.c`/`tc_analyzer_pass2.c` | `goto_outside_function`；`label_outside_function`；`goto_toplevel_*` |
| C-02 | 阶段 6a：`goto`/`label` 有 `while` 祖先 → `GOTO/LABEL_INSIDE_LOOP` | 编译器标准 §7.5 | 通过 | 同上 | `goto_inside_loop`；`label_inside_loop` |
| C-03 | 阶段 6b：作用域预建（函数表、标签表、本库成员索引） | 编译器标准 §9.1 | 通过 | `tc_scope.c` + pass1 | `test_analyzer`；`phase4_func_goto` |
| C-04 | 阶段 6c：goto/label 名称解析（祖先链查找、跨控制流域拒绝） | 编译器标准 §7.5 | 通过 | `tc_cfg.c`/`tc_analyzer_pass2.c` | `goto_undefined`；`goto_into_block`；`goto_sibling` |
| C-05 | 阶段 10：静态布尔三态判定（static true/false/unknown）；形态归类见 [语言标准 §5.2.2] | 编译器标准 §5.2.4；语言标准 §5.2.2 | 通过 | `tc_const_eval.c` `tc_try_eval_static_bool*` | `test_analyzer`；`uninit_const_condition_*` |
| C-06 | 阶段 10：逻辑 RHS 读边判定 | 编译器标准 §6.5.2.2 | 通过 | `tc_cfg.c` + `tc_const_eval.c` | `test_cfg`；`uninit_shortcircuit*`；`let_logic_short_circuit` |
| C-07 | 阶段 11：多域 CFG 构建（顶层独立 + 各函数独立） | 编译器标准 §9.2.1 | 通过 | `tc_cfg.c` `tc_cfg_build_all`/`TcCfgSet` | `test_cfg`；`unreachable_after_return`；`missing_return` |
| C-08 | 阶段 11：CFG 可达性（函数入口为各自可达根，不因未调用而判不可达） | 编译器标准 §9.2.1 | 通过 | `tc_cfg.c` | `test_cfg`；`phase4_func_goto` |
| C-09 | 阶段 11：不可达语句 → `TC_CE_UNREACHABLE_STATEMENT` | 编译器标准 §9.2.1 | 通过 | `tc_cfg.c`/`tc_analyze_definite_init_all` | `unreachable_after_return` |
| C-10 | 阶段 11：确定初始化固定点（最大固定点语义，`IN`/`OUT` 传递函数） | 编译器标准 §9.2.1 | 通过 | `tc_cfg.c` `tc_analyze_definite_init*` | `test_cfg`；`uninit_*` |
| C-11 | 阶段 11：`TC_CE_UNINITIALIZED_VARIABLE`（运行时绑定读取时 `∉ IN`） | 编译器标准 §9.2.1 | 通过 | 同上 | `uninit_simple`/`chain`/`if_path`/`goto_skip_init` |
| C-12 | 阶段 11：函数末尾缺少返回 → `TC_CE_MISSING_RETURN` | 编译器标准 §9.2.1 | 通过 | `tc_cfg.c` | `missing_return` |
| C-13 | 活动作用域与边规范化（离开块时清除退出绑定状态） | 编译器标准 §9.2.1 | 通过 | `tc_cfg.c` + `tc_analyzer_dfa.c` | `if_cross_block_ref_*`；`test_cfg` |
| C-14 | 初始化错误分层（语法层→名称层→数据流层） | 编译器标准 §9.3 | 通过 | `tc_analyzer.c` 阶段序 | `var_missing_initializer`；`undefined_variable`；`uninit_*` |
| C-15 | `break`/`continue` 循环外 → `BREAK/CONTINUE_OUTSIDE_LOOP` | 编译器标准 §7.4 | 通过 | `tc_analyzer_pass2.c` | `break_outside_loop`；`continue_outside_loop` |

---

## 8. 表达式与运算（E-01～E-25）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| E-01 | 算术运算类型一致性（操作数与显式 `T` 一致） | 编译器标准 §6.3.1 | 通过 | `tc_analyzer_pass2.c` + `tc_semantics.c` | `type_mismatch*`；`fp_basic` |
| E-02 | 算术模式验证矩阵（合法模式 vs `TC_CE_MODE_MISMATCH`） | 编译器标准 §6.0 | 通过 | `tc_analyzer_pass2.c` | `wrap_mode_error*`；`fp_*wrap*_mode_mismatch` |
| E-03 | 浮点执行环境约束（roundTiesToEven、禁止FMA、保留非规格化数） | 编译器标准 §6.0.2 | 通过 | `tc_sem_fp.c` 逐步舍入 | `let_float32_step_rounding`（逐步 RTE）；`fp_exact_subnormal`（精确非规格化）；unit `test_fp_round_ties_to_even` / `test_fp_exact_subnormal_*`；语言无 FMA 算子 |
| E-04 | 浮点 `mod` 商向零截断语义 | 编译器标准 §6.0.3 | 通过 | `tc_sem_fp.c` | `fp_arith`；`fp_mod_type_error` |
| E-05 | 浮点异常优先级（Invalid → DivByZero → Overflow → Underflow） | 编译器标准 §6.3.3 | 通过 | `tc_sem_fp.c` | `fp_strict_invalid_before_divzero`（0/0→invalid）；`fp_div_zero`/`fp_strict_*` 分项；unit `test_fp_strict_exception_priority` |
| E-06 | 位运算类型约束（仅整数或 `bool`） | 编译器标准 §6.4 | 通过 | `tc_sem_bitwise.c` | `bitwise_*`；`fp_bitwise_type_error` |
| E-07 | 比较运算：标识符类型与显式 `T` 不一致 → `TC_CE_COMPARISON_TYPE_MISMATCH` | 编译器标准 §6.5.1 | 通过 | `tc_analyzer_pass2.c` | `compare_type_mismatch`；`compare_type_mismatch_var` |
| E-08 | 逻辑运算短路规则（`and`/`or` 短路，`xor` 不短路） | 编译器标准 §6.5.2 | 通过 | `tc_executor.c`/`tc_const_eval.c` | `logic_ops`；`let_logic_short_circuit`；`uninit_shortcircuit*` |
| E-09 | `cast` 严格模式范围检查 → `TC_RE_CAST_OVERFLOW` | 编译器标准 §3.7 | 通过 | `tc_sem_cast.c` | `cast_strict_overflow*`；`fp_cast_overflow` |
| E-10 | `cast(T, truncate, operand)` 仅整数缩窄验证 | 编译器标准 §3.7 | 通过 | `tc_sem_cast.c` | `truncate_cast`；`cast_truncate_bool_source_error` |
| E-11 | `bitcast` 等宽验证 → `TC_CE_BITCAST_WIDTH` | 编译器标准 §3.7 | 通过 | `tc_sem_cast.c` | `bitcast_width_mismatch`；`bitcast_roundtrip*` |
| E-12 | `memblock_load`：显式 `T`、边界检查 `0 ≤ i < N` | 编译器标准 §6.6.1 | 通过 | `tc_memblock_check.c` + `tc_memblock_exec.c` | `phase5_memblock_basic`；`memblock_oob_rt`；`memblock_index_oob` |
| E-13 | `memblock_store`：可写验证、边界检查 | 编译器标准 §6.6.2 | 通过 | 同上 | `phase3_memblock_store`；`memblock_oob_store_rt`；`memblock_store_oob` |
| E-14 | `memblock_copy`：区间检查、memmove 语义 | 编译器标准 §6.6.3 | 通过 | `tc_memblock_exec.c`/`tc_aot_rt.c` | `phase5_memblock_copy`；`memblock_copy_size` |
| E-15 | `ptr_load`：`nullptr` → `TC_RE_NULL_POINTER_DEREFERENCE` | 编译器标准 §6.7 | 通过 | `tc_ptr_exec.c`/`tc_aot_ptr_load` | `null_ptr_deref`；`phase5_ptr_basic` |
| E-16 | `ptr_store`：可写验证 + 空指针 | 编译器标准 §6.7 | 通过 | `tc_ptr_check.c` + `tc_ptr_exec.c` | `ptr_store_readonly`；`null_ptr_store` |
| E-17 | `ptr_address`：仅可写绑定 | 编译器标准 §6.7 | 通过 | `tc_ptr_check.c` | `ptr_address_const`；`phase5_ptr_basic` |
| E-18 | `ptr_add`/`ptr_sub`：`nullptr` → `TC_RE_NULL_POINTER_ARITHMETIC` | 编译器标准 §6.7 | 通过 | `tc_ptr_exec.c`/`tc_aot_ptr_arith` | `null_ptr_arith`；`phase5_ptr_arith_cmp` |
| E-19 | `ptr_eq`/`ptr_ne`：允许 `nullptr` 比较 | 编译器标准 §6.7 | 通过 | `tc_ptr_exec.c` | `phase5_nullptr_eq`；`phase5_ptr_cmp_more` |
| E-20 | `ptr_lt`～`ptr_ge` 序关系比较：非 `nullptr` + 同型约束 | 编译器标准 §6.7 | 通过 | `tc_ptr_check.c` + exec | `phase5_ptr_cmp_more`；`null_ptr_cmp`；`ptr_compare_not_bool` |
| E-21 | `ptr_size`：编译期常量，返回 `sizeof_bits(T)` | 编译器标准 §6.7 | 通过 | `tc_ptr_check.c`/`tc_aot_ptr_size` | `ptr_size_not_usize`；`phase3_ptr_ops` |
| E-22 | `memcopy_unsafe`：空指针→解引用、负长度→无效范围、memmove 语义 | 编译器标准 §6.7 | 通过 | `tc_ptr_exec.c`/`tc_aot_rt.c` | `memcopy_unsafe_null`；`memcopy_unsafe_neg` |
| E-23 | 赋值语句：左侧可变性检查（`let` → `CONSTANT_ASSIGNMENT`，形参 → `PARAMETER_ASSIGNMENT`） | 编译器标准 §6.2 | 通过 | `tc_analyzer_pass2.c`/`tc_func_check.c` | `const_assign`；`assign_to_let`；`parameter_assignment` |
| E-24 | 字段赋值双层可变性 | 编译器标准 §6.2 | 通过 | `tc_struct_check.c` | `struct_immutable_field`；`struct_assign_let_outer_*`；`struct_assign_through_param`/`param_let`；`phase5_struct_mut_matrix_ok` |
| E-25 | `bool` ↔ 整数/浮点转换规范字节 | 编译器标准 §3.7 | 通过 | `tc_sem_cast.c` | `bool_cast`；`format_bool` |

---

## 9. 常量求值与 let（K-01～K-12）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| K-01 | `let` 常量表达式形态验证（拒绝嵌套调用、`var` 引用、非法形态） | 编译器标准 §5.2.1 | 通过 | `tc_parser_rhs.c` + `tc_const_eval.c` | `let_nested_call`；`let_non_literal`；`let_short_circuit_invalid_rhs` |
| K-02 | `let` 声明类型闭合检查顺序 | 编译器标准 §5.2.2 | 通过 | `tc_analyzer_pass2.c`/`tc_const_eval.c` | `let_constant*`；`forward_reference` |
| K-03 | 阶段 9：编译期求值（按源序求值全部 `let`） | 编译器标准 §5.2.3 | 通过 | `tc_const_eval.c` | `const_expr`；`let_runtime_equivalence` |
| K-04 | 常量溢出 → `TC_CE_CONSTANT_OVERFLOW` | 编译器标准 §5.2.3 | 通过 | `tc_const_eval.c` | `const_overflow`；`bitwise_shl_const_overflow` |
| K-05 | 常量除零 → `TC_CE_CONSTANT_DIV_ZERO` | 编译器标准 §5.2.3 | 通过 | `tc_const_eval.c` | `const_div_zero` |
| K-06 | 常量转换溢出 → `TC_CE_CONSTANT_CAST_OVERFLOW` | 编译器标准 §5.2.3 | 通过 | `tc_const_eval.c` | `let_const_cast_overflow`；`let_const_cast_overflow_fp` |
| K-07 | 浮点无效操作/负移位 → `TC_CE_CONSTANT_EXPRESSION` | 编译器标准 §5.2.3 | 通过 | `tc_const_eval.c` | `const_expr`；`fp_const_expr` |
| K-08 | 浮点常量算术精度规则（binary32/binary64 独立精度、RTE、禁止 FMA） | 编译器标准 §5.2.3.1 | 通过 | `tc_const_eval.c` + `tc_sem_fp.c` | `let_float_ieee`；`let_float32_step_rounding`；`fp_const_let_arith` |
| K-09 | `static let` 按依赖拓扑序求值 | 编译器标准 §4.3 | 通过 | `tc_func_check.c` `tc_func_eval_static_lets` | `phase4_static_let`；`static_let_forward` |
| K-10 | `static var` 初始化器编译期约束验证（不执行求值） | 编译器标准 §4.3 | 通过 | `tc_func_check.c` `tc_func_check_static_vars` | `static_var_bad_init` |
| K-11 | `let` 常量求值与运行时语义共享同一纯语义核心 | 编译器标准 §5.2.3 | 通过 | `tc_const_eval.c` 委托 `tc_semantics`/`tc_sem_*` | `let_runtime_equivalence`；`let_wrap_allowed` |
| K-12 | 合法常量形态的求值错误不降级为 `unknown` | 编译器标准 §5.2.4 | 通过 | `tc_const_eval.c`/`tc_try_eval_static_bool*` | `test_analyzer`；`const_overflow`/`const_div_zero` |

---

## 10. I/O（I-01～I-10）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| I-01 | 阶段 6e：格式说明符确定性拆分算法 | 编译器标准 §10.1 | 通过 | `tc_io.c` 格式拆分 | `test_io`；`format_spec_all` |
| I-02 | 格式控制项合法性检查（含 `width`/`precision` 上界 `65535`）→ `TC_CE_FORMAT_SPECIFIER` | 编译器标准 §10.1；语言标准 §10.5 | 通过 | `tc_io.c`/`tc_analyzer_pass2.c` | `format_string_error` |
| I-03 | 13 种格式转换符兼容性检查 → `TC_CE_FORMAT_TYPE_MISMATCH` | 编译器标准 §10.2 | 通过 | 同上 | `format_type_mismatch*`；`format_fp_type_mismatch` |
| I-04 | `write`/`writeln` 显式类型与操作数一致性 | 编译器标准 §10.2 | 通过 | `tc_analyzer_pass2.c` + `tc_io.c` | `format_output`；`io_extended` |
| I-05 | `read` 目标变量名称/类型/确定初始化检查 | 编译器标准 §10.3 | 通过 | `tc_analyzer_pass2.c` | `read_write`；`parameter_assignment_read` |
| I-06 | `read` 输入 Token 验证（EBNF：signed/unsigned/bool/float） | 编译器标准 §10.3 | 通过 | `tc_io.c` | `read_invalid*`；`read_bool_invalid_input`；`read_fp_invalid` |
| I-07 | 浮点输入转换规则（精确十进制→roundTiesToEven 直接舍入） | 编译器标准 §10.3 | 通过 | `tc_io.c` | `test_io` RTE 用例；`fp_io`；`read_fp_invalid` |
| I-08 | 格式转换符定义与确定性输出规则 | 编译器标准 §10.4 | 通过 | `tc_io.c` | `format_hex_bin`；`format_bool`；`format_spec_fp` |
| I-09 | C99 风格格式控制实现（标志、宽度、精度） | 编译器标准 §10.5 | 通过 | `tc_io.c` | `format_spec_all`；`io_extended` |
| I-10 | 输出原子提交规则 | 编译器标准 §10.5 | 通过 | `tc_io.c` `tc_io_write_value`：open_memstream 渲染后一次 `fwrite` | unit `test_write_atomic_commit_no_partial`（半写拒绝→零字节）；`test_write_stream_failure` |

---

## 11. VM 执行器（V-01～V-12）

| ID | 检查项 | 设计文档依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | ------------ | ---- | -------- | -------- |
| V-01 | 输入契约：仅接受成功编译的 typed program | VM 设计说明书 §11.1 | 通过 | `tc_lib.c` → `tc_executor.c` | `scripts/vm/run_tests.sh`；`--check` 失败不执行 |
| V-02 | 控制信号模型（NORMAL/BREAK/CONTINUE/GOTO/RETURN/ERROR） | VM 设计说明书 §11.2 | 通过 | `tc_executor.c` | `while_break_continue`；`phase4_func_goto`；`phase5_funcall_return` |
| V-03 | 顶层执行：按源序执行语句、`return` 已在静态阶段拒绝 | VM 设计说明书 §11.3 | 通过 | `tc_executor.c` | valid 顶层用例；`return_*` static |
| V-04 | 函数执行：创建调用帧 → 执行函数体 → return 传播返回值 | VM 设计说明书 §11.4 | 通过 | `tc_call_frame.c`/`tc_executor.c` | `phase5_funcall_return`；`phase5_nested_funcall` |
| V-05 | while 循环执行（条件求值 → 体执行 → 控制信号处理） | VM 设计说明书 §11.5 | 通过 | `tc_executor.c` | `while_counted`；`while_break_continue`；`while_nested` |
| V-06 | goto/label 执行（零成本标签、向后跳覆盖槽、向外跳清除绑定） | VM 设计说明书 §11.6 | 通过 | `tc_executor.c` | `phase4_func_goto`；`let_goto_inline` |
| V-07 | 运行时槽管理（memblock/struct 堆指针存于 `slots[]`，非独立 `struct_storage[]`） | VM 设计说明书 §11.7 | 通过 | `tc_executor.c` + `tc_memblock_exec.c`/`tc_struct_exec.c` | `phase5_memblock_*`；`phase5_struct_*`；槽模型见实现 |
| V-08 | 模块初始化：按依赖拓扑序执行 `static var` 初始化器 | VM 设计说明书 §11.8 | 通过 | `tc_executor.c`/`tc_lib.c` `tc_run_program` | `phase5_static_var`；`test_libtc` |
| V-09 | 整数执行（strict/wrap 模式、isize/usize 同字长语义） | VM 设计说明书 §12.2 | 通过 | `tc_sem_int.c` + executor | `wrap_arithmetic_all`；`isize_arith`/`usize_arith`；`signed_strict_overflow*` |
| V-10 | 浮点执行（strict/ieee 模式、每步精度舍入、异常优先级） | VM 设计说明书 §12.3 | 通过 | `tc_sem_fp.c` | `fp_arith`/`fp_arith_ieee`；`fp_strict_*` |
| V-11 | 指针操作执行（ptr_load/store/address/add/sub/cmp/size） | VM 设计说明书 §12.7 | 通过 | `tc_ptr_exec.c` | `phase5_ptr_*`；`null_ptr_*` |
| V-12 | memblock 操作执行（load/store/copy 含边界检查） | VM 设计说明书 §12.8 | 通过 | `tc_memblock_exec.c` | `phase5_memblock_*`；`memblock_oob_*` |

---

## 12. AOT 代码生成（A-01～A-12）

| ID | 检查项 | 设计文档依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | ------------ | ---- | -------- | -------- |
| A-01 | 输入契约：仅接受成功编译的 typed program | AOT 设计说明书 §3 | 通过 | `tc_aot_codegen.c`/`aot/main.c` | `scripts/aot/run_tests.sh` static/runtime 门禁 |
| A-02 | C99 目标骨架生成（统一 `slots[]`、函数前向声明、static var 初始化） | AOT 设计说明书 §4.1 | 通过 | `tc_aot_codegen.c` 发射 `slots[]` | AOT 差分；`phase5_static_var`；无独立 `struct_storage[]` |
| A-03 | 固定槽位与 RHS 求值生成 | AOT 设计说明书 §4.2 | 通过 | `tc_aot_codegen.c` | `check_rhs_coverage.py`；AOT valid 差分 |
| A-04 | 控制流代码生成（if → 原生 C if/else；while → for(;;)+显式条件） | AOT 设计说明书 §6 | 通过 | `tc_aot_codegen.c` | `if_*`/`while_*` AOT 差分 |
| A-05 | goto/label C 代码生成（唯一 C 标签） | AOT 设计说明书 §6.3 | 通过 | `tc_aot_codegen.c` | `phase4_func_goto` AOT 路径；`let_goto_inline` |
| A-06 | 函数代码生成（声明/定义/调用约定/return 传播） | AOT 设计说明书 §7 | 通过 | `tc_aot_codegen.c` | `phase5_funcall_return`/`nested_funcall` AOT 差分 |
| A-07 | 模块代码生成（多文件合并、static var 拓扑初始化） | AOT 设计说明书 §8 | 通过 | `tc_aot_codegen.c` `tc_init_static_vars` | `phase5_static_var`；modules 相关 AOT |
| A-08 | 指针操作 shim 代码生成 | AOT 设计说明书 §11.1 | 通过 | `tc_aot_rt.c` `tc_aot_ptr_*` + codegen | `phase5_ptr_*` AOT 差分；`null_ptr_*` |
| A-09 | memblock 操作 shim 代码生成（含 memcopy_unsafe） | AOT 设计说明书 §11.2–§11.3 | 通过 | `tc_aot_rt.c` `tc_aot_memblock_*` | `phase5_memblock_*`；`memcopy_unsafe_*` AOT |
| A-10 | bitcast 位模式策略（memcpy 而非 union punning） | AOT 设计说明书 §12.1 | 通过 | `tc_aot_codegen.c`/`tc_sem_cast.c` | `bitcast_roundtrip*` AOT 差分 |
| A-11 | `let`/`static let` 常量发射（十六进制位模式，不用十进制） | AOT 设计说明书 §13 | 通过 | `tc_aot_codegen.c` | AOT let 无槽断言；`phase4_static_let` |
| A-12 | 生成 C 以 `-std=c99 -Wall -Wextra -Werror -pedantic` 编译 | AOT 设计说明书 §15.2 | 通过 | `aot/main.c` 编译命令字符串 | `scripts/aot/run_tests.sh` 全程以该标志编译 |

---

## 13. libtc API 与诊断（D-01～D-18）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| D-01 | `tc_compile_source` 含 `name` 参数 | libtc API §2 | 通过 | `tc_lib.c`/`tc_lib.h` | `test_libtc` |
| D-02 | `tc_compile_file_opts` 自动加载所有可达模块 | libtc API §4 | 通过 | `tc_lib.c` + `tc_module.c` | `import_ok`；`test_libtc`；`test_module` |
| D-03 | 会话式搜索路径（`TcCompileOptions`）可用 | libtc API §2/§4 | 通过 | `tc_lib.c` `tc_compile_*_opts` | `test_libtc` opts search；`include_search_ok` |
| D-04 | compile 仅成功时转移 `out` 所有权 | libtc 设计说明书 §1.4 | 通过 | `tc_lib.c` | `test_libtc` ownership |
| D-05 | 任一阶段失败完整回滚 | libtc 设计说明书 §6 | 通过 | `tc_analyze_ex`/`tc_lib.c` free 路径 | `test_libtc`；失败 `.tc` 门禁 |
| D-06 | 诊断三域分离（Language/API/Implementation） | libtc 设计说明书 §7.1 | 通过 | `tc_diagnostic.c`/`tc_types.h` | `test_diagnostic`；`test_types` 错误名 |
| D-07 | 首个诊断确定性选择（阶段优先→源位置优先→规则优先） | 编译器标准 §1.3 | 通过 | `tc_analyzer.c` 管线序 | `diag_priority_*`；`test_analyzer` |
| D-08 | 70+ 语言错误码完整映射，打印名无重复 | 编译器标准 §11.4 | 通过 | `tc_error_kind_name()` in `tc_types.c` | `test_types` 白名单唯一性（**91** 种） |
| D-09 | 文件错误不冒充 SyntaxError | libtc 设计说明书 §7.1 | 通过 | `tc_lib.c`/`tc_driver.c` | `test_libtc`；CLI 文件错误路径 |
| D-10 | OOM 含免分配后备消息 | libtc 设计说明书 §6.3 | 通过 | `tc_diagnostic.c` | `test_types` OutOfMemory；`rg TC_ERR_OUT_OF_MEMORY` |
| D-11 | typed program 可重复 VM/AOT 消费 | libtc 设计说明书 §4.3 | 通过 | `TcTypedProgram` + VM/AOT 入口 | VM+AOT 双路径差分门禁 |
| D-12 | TC 无编译警告 | 编译器标准 §11.3 | 通过 | `tc_warning.c` 空壳策略 | `test_warning`；无警告产出门禁 |
| D-13 | `tc_run_program` 含 `static var` 拓扑初始化 | libtc API §5 | 通过 | `tc_lib.c` `tc_run_program` | `test_libtc`；`phase5_static_var` |
| D-14 | 运行时错误单槽 fail-fast，提交规则 | 编译器标准 §11.2 | 通过 | `tc_diagnostic.c` + executor/AOT | runtime stderr 对齐用例；`test_diagnostic` |
| D-15 | `TC_ERR_OUT_OF_MEMORY` 不得用作规模限制替代 | 编译器标准 §11.4 | 通过 | 编码约定：`tc_*` OOM 路径；CLI `-I` 上限独立文案 | `test_oom_not_used_as_size_limit_proxy`；CLI `cli -I path limit is not OutOfMemory` |
| D-16 | 诊断阶段归属正确（Lexer/Parser/模块/函数/Binder/Control/CFG/Const/Runtime） | 编译器标准 §11.4 | 通过 | `tc_analyzer.c` 13 阶段序 | `errors.md` 账本；各域 `.tc` + unit |
| D-17 | 语法拒绝的程序不进入后续语义检查 | 编译器标准 §1.3 | 通过 | `tc_lib.c` 词法/语法失败早退 | `syntax_error`；`indent_*`；`test_parser` |
| D-18 | `TC_CE_OPERAND_COUNT` 仅用于平衡外壳且值操作数不符 | 编译器标准 §1.3 | 通过 | `tc_parser_rhs.c` | `format_operand_count*`；RHS 权威表 |

---

## 14. 发布、文档与清理（R-01～R-10）

| ID | 检查项 | 规范依据 | 状态 | 实现位置 | 测试证据 |
| -- | ------ | -------- | ---- | -------- | -------- |
| R-01 | VM/AOT 版本为 0.0.41 | 版本头文件 | 通过 | `tc_version.h` `TC_VM_VERSION`；`aot/main.c` `TC_AOT_VERSION` | CLI `-V` golden；`scripts/vm/run_tests.sh` |
| R-02 | 设计文档与 Agent 索引同步 | — | 通过 | `docs/*-0.0.41.md`（不含语言标准日常改动）；[AGENTS.md](../AGENTS.md) · [.cursor/README.md](../.cursor/README.md) · `features/*.md` | 文档树 + `check_doc_counts.py` |
| R-03 | RHS 覆盖检查脚本含所有新增 `TcRhsKind` | check_rhs_coverage.py | 通过 | `scripts/sync/check_rhs_coverage.py` | CI/`make` 同步检查；Phase 5 RHS 已纳入 |
| R-04 | 源文件命名检查含所有新增模块 | check_source_naming.py | 通过 | `scripts/sync/check_source_naming.py` | 含 `tc_struct_exec`/`tc_*_check` 等成对模块 |
| R-05 | 架构索引与测试映射同步 | `features.md`/`features/*.md`、`test-map.md` | 通过 | `.cursor/skills/tc-architecture/` | `test-map.md` Phase 1–7 账本 |
| R-06 | Parser free 分发覆盖所有新增 statement/RHS kind | tc_statement_free | 通过 | `tc_parser_free.c` | `check_rhs_coverage.py` free 分发点 |
| R-07 | Analyzer 分发覆盖所有新增 statement/RHS kind | tc_analyzer | 通过 | `tc_analyzer_pass2.c`/`tc_type_check.c` | `check_rhs_coverage.py`；`test_type_check` |
| R-08 | Executor 分发覆盖所有新增 statement/RHS kind | tc_executor | 通过 | `tc_executor.c` + `tc_{ptr,memblock,struct}_exec.c` | `check_rhs_coverage.py`；`phase5_*` |
| R-09 | AOT codegen 分发覆盖所有新增 statement/RHS kind | tc_aot_codegen | 通过 | `tc_aot_codegen.c` + `tc_aot_rt.c`（含 `tc_aot_struct_*`） | `check_rhs_coverage.py`；AOT `phase5_struct_*` |
| R-10 | 全量测试基线（VM + AOT + unit） | scripts/run_tests.sh | 通过 | `scripts/run_tests.sh` + `scripts/{vm,aot}/run_tests.sh` | `make test` / `bash scripts/run_tests.sh`；**2026-07-24 证据**：约 726 VM · ~318 AOT · 3025 unit；**现网（2026-08）**：约 733 VM · ~322 AOT 注册 / ~358 执行 · ~3036 unit |

---

## 15. v0.0.31 → v0.0.41 迁移检视

| 类别 | v0.0.31 | v0.0.41 | 迁移检查 |
| ---- | ------- | ------- | -------- |
| 模块系统 | 单文件 | 多文件 `#program`/`#lib` + `import` | 已落地：`tc_module.c`/`tc_scope.c` + modules 测试 |
| 函数 | 无 | `func`/`funcall`/`return` | 已落地：`tc_func_check.c`/`tc_call_frame.c` + phase4/5 |
| 类型 | 12 种标量 | 标量 + `ptr`/`memblock`/`struct`/`isize`/`usize`/`void` | 已落地：`tc_types.c` + `tc_*_check.c` |
| 运算 | ~15 种 | 增加全部 `ptr_*`、`memblock_*`、`memcopy_unsafe` | 已落地：check/exec/AOT shim + RHS 覆盖脚本 |
| 语句 | ~13 种 | 增加 `func`/`funcall`/`return`/`struct`/`static`/`import` | 已落地：parser + free/analyzer/executor/AOT 分发 |
| 编译管线 | 6 阶段 | 13 阶段（含子阶段） | 已落地：`tc_analyzer.c` `tc_analyze_ex` |
| 错误码 | 41+1 | **91** | 已落地：`tc_error_kind_name` + `test_types` |
| CFG | 单一域 | 多域（顶层 + 各函数） | 已落地：`TcCfgSet`/`tc_cfg_build_all` |
| REPL | 包含 | 无 | 已落地：无 `tc_repl`；CLI 无 `--repl` |
| struct 运行时 | 无 | 堆对象经 `slots[]` | 已落地：`tc_struct_exec.c` + `tc_aot_struct_*` |
| TC-Embed | 无 | VM/AOT 双模式 C 宿主 API | 已落地：`src/vm/embed/` + `check-embed` / `check-embed-aot` |
| 双层可变性 | 无 | 外层绑定 × 字段 `let`/`var` | 已落地：`struct_immutable_field`/`struct_assign_let_outer_*`/`struct_assign_through_param`/`param_let`/`phase5_struct_mut_matrix_ok` |

---

## 16. 最终发布结论

截至 **2026-08-23**，TC 0.0.41 Phase 1–7（含 struct 运行时与 TC-Embed）已落地。本报告约 **183** 项检查点已回填实现位置与测试证据；**T-05a**（struct 指针自引用，[语言标准 §3.9.1]）已于 **2026-08-16** 落地：`TcType.pending_name` 承载未决结构体名，字段类型树按位置规则校验并解析为 struct_id，同时补齐 `ptr<S>`/`ptr<ptr<S>>`/`memblock<ptr<S>,N>` 自引用、`ptr<B>` 更早结构体指针、`memblock<B,N>` 字段的 VM/AOT 运行时值语义（内联存储 + 深拷贝）与测试，其余几乎全部为「通过」。

**发布判断**：实现满足 0.0.41 发布条件（在现有门禁下）。合规证据以本报告、[`.cursor/skills/tc-architecture/test-map.md`](../.cursor/skills/tc-architecture/test-map.md)、以及 `scripts/vm/run_tests.sh` / `scripts/aot/run_tests.sh` / `scripts/run_tests.sh` 为准。

关键实现事实摘要：

- **版本**：VM/AOT 均为 **v0.0.41**
- **管线**：13 阶段确定性分析入口在 `tc_analyzer.c`（`tc_analyze_ex`）
- **struct 运行时**：`src/vm/executor/tc_struct_exec.c`；AOT shim `tc_aot_struct_*` in `tc_aot_rt.c`
- **槽模型**：memblock/struct 堆指针存于统一 `slots[]`，非独立 `struct_storage[]`
- **Embed**：`src/vm/embed/`；`check-embed` / `check-embed-aot`；详设 `docs/TC-Embed详细设计说明书-0.0.41.md`
- **REPL**：无
- **双层可变性**：`struct_immutable_field`、`struct_assign_let_outer_*`、`struct_assign_through_param` / `param_let`、`phase5_struct_mut_matrix_ok`
- **结构体自引用**（T-05a，2026-08-16 落地）：`TcType.pending_name` + `tc_struct_validate_type_node` / `tc_struct_resolve_type_tree`；测试 `phase5_struct_ptr_self_ref` / `_nested_self_ref` / `_roundtrip` / `_memblock_of_struct` / `_memblock_deepcopy`、`struct_self_ref` / `struct_memblock_self_ref` / `struct_ptr_fwd_ref` / `struct_memblock_undefined` / `ptr_undefined_struct` / `ptr_struct_type_distinct`、unit `test_struct_self_reference`

少数项因细则难以用独立自动化用例完全证明，标为「通过（实现存在；专项测试薄弱）」：无。
I-10 / D-15 已由 unit + CLI 专项覆盖（2026-07-24 补测）。
E-03 / E-05 已由 `.tc` + unit 专项覆盖（2026-07-24 补测）。

---

*语言合法性与可观察语义以 [TC 语言标准 0.0.41](./TC语言标准设计说明书-0.0.41.md) 与 [TC 编译器标准 0.0.41](./TC编译器标准设计说明书-0.0.41.md) 为准。*

# TC 编译器 设计实现合规审查报告

> **审查日期**：2026-07-06（独立复验，结论一致）  
> **版本**：v0.0.23  
> **对照文档**：`TC语言标准` / `TC-VM` / `TC-AOT` / `libtc` 设计说明书；知识图谱 `@knowledge-graph`（8 分发点 / let / 短路 / 重载）  
> **审查对象**：`src/vm/`、`src/aot/`、`src/libtc/`  
> **方法**：设计文档逐节对照源码；自动化脚本 + 全量测试复验

---

## 结论

**225/225 检查项全部合规（100%）**。无已知不合规项；此前 4 处可维护性瑕疵（过时注释、防御分支、风格统一、`void` API 确认）均已处理，语义无变更。

| 模块 | 合规 | 不合规 |
|------|------|--------|
| 类型系统 `tc_types.h` | 27 | 0 |
| 词法 `tc_lexer.c` | 18 | 0 |
| 语法 `tc_parser.c` | 29 | 0 |
| 静态分析 `tc_analyzer.c` | 37 | 0 |
| 常量求值 `tc_const_eval.c` | 17 | 0 |
| 语义 `tc_semantics.c` | 40 | 0 |
| 执行器 `tc_executor.c` | 18 | 0 |
| I/O `tc_io.c` | 18 | 0 |
| AOT `tc_aot_codegen.c` + `rt.c` | 17 | 0 |
| 错误诊断 `tc_diagnostic.c` | 8 | 0 |
| libtc `tc_lib.c` | 6 | 0 |
| 位运算/移位（v0.0.23 专项） | 8 | 0 |
| **合计** | **225** | **0** |

---

## 复验证据

| 步骤 | 命令 | 结果 |
|------|------|------|
| 全量测试 | `make test`（VM + unit + AOT） | 全通过 |
| RHS 分发点 | `python3 scripts/sync/check_rhs_coverage.py` | 12 × 8 全覆盖 |
| 源文件命名 | `python3 scripts/sync/check_source_naming.py` | `tc_*.h` ↔ `tc_*.c` 一致 |
| 知识图谱抽样 | let 禁 wrap、逻辑短路三侧、整数重载无短路、移位 k 不掩码 | 一致 |
| 设计文档对照 | 各模块检查表（见下节） | 225/225 |

分项命令：`bash scripts/vm/run_tests.sh`、`make test-unit`、`bash scripts/aot/run_tests.sh`。v0.0.23 位运算用例已注册于 VM（含 static `--check` 双路径）与 AOT 差分脚本。

---

## 模块合规摘要

以下按编译流水线顺序归纳已验证的设计要点；细项编号与对照源码一一对应，此处仅保留结论性描述。

### 类型系统（27）

- `TcIntType` 8 种定宽整数 + `bool`；`TcWrapMode` / `TcTruncateMode` / 算术·比较·逻辑·格式·符号枚举完整
- `TcValue { type, bits }` 统一容器；`TcLiteral` 含 `magnitude/negative/unsigned_suffix/is_bool`，`is_bool` 与整数 `0/1` 可区分

### 词法（18）

- 标识符 ASCII；四进制整数字面量（十/十六/二/八）、`_` 分隔符、`u` 后缀与范围/前导零校验
- `true`/`false` → `TC_TOK_BOOL_LIT`；`;` 行注释；非法 `%` 格式报错

### 语法（29）

- 语句分派：`var`/`let`/I/O/赋值；可选 `;`
- 12 种 `TcRhsKind` 解析分支完整（含 `BITWISE_BIN/UN`、`SHIFT`）
- 关键字约束：`wrap`/`truncate` 禁用于非法上下文；cast 源须变量；let 必有 RHS；按位/移位禁模式关键字；let 中 `shl wrap` 在 Parser 拒绝

### 静态分析（37）

- Pass1 符号收集与 Pass2 源序可见性；重复/前向引用/自引用检测
- 字面量种类与范围；I/O 格式符与操作数数量；未初始化变量警告（含 `read` 初始化、`last_init_stmt_index`）
- **逻辑短路**：`and`/`or` 字面量 lhs 抑制 rhs 未初始化警告；非字面量不抑制
- **整数按位无短路**：`BITWISE_BIN/UN` 两侧均检查；`shr` + `wrap` → 关键字错误

### 常量求值（17）

- let RHS 委托 `tc_exec_*`（strict）；禁 `wrap`/`truncate`、禁 var 引用、循环依赖与缩窄 cast 检测
- 位运算/移位编译期求值；let 逻辑短路（按 `bits`）；let 禁 `shl wrap`

### 语义（40）

- 双目/单目算术：strict 溢出检测、wrap 二补数、除零与 `INT_MIN/-1` 特例、64 位乘法分块
- 比较返回 bool；逻辑短路求值；cast 加宽/缩窄/truncate 与 `bool`↔整数规则
- 位运算 and/or/xor/not 无溢出；移位 shl strict/wrap、shr 算术/逻辑、k 不掩码

### 执行器（18）

- 槽位 = 符号表 count，毒化 `0xFE`；VarDef/ConstDef/Assign/I/O 分派
- 逻辑短路在 `tc_eval_rhs`；整数按位无短路；let 常量读 `const_value` 不求值

### I/O（18）

- 无格式与 `%d/%i/%u/%x/%X/%o/%b/%t` 输出；`write`/`writeln`；int8/uint8 走整数路径
- `read` 有/无符号与 bool（小写敏感）解析及范围校验

### AOT（17）

- 语义与 I/O 委托 `tc_semantics` / `tc_io`；12 种 RHS codegen；let 编译期折叠
- shim 覆盖 `tc_aot_bitwise_*`、`tc_aot_shift`

### 错误诊断（8）

- 22 种 `TcErrorKind`（含 `TC_ERR_SYNTAX`）；1 种 `TC_WARN_UNINITIALIZED_VARIABLE`
- `TcDiagnostic` 含 snippet/caret；fail-fast 单槽

### libtc（6）

- `tc_compile_source` / `tc_compile_file` / `tc_run_typed` 签名与 fail-fast 契约
- Parse 失败不改 `out`；`TC_BENCH=1` 三阶段计时

---

## 跨模块要点（v0.0.23）

| 主题 | 设计要求 | 实现状态 |
|------|---------|---------|
| 类型/词法 | `TcBitwiseOp`/`TcShiftOp`；`xor`/`shl`/`shr` token；`and`/`or`/`not` 按类型参数分派 | ✅ |
| 语义 | 按位无溢出；shl strict/wrap；shr 禁 wrap | ✅ |
| let | 编译期位运算；禁 wrap | ✅ |
| 8 分发点 | parser / analyzer / const_eval / executor / aot_codegen / aot_rt 全覆盖 | ✅ |
| 测试 | valid `bitwise_*` 系列；static 关键字/类型错误；runtime overflow；unit `test_bitwise`/`test_shift`；AOT 差分 | ✅ |

---

## TcRhsKind 分发点

12 种 Kind：`LIT` `CONST_REF` `ARITH` `UNARY` `COMPARE` `LOGIC_BIN` `LOGIC_UN` `BITWISE_BIN` `BITWISE_UN` `SHIFT` `CAST` `CONST_CAST`

| 分发点 | 文件 |
|--------|------|
| `tc_parse_rhs` / `tc_parse_const_rhs` / `tc_rhs_free` | `parser/tc_parser.c` |
| `tc_check_rhs` | `analyzer/tc_analyzer.c` |
| `tc_eval_const_rhs` | `analyzer/tc_const_eval.c` |
| `tc_eval_rhs` | `executor/tc_executor.c` |
| `tc_aot_emit_rhs` + shim | `aot/tc_aot_codegen.c`、`aot/tc_aot_rt.c` |

复验：`python3 scripts/sync/check_rhs_coverage.py` → 全覆盖（`CAST` 为 emit 链 fallthrough，脚本已标注）。

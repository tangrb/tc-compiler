# TC 语言标准设计说明书

> **版本**：0.0.31（规范收敛版）
> **作者**：唐荣兵（[yanhuang8923@qq.com](mailto:yanhuang8923@qq.com)）

> **版本边界**：本文仅定义 TC 0.0.31 的语言规范，不表示对应实现已经交付。本文编写时，libtc / TC-VM / TC-AOT 的现行实现版本仍为 v0.0.26；实现须在完成本文全部强制规则后，方可声明符合 TC 0.0.31。除显式标记为“非规范性”的实现说明外，符合 0.0.31 的实现必须接受、拒绝同一组程序，并产生本文规定的可观察结果。

---

## 目录

### 第一部分：基础与核心

1. [概述](#1-概述)
2. [词法结构](#2-词法结构)
3. [类型系统](#3-类型系统)

### 第二部分：语句与表达式

4. [程序结构与语句](#4-程序结构与语句)
5. [算术运算](#5-算术运算)
6. [位运算](#6-位运算)
7. [比较运算与逻辑运算](#7-比较运算与逻辑运算)
8. [类型转换](#8-类型转换)

### 第三部分：I/O 与交互

9. [标准 I/O](#9-标准-io)

### 第四部分：完整示例与规范

10. [完整示例集](#10-完整示例集)
11. [错误与警告](#11-错误与警告)（含 §11.0 确定性诊断阶段与初始化错误分工）

### 第五部分：附录

- [附录 A：形式化语法（EBNF）](#附录-a形式化语法ebnf)
- [附录 B：与 C 语言标量语义对照](#附录-b与-c-语言标量语义对照)
- [附录 C：各类型边界常量速查](#附录-c各类型边界常量速查)
- [附录 D：扩展路线图](#附录-d扩展路线图)
- [附录 E：文档修订记录](#附录-e文档修订记录)
- [附录 F：0.0.30 → 0.0.31 迁移指南](#附录-f0030--0031-迁移指南)

---

# 第一部分：基础与核心

---

## 1. 概述

### 1.1 语言定位

TC 是一门伪汇编语言：语法接近高级语言的赋值表达式，执行模型保留汇编式的 **逐条语句** 顺序执行。每条语句至多包含 **一个** 运算或转换调用，**不支持** 表达式嵌套。

本文档定义：

- **8 种** 固定宽度整数类型（`int8`～`uint64`）
- **2 种** 浮点类型（`float32`、`float64`），符合 IEEE 754 标准
- **1 种** 布尔类型（`bool`，取值为 `true`/`false`）
- **7 类** 算术运算指令：
  - 双目运算（5 类）：`add`、`sub`、`mul`、`div`、`mod`
  - 单目运算（2 类）：`abs`（取绝对值）、`neg`（取负数）
  - 以上运算统一支持整数和浮点类型（除 `mod` 仅支持整数）
- **4 类** 位运算指令：`and`、`or`、`xor`（按位逻辑）、`not`（按位取反）
  - 仅支持整数类型，浮点类型不支持
- **2 类** 移位运算指令：`shl`（左移）、`shr`（右移）
  - 仅支持整数类型
- **6 类** 比较运算指令：`eq`、`ne`、`lt`、`le`、`gt`、`ge`
  - 统一支持整数和浮点类型
- **3 类** 逻辑运算指令：`and`、`or`、`not`（布尔逻辑，与整数位运算共享关键字，由类型参数区分）
- **2 类** 转换指令：`cast`、`bitcast`
  - `cast` 支持整数 ↔ 整数、整数 ↔ 浮点、浮点 ↔ 浮点、整数/浮点 ↔ `bool`；`truncate` 是其整数窄化形式
  - `bitcast` 在等位宽整数/浮点之间保持位模式重解释
- **3 条** 标准 I/O 语句：`write`、`writeln`、`read`
  - `write`/`writeln` 支持格式化输出（13 种格式化符号，含 `%t`、`%f`、`%e`、`%E`、`%g`、`%G`）
- **控制流语句**：
  - **结构化控制**：`if-then-else`（Pascal 风格，缩进敏感，以 `end` 结束）、`while-then-end`（结构化循环，含 `break`/`continue`）
  - **非结构化控制**：`goto` 与 `label`（受限跳转），用于 `if` + `goto` 构建底层汇编风格循环；所有可达路径统一纳入完整 CFG 确定初始化分析（§3.9、§4.9.7）
  - **范式隔离原则**：`while` 循环体内**严格禁止**使用 `goto` 和 `label`；若需灵活的循环控制，应使用 `if` + `goto` 组合（见 §4.8.4、§4.9）
- 算术模式关键字：
  - **整数**：`wrap`（回绕模式，用于 `add`/`sub`/`mul`/`neg`/`shl`）
  - **浮点**：`ieee`（IEEE 754 标准模式）；0.0.31 不再支持浮点 `wrap`
  - 默认严格模式（整数检测溢出，浮点检测异常）
- 整数截断模式关键字 **`truncate`**（仅用于整数缩窄）

### 1.2 设计原则

| 原则           | 说明                                                         |
| -------------- | ------------------------------------------------------------ |
| **声明时必须初始化** | **`var` 唯一定义形态为 `var <名>: <类型> = <rhs>`**；禁止无 `=` 的声明。缺省 → `TC_ERR_VAR_MISSING_INIT`（§4.2） |
| 声明与再赋值分离 | 首次引入用 `var … = …`；其后修改省略 `var`，写作赋值语句。不存在「先声明、后首次赋值」的合法路径 |
| 常量与变量分离 | 不可变常量使用 `let` 定义，须在编译期初始化                  |
| 显式类型标注   | 变量/常量定义须声明具体类型                                  |
| 运算函数化     | 算术以 `op(<类型> [, 模式], 操作数...)` 形式调用；比较/逻辑以 `op(<类型> [, 模式], 操作数...)` 形式调用 |
| 类型参数首位   | 运算与转换的类型均作为 **第一个参数** 显式给出               |
| 默认严格模式   | 有符号整数运算默认检测溢出并报错；浮点运算默认检测异常并报错；允许的回绕或 IEEE 模式须显式传入对应关键字 |
| 同类型运算     | 同一运算的操作数须为相同类型                                 |
| 显式类型转换   | 跨类型须通过 `cast` 完成，禁止隐式转换                       |
| 逐条执行       | 每条语句独立执行，无隐式表达式嵌套求值                       |
| 字面量类型显式 | 无后缀整数字面量为有符号整数；`u`/`U` 后缀为无符号整数；无后缀浮点字面量为 `float64`；`f`/`F` 后缀为 `float32`；`true`/`false` 为布尔字面量 |
| I/O 格式化     | `write`/`writeln` 支持格式化符号，单符号单操作数             |
| 缩进强制       | 块结构由缩进界定，`if`/`else`/`end`/`while` 缩进必须一致，块内语句缩进必须多一级 |
| **内存安全**   | 语法层要求声明时初始化；控制流层对完整 CFG 做确定初始化分析，任何可达的未初始化读取均为静态错误（§3.9、§4.9.4） |
| **实现一致性** | VM、AOT 与常量求值必须遵循相同的位宽、舍入、错误和控制流规则；规范正文不提供可改变合法程序集合的可选加强项 |

### 1.3 设计哲学

TC 的设计遵循以下核心理念：

| 理念             | 说明                                                         |
| ---------------- | ------------------------------------------------------------ |
| **显式优于隐式** | 类型转换、异常处理、符号性、块结束（`end`）、结构化/非结构化范式选择均须显式标注，杜绝隐式歧义 |
| **安全优先**     | 默认检测溢出和转换错误；**声明时必须初始化**；完整确定初始化分析覆盖 `if`/`while`/`goto` 的所有可达路径 |
| **教学友好**     | 伪汇编风格让底层概念（位宽、符号、溢出、截断、浮点异常、内存分配、跳转底层实现）直观可见；缩进强制培养结构化编程习惯；范式隔离揭示高级循环与汇编跳转的本质差异；声明时初始化使「引入变量」与「给定初值」绑定为同一步 |
| **渐进扩展**     | 当前已覆盖整数、布尔与浮点；复合类型（数组/结构体/指针）、字符串与函数等为后续扩展预留清晰的设计空间 |

### 1.4 完整示例

以下是一个完整的 TC 程序，展示语言的核心能力（含整数、浮点、结构化循环与非结构化循环）：

```text
; ============================================================
; 全局变量与常量
; ============================================================
var a: int32 = 10
var b: int32 = 20
let SCALE: int32 = 1000
let DEBUG: bool = true

; ============================================================
; 浮点常量
; ============================================================
let PI: float64 = 3.141592653589793
let E: float32 = 2.71828f

; ============================================================
; 常量表达式
; ============================================================
let BASE: int32 = 1000
let MULTIPLIER: int32 = 3
let SCALED_BASE: int32 = mul(int32, BASE, MULTIPLIER)   ; 3000
let THRESHOLD: int32 = 100
let IS_VALID: bool = gt(int32, SCALED_BASE, THRESHOLD)   ; true
let FLAG: bool = true
let FLAG_AS_INT: int32 = cast(int32, FLAG)               ; 1
let CIRCUMFERENCE: float64 = mul(float64, PI, 2.0)       ; 6.283185307179586

; ============================================================
; 算术运算（整数）
; ============================================================
var sum: int32 = add(int32, a, b)           ; 30
var diff: int32 = sub(int32, a, b)          ; -10
var prod: int32 = mul(int32, a, b)          ; 200
var quot: int32 = div(int32, b, a)          ; 2
var rem: int32 = mod(int32, b, a)           ; 0

; ============================================================
; 算术运算（浮点）
; ============================================================
var x: float64 = 3.5
var y: float64 = 2.0
var fsum: float64 = add(float64, x, y)      ; 5.5
var fprod: float64 = mul(float64, x, y)     ; 7.0
var fdiv: float64 = div(float64, x, y)      ; 1.75
var fabs: float64 = abs(float64, -3.14)     ; 3.14

; ============================================================
; 浮点模式：IEEE 754 宽松模式
; ============================================================
var big: float32 = 1.0e30f
var huge: float32 = mul(float32, ieee, big, big)  ; inf

; ============================================================
; 比较与逻辑
; ============================================================
var is_equal: bool = eq(int32, a, b)        ; false
var is_less: bool = lt(int32, a, b)         ; true
var is_less_or_equal: bool = or(bool, is_equal, is_less)  ; true

; 浮点比较
var f_gt: bool = gt(float64, x, y)          ; true

; ============================================================
; if-else 控制流（缩进敏感，以 end 结束）
; ============================================================
var max: int32 = 0
if gt(int32, a, b) then
    max = a
else
    max = b
end
writeln(int32, %d, max)                     ; 20

; ============================================================
; 结构化循环：while（求和 1..10）
; ============================================================
var sum_loop: int32 = 0
var i: int32 = 1
while le(int32, i, 10) then
    sum_loop = add(int32, sum_loop, i)
    i = add(int32, i, 1)
end
writeln(int32, %d, sum_loop)                ; 55

; ============================================================
; 非结构化循环：if + goto（同样求和 1..10）
; 注意：此范式独立于 while，不可混用
; label 不产生新缩进；同级后续语句与 label 缩进一致（参见 §4.9.2）
; ============================================================
var sum_goto: int32 = 0
var j: int32 = 1

label loop_start:
sum_goto = add(int32, sum_goto, j)
j = add(int32, j, 1)
if le(int32, j, 10) then
    goto loop_start
end
writeln(int32, %d, sum_goto)                ; 55

; ============================================================
; 回绕与截断（整数）
; ============================================================
var max_i8: int8 = 127
var one: int8 = 1
var wrapped: int8 = add(int8, wrap, max_i8, one)  ; -128
var big_int: int32 = 1000
var result_i8: int8 = cast(int8, truncate, big_int)  ; -24

; ============================================================
; 浮点按位重解释
; ============================================================
var f_repr: float32 = -1.0f
var bits: uint32 = bitcast(uint32, f_repr)  ; 0xBF800000
var pi_from_bits: float32 = bitcast(float32, 0x40490FDBu)  ; 3.1415927

; ============================================================
; 布尔与整数/浮点互转
; ============================================================
var flag_bool: bool = cast(bool, a)         ; true
var as_int: int32 = cast(int32, is_equal)   ; 0
var as_float: float64 = cast(float64, flag_bool)  ; 1.0

; ============================================================
; I/O
; ============================================================
write(int32, %d, sum)                      ; "30"
writeln(int32, %d, SCALED_BASE)            ; "3000\n"
writeln(bool, %t, is_less)                 ; "true\n"
writeln(float64, %f, PI)                   ; "3.141593\n"
writeln(float64, %e, PI)                   ; "3.141593e+00\n"
```

> **范式说明**：上述两种循环方式在 TC 中属于**互斥范式**（文件内可并存，禁止在同一 `while` 体内混用）。`while` 块内严禁出现 `goto`/`label`；若需使用 `goto` 构建循环，请完全放弃该处的 `while`，直接使用 `if` + `goto`。两种范式均纳入 §3.9 的完整确定初始化分析。

---

## 2. 词法结构

本章定义 TC 语言的基本词法单元。完整的 EBNF 定义见附录 A。

### 2.1 字符集与 Token

TC 源文件由以下字符构成：

- **字母**：`A`–`Z`、`a`–`z`、`_`
- **数字**：`0`–`9`
- **符号**：`+`、`-`、`*`、`/`、`%`、`=`、`:`、`,`、`(`、`)`、`;`、`_`、`.`、`e`、`E`
- **空白**：空格、制表符（`\t`）、换行符（`\n`、`\r\n`、`\r`）

源文件编码为 **UTF-8**，但语言关键字和标识符仅限于 ASCII 字符集。

### 2.2 标识符

- 由字母、数字与 `_` 组成，**必须以字母或 `_` 开头**。
- **区分大小写**：`a` 与 `A` 为不同变量。
- 合法：`a`、`count`、`_tmp1`、`my_var`
- 非法：`1a`、`-x`（`-` 非标识符字符）

### 2.3 整数字面量

#### 2.3.1 后缀与符号性

TC 字面量通过后缀区分符号性：

| 后缀      | 符号性 | 示例                   |
| --------- | ------ | ---------------------- |
| 无后缀    | 有符号 | `42`、`-128`、`0xFF`   |
| `u` / `U` | 无符号 | `42u`、`255u`、`0xFFu` |

**关键规则**：
- 无后缀字面量视为有符号整数，可用于有符号类型上下文；若用于无符号上下文，须满足范围约束
- `u` 后缀字面量视为无符号整数，**仅可用于无符号类型上下文**；用于有符号上下文触发 **字面量类型错误**
- 负号 `-` **仅可与无后缀字面量**组合；`-42u` 为非法

#### 2.3.2 进制

TC 支持四种进制字面量：

| 进制           | 前缀        | 数字字符集                | 示例                      |
| -------------- | ----------- | ------------------------- | ------------------------- |
| 十进制（默认） | —           | `0`–`9`                   | `0`、`255`、`-128`、`42u` |
| 十六进制       | `0x` / `0X` | `0`–`9`、`a`–`f`、`A`–`F` | `0xFF`、`0x1Au`、`-0x80`  |
| 二进制         | `0b` / `0B` | `0`、`1`                  | `0b1010`、`0b11111111u`   |
| 八进制         | `0o` / `0O` | `0`–`7`                   | `0o377`、`-0o100`         |

- 前缀中的 `0` 是必需的前导，**不区分大小写**
- 前缀与数字之间 **不可有空白**
- 后缀紧跟数字部分，前缀与后缀之间 **不可有空白**

#### 2.3.3 数字分隔符

为增强长数字的可读性，允许在下述位置插入单个下划线 `_`：

- 十进制：数字之间（首数字之后，末数字之前）
- 十六进制：`0x`/`0X` 与末数字之间
- 二进制：`0b`/`0B` 与末数字之间
- 八进制：`0o`/`0O` 与末数字之间

示例：

```text
1_000_000           → 1000000
0xFFFF_0000u        → 4294901760
0b1010_0011u        → 163
-0x7F_FF            → -32767
```

**约束**：不可连续两个 `_`，不可在字面量首尾放置 `_`，不可在前缀内部插入 `_`（如 `0x_FF` 非法）。

#### 2.3.4 前导零

- 十进制字面量 **不允许前导零**（`007` **非法**，应写作 `7`）
- 单个 `0` 是合法的十进制零，不视为前导零特例
- 八进制须显式使用 `0o` 前缀

#### 2.3.5 字面量的词法上限

- 整数字面量的**数值**不得超过 `uint64` 范围：`0 ≤ N ≤ 18446744073709551615`
- 整数字面量超出此范围 → **字面量范围错误**

#### 2.3.6 负号与减法运算符的关系

`-` 前缀仅出现在无后缀整数字面量中，**不作为独立的一元运算符**。若需变量间的减法，使用 `sub` 指令。

#### 2.3.7 词法分析器匹配策略

词法分析器采用 **最长匹配** 策略：
1. 扫描 `0` 时，检查后续字符是否为 `x`/`X`/`b`/`B`/`o`/`O`，转入对应进制（十六进制、二进制或八进制）
2. 数字部分扫描完成后，检查后续字符是否为 `u`/`U`，归约为带后缀字面量
3. 否则归约为无后缀字面量

### 2.4 浮点字面量

#### 2.4.1 格式与后缀

TC 支持十进制浮点字面量和科学计数法：

| 形式                    | 示例                     | 类型                     |
| ----------------------- | ------------------------ | ------------------------ |
| 十进制浮点              | `3.14`、`-0.5`、`1.0`    | 无后缀 → `float64`       |
| 单精度后缀              | `3.14f`、`-0.5F`、`1.0f` | `f`/`F` 后缀 → `float32` |
| 科学计数法（小写 e）    | `1.2e5`、`-3.14e-2`      | 无后缀 → `float64`       |
| 科学计数法（大写 E）    | `1.2E5`、`-3.14E-2`      | 无后缀 → `float64`       |
| 科学计数法 + 单精度后缀 | `1.2e5f`、`-3.14E-2F`    | `f`/`F` 后缀 → `float32` |

**关键规则**：
- 浮点字面量必须包含小数点 `.` 或指数标记 `e`/`E`
- 无后缀浮点字面量为 `float64` 类型
- `f`/`F` 后缀浮点字面量为 `float32` 类型
- 浮点字面量 **不区分符号性**（浮点数无符号概念）
- 负号 `-` 可与浮点字面量组合
- 浮点字面量 **仅可用于能确定源浮点类型的上下文**（含 `float32`/`float64` 运算、`cast` 或 `bitcast`）
- 有限十进制字面量先解析为精确数学值，再直接按 roundTiesToEven 舍入到后缀决定的 binary32/binary64；不得通过另一浮点类型中转
- 非零有限值舍入为零，或有限值舍入为无穷 → **字面量范围错误**；可表示的非规格化数合法
- 数学值为零且字面量显式带 `-` 时生成负零；其余零字面量生成正零

**特殊浮点值**：

| 字面量 | 含义             | IEEE 754 表示                |
| ------ | ---------------- | ---------------------------- |
| `inf`  | 正无穷           | 指数全 1，尾数全 0，符号位 0 |
| `-inf` | 负无穷           | 指数全 1，尾数全 0，符号位 1 |
| `nan`  | 非数（静默 NaN） | 指数全 1，尾数非 0           |

**约束**：
- `inf`、`-inf`、`nan` 为保留关键字，不可用作标识符
- 区分大小写：均为小写。`INF`/`NaN` 等其他大小写形式按普通标识符词法化，若未声明则报 `TC_ERR_UNDEFINED_VARIABLE`

```text
var pi: float64 = 3.141592653589793
var e: float32 = 2.71828f
var speed: float64 = 3.0e8
var tiny: float32 = 1.0e-38f
var inf_pos: float64 = inf
var nan_val: float64 = nan
```

#### 2.4.2 词法错误

```text
3.14u     ; 非法：浮点不支持 u 后缀
.5        ; 非法：须 0.5
1e5       ; 合法：科学计数法（整数指数形式）
```

### 2.5 布尔字面量

| 字面量  | 值   | 类型   |
| ------- | ---- | ------ |
| `true`  | 真   | `bool` |
| `false` | 假   | `bool` |

**约束**：
- `true` 和 `false` 为保留关键字，不可用作标识符
- **区分大小写**，且仅小写形式是布尔字面量。`True` / `FALSE` 等按普通标识符词法化，若未声明则报 `TC_ERR_UNDEFINED_VARIABLE`
- 布尔字面量 **仅可用于** `bool` 类型上下文

```text
var flag: bool = true
var done: bool = false
var err: int32 = true      ; 字面量类型错误
```

### 2.6 字符与字符串字面量（预留）

> **扩展预留**：未来版本将支持：
> - 字符字面量：`'a'`、`'\n'`、`'\x41'`
> - 字符串字面量：`"hello"`、`"world\n"`
>
> 当前版本中，单引号或双引号包裹的内容为 **词法错误**。

### 2.7 注释

- 行注释以 `;` 开始，延续至行尾
- 示例：`var a: int32 = 10    ; 定义 32 位整数`

### 2.8 关键字

以下 token 为保留关键字，不可用作标识符：

```text
var  let  bool  true  false
int8  uint8  int16  uint16  int32  uint32  int64  uint64
float32  float64
add  sub  mul  div  mod  abs  neg
eq  ne  lt  le  gt  ge
and  or  not           ; and/or/not 支持 bool 逻辑与整数按位重载
xor                    ; 仅整数位运算，不支持 bool
shl  shr
cast  bitcast  wrap  truncate  ieee
write  writeln  read
if  then  else  end
while  break  continue   ; 结构化循环控制
goto  label            ; 非结构化跳转
inf  nan
```

> **扩展预留**：以下关键字为未来功能保留：`func`、`return`、`string`、`array`、`struct`、`ptr`、`fmod`、`remainder`、`sqrt`、`sin`、`cos`。

### 2.9 语句与行结构

- 每条语句独占一行
- **空行**、**仅含空白符的行**、**仅含行注释**的行均合法，不产生语句
- 语句由 `NEWLINE` 结束。`;` 始终是行注释起始符；因注释可紧跟语句，外观上可写为“语句末尾的可选 `;`”，但语法 Token 序列中不存在分号终结符

---

## 3. 类型系统

### 3.1 类型分类

TC 的类型系统设计为可扩展的分类体系。当前支持 **整数类型**、**浮点类型** 和 **布尔类型**，未来将逐步引入其他类别：

| 类别            | 当前支持               | 未来预留                 | 说明                   |
| --------------- | ---------------------- | ------------------------ | ---------------------- |
| **整数类型**    | ✅ `int8`～`uint64`     | `int128`、`uint128`      | 固定宽度二进制补码整数 |
| **浮点类型**    | ✅ `float32`、`float64` | `float128`               | IEEE 754 浮点数        |
| **布尔类型**    | ✅ `bool`               | —                        | 逻辑真/假              |
| **字符/字符串** | —                      | `char`、`string`         | Unicode 字符与字符串   |
| **复合类型**    | —                      | `array`、`struct`、`ptr` | 聚合与间接类型         |
| **单位类型**    | —                      | `void`                   | 无值（用于函数返回）   |

> **实现注记**：编译器内部 `TcType` 枚举将 `TC_BOOL`、整数类型及浮点类型统一管理，但类型分类函数（如 `tc_type_is_integer()`、`tc_type_is_float()`）分别返回对应真值。

### 3.2 整数类型

| 类型     | 位宽 | 符号   | 最小值                     | 最大值                     |
| -------- | ---- | ------ | -------------------------- | -------------------------- |
| `int8`   | 8    | 有符号 | −128                       | 127                        |
| `uint8`  | 8    | 无符号 | 0                          | 255                        |
| `int16`  | 16   | 有符号 | −32,768                    | 32,767                     |
| `uint16` | 16   | 无符号 | 0                          | 65,535                     |
| `int32`  | 32   | 有符号 | −2,147,483,648             | 2,147,483,647              |
| `uint32` | 32   | 无符号 | 0                          | 4,294,967,295              |
| `int64`  | 64   | 有符号 | −9,223,372,036,854,775,808 | 9,223,372,036,854,775,807  |
| `uint64` | 64   | 无符号 | 0                          | 18,446,744,073,709,551,615 |

类型命名规则：`<符号前缀><位宽>`，其中符号前缀为 `int`（有符号）或 `uint`（无符号），位宽为 `8`/`16`/`32`/`64`。

### 3.3 浮点类型

| 类型      | 位宽 | IEEE 754 对应 | 最小正非零值（约） | 最大有限绝对值（约） | 精度（十进制） |
| --------- | ---- | ------------- | ------------ | ------------ | -------------- |
| `float32` | 32   | binary32      | 1.4e-45      | 3.4e38       | ~7 位          |
| `float64` | 64   | binary64      | 5e-324       | 1.8e308      | ~16 位         |

**内部表示**：
- 浮点数以 IEEE 754 二进制格式存储
- `float32`：符号 1 位 + 指数 8 位 + 尾数 23 位
- `float64`：符号 1 位 + 指数 11 位 + 尾数 52 位
- 浮点数的“位模式”只能通过等位宽 `bitcast` 与整数互转

### 3.4 布尔类型

`bool` 类型表示逻辑真值，取值为 `true` 或 `false`。

**内部表示**：
- 在运行时，`bool` 值以字节存储。
- `false` 的表示为其存储单元的所有位均为 `0`。
- `true` 的表示为任何非零的位模式（建议实现统一为 `1`）。
- 但 **不可** 将 `bool` 直接解释为整数（须通过 `cast` 显式转换）。

**类型性质**：
- 比较运算和逻辑运算的结果类型为 `bool`。
- `bool` 变量可被赋值、读取、输出。
- 与所有类型相同：`bool` 的 `var` 定义**声明时必须初始化**（§4.2）。

```text
var flag: bool = true      ; 声明并初始化
var done: bool = false     ; 声明并初始化
; var uninit: bool         ; ❌ 静态错误：变量定义必须初始化
```

### 3.5 类型性质

**有符号类型（`int8`～`int64`）**

- 采用 **二进制补码** 表示
- `add`/`sub`/`mul`/`neg` 默认 **严格模式**：结果超出范围 → **整数溢出错误**
- 显式 `wrap` 模式：按模 `2^n` 回绕，不报错
- `shl` 默认严格模式：左移结果超出范围 → **整数溢出错误**；`wrap` 模式截断高位

**无符号类型（`uint8`～`uint64`）**

- 仅表示非负整数
- 算术结果超出最大值时 **模 `2^n` 回绕**，不报错
- `div`/`mod` 结果恒为非负
- `shl` 默认严格模式（仍可检测值溢出，例如 `shl(uint8, 255u, 1)` 的数学结果超过 255 时报错），`wrap` 模式截断高位

**浮点类型（`float32`、`float64`）**

- 符合 IEEE 754 标准
- 支持两种算术模式（详见 §5）：
  - **严格模式**（默认）：上溢/下溢/除零/无效操作均报错
  - **IEEE 754 模式**（`ieee`）：遵循 IEEE 754，返回 ±inf/nan，不报错
- 0.0.31 **不支持浮点 `wrap`**；位模式操作须通过等宽 `bitcast` 转为整数后完成
- 浮点数不支持位运算（`and`/`or`/`xor`/`not`/`shl`/`shr`）

**布尔类型**

- 仅表示逻辑真值
- 比较运算和逻辑运算返回 `bool`
- 不参与算术运算（须显式 `cast`）

**运行时值模型**

TC 中每个变量的值由 **n 位位模式**（`n` 为类型位宽）与 **类型解释** 共同构成：

- 整数内部以 n 位无符号位模式存储；有符号类型按 **二进制补码** 解释，无符号类型按非负整数解释
- 浮点数内部以 IEEE 754 位模式存储
- `bool` 内部以字节存储，语义上仅区分零和非零
- `wrap` 算术回绕、整数 `cast(..., truncate, ...)` 与等宽 `bitcast` 均在此位模式上操作
- 有符号 `wrap` 下的 `add`/`sub`/`mul`/`neg` 与同位宽无符号默认回绕 **位模式一致**

### 3.6 字面量与类型的关系

整数字面量的符号性由后缀决定，数值须满足上下文类型的约束：

```text
; 有符号字面量（无后缀）——可用于有符号类型
var a: int8 = 127           ; 合法
var b: int8 = 128           ; 字面量范围错误
var c: int32 = -100         ; 合法

; 有符号字面量用于无符号类型——需满足范围
var e: uint8 = 255          ; 合法
var f: uint8 = 256          ; 字面量范围错误
var g: uint8 = -1           ; 字面量范围错误

; 无符号字面量（u 后缀）——仅可用于无符号类型
var h: uint8 = 255u         ; 合法
var j: int32 = 42u          ; 字面量类型错误

; 浮点字面量——仅可用于浮点类型上下文
var pi: float64 = 3.14      ; 合法
var e: float32 = 2.71828f   ; 合法
var err: int32 = 3.14       ; 字面量类型错误

; 布尔字面量——仅可用于 bool 类型
var flag: bool = true       ; 合法
var err: int32 = true       ; 字面量类型错误
```

#### 字面量检查规则

**整数字面量**：
1. 解析字面量数值 `N`（`0 ≤ N ≤ 18446744073709551615`）
2. 若带负号（仅无后缀字面量允许），取 `−N`（`N=0` 时 `−0 = 0`）
3. 根据后缀确定符号性：
   - 无后缀 → 有符号
   - `u`/`U` 后缀 → 无符号
4. 检查上下文类型：
   - 有符号类型：仅允许无后缀字面量，值在范围内
   - 无符号类型：允许无后缀（≥0）或 `u` 后缀字面量，值在范围内
5. 不满足则报错

| 上下文类型 | 允许的字面量形式        | 可接受数值范围                                          |
| ---------- | ----------------------- | ------------------------------------------------------- |
| `int8`     | 无后缀                  | −128 ～ 127                                             |
| `uint8`    | 无后缀（≥0）或 `u` 后缀 | 0 ～ 255                                                |
| `int16`    | 无后缀                  | −32,768 ～ 32,767                                       |
| `uint16`   | 无后缀（≥0）或 `u` 后缀 | 0 ～ 65,535                                             |
| `int32`    | 无后缀                  | −2,147,483,648 ～ 2,147,483,647                         |
| `uint32`   | 无后缀（≥0）或 `u` 后缀 | 0 ～ 4,294,967,295                                      |
| `int64`    | 无后缀                  | −9,223,372,036,854,775,808 ～ 9,223,372,036,854,775,807 |
| `uint64`   | 无后缀（≥0）或 `u` 后缀 | 0 ～ 18,446,744,073,709,551,615                         |

**浮点字面量**：
- 无后缀 → `float64`，精确十进制值按 roundTiesToEven 直接舍入为 binary64
- `f`/`F` 后缀 → `float32`，精确十进制值按 roundTiesToEven 直接舍入为 binary32
- 非零有限值舍入为零或有限值舍入为无穷 → **字面量范围错误**；可表示非规格化数合法
- 特殊值 `inf`/`-inf`/`nan` 可用于浮点类型上下文；`nan` 使用 §5.3 的 canonical quiet NaN
- 用于非浮点类型 → **字面量类型错误**

**布尔字面量**：
- `true` 仅可用于 `bool` 类型上下文
- `false` 仅可用于 `bool` 类型上下文
- 用于非 `bool` 类型 → **字面量类型错误**

**`wrap`/`ieee`/`truncate`/`bitcast` 不绕过字面量检查**：即使运算或转换使用特殊形式，字面量仍须先在其自身类型上下文中通过静态检查。

### 3.7 类型转换的通用框架

TC 0.0.31 将数值转换、整数截断与位重解释分开：

| 形式 | 用途 | 核心约束 |
| ---- | ---- | -------- |
| `cast(T, operand)` | 数值转换 | 按目标类型做范围检查与规定舍入 |
| `cast(T, truncate, operand)` | 整数缩窄 | 仅整数→更窄整数，保留低位 |
| `bitcast(T, operand)` | 位重解释 | 源、目标等宽；不改变任何位；`bool` 不参与 |

严格 `cast` 覆盖整数 ↔ 整数、整数 ↔ 浮点、浮点 ↔ 浮点，以及整数/浮点 ↔ `bool`。`truncate` 不再承担浮点位重解释；所有整数/浮点位模式互转统一使用 `bitcast`。未来扩展复合类型时，不得复用这三种形式表达地址、所有权等不同性质的转换。

### 3.8 作用域

TC 采用 **全局作用域** 与 **块级作用域** 相结合的设计：

- **全局作用域**：文件顶层（即不在任何 `if`/`while` 块内部）定义的 `var` 变量和 `let` 常量属于全局作用域。全局标识符在整个文件中可见（需遵循源序可见性），生命周期贯穿程序运行始终。
- **块级作用域**：每个 `if` 语句（从 `if` 行到对应的 `end` 行）和每个 `while` 语句（从 `while` 行到对应的 `end` 行）各自构成一个独立的块级作用域。`then` 与 `else` 各自为互斥子作用域：两侧局部标识符互不可见，但允许分别定义同名局部变量。块内定义的 `var` / `let` 在**离开该块之后**不可访问（见下「变量生命周期」）。嵌套块支持内层屏蔽外层同名标识符（建议避免）。
- **标签作用域**：标签遵循与变量（`var`）和常量（`let`）**完全一致的块级作用域规则**，并受范式隔离约束：
  - `while` 块内**禁止**定义 `label`（§4.8.4）。合法的标签仅出现在全局作用域或 `if` 块（含其 `then`/`else` 子块）内。
  - 标签在定义所在的**最内层代码块**内可见。
  - 内部代码块可以访问（引用）外部代码块中定义的标签（对应"向外跳转"），但同一代码块内**禁止**定义同名标签。
  - 不同代码块（如 `then` 分支与 `else` 分支，或不同嵌套层级）允许定义**同名**标签，互不冲突（与 `var` 允许在互斥分支定义同名变量的规则一致）。
  - 标签的可见性基于**词法作用域**静态确定，编译器向上遍历父级作用域链进行标签引用解析。
- **源序可见性**：变量/常量在**定义语句执行之后**才可供后续语句引用。定义自身的 RHS **不可**引用自身，亦不可引用尚未定义的标识符。
- **变量生命周期**：
  - 全局变量：程序启动时建立固定槽位，但不自动写入初值；执行到对应 `var` 后才成为已初始化。程序终止时销毁。
  - 局部变量（块内）：进入作用域时为该作用域的全部 `var` 绑定建立固定槽位；执行到对应 `var` 时计算 RHS、写入槽位并成为已初始化。反向跳转或下一次循环迭代重新执行同一 `var` 时，重新初始化同一词法槽，不重复分配。离开该块时统一销毁一次——离开方式包括对应的 `end`、`break` 或向外 `goto`。块内 `let` 随词法作用域结束而不再可见（本身无运行时槽，见 §3.9）。

> **扩展预留**：未来引入函数时，将进一步增加函数级局部作用域。

### 3.9 变量生命周期与确定初始化

| 阶段 | 行为 |
| ---- | ---- |
| 作用域进入 | 建立本作用域全部 `var` 的固定槽位，初始状态为“未初始化” |
| `var` 执行 | 先检查并计算 RHS，再写入对应槽位；成功后状态变为“已初始化” |
| 赋值 / `read` | 目标变量必须已确定初始化；成功后更新同一槽位，不改变其生命周期 |
| 变量读取 | 该绑定必须在当前 CFG 点上确定初始化，否则静态错误 `TC_ERR_UNINITIALIZED_VARIABLE` |
| 反向跳转 / 下一迭代 | 再次执行 `var` 时重新初始化同一槽位，不创建新绑定或新槽位 |
| 作用域离开 | 因 `end`、`break` 或向外 `goto` 离开时，槽位统一销毁一次 |
| `let` | 编译期求值并内联，不生成运行时槽 |

#### 确定初始化数据流

编译器必须为全部可达控制流建立 CFG；`if`、`while`、`break`、`continue` 与 `goto` 使用同一分析。设 `IN[n]` 为语句 `n` 执行前确定已初始化的绑定集合，`OUT[n]` 为执行后的集合：

```text
IN[entry] = 已由祖先作用域确定初始化的绑定集合
IN[n]     = ∩ OUT[p]，其中 p 为 n 的全部可达前驱
OUT[var x = rhs] = IN[n] ∪ {x}
OUT[其他语句]    = IN[n]
```

- `var x = rhs` 的 RHS 在加入 `x` 之前检查，因此初始化自引用仍非法。
- 任何可达的变量读取、赋值目标或 `read` 目标都要求对应绑定属于当前 `IN`。逻辑短路的右操作数是否可达按 §7.2.2 判定。
- 会合点取可达前驱集合的交集；只有所有可达路径均已初始化才成立。
- 循环按上述传递函数迭代到固定点。
- 可达性在 §4.3 常量求值与静态布尔三态判定完成后确定。若 `if`/`while` 条件按 §4.3 判定为静态 `true` 或静态 `false`，删除不可能边；判定为 `unknown` 时同时保留 `true` 与 `false` 边。CFG 不另行维护运算符白名单。
- 不可达语句不参与可达前驱交集，但仍须通过词法、语法、名称与类型检查。
- 分析结果是 0.0.31 的强制语义，不允许实现通过更弱或更强的默认规则改变合法程序集合。

声明时强制 RHS 与确定初始化分析是两个独立保证：前者约束源代码形态，后者证明每次可达使用之前相应初始化语句确已执行。

---

# 第二部分：语句与表达式

---

## 4. 程序结构与语句

### 4.1 程序构成

TC 源文件由若干 **行** 组成，每行至多一条语句。程序执行的入口为文件的第一条非空、非注释语句，按顺序执行至文件末尾；遇到 `if` 语句时按 §4.7 的条件分支规则选择执行路径，遇到 `while` 语句时按 §4.8 的循环规则执行，遇到 `goto` 时按 §4.9 的跳转规则转移控制流。

**语句种类**：
- 变量定义（`var`）、常量定义（`let`）、赋值
- I/O（`write`/`writeln`/`read`）
- 结构化控制：条件分支（`if`-`then`-`else`-`end`）、循环（`while`-`then`-`end`）、循环控制（`break`/`continue`）
- 非结构化控制：标签定义（`label`）、跳转（`goto`）

**程序结构约束**：
- 源文件由若干行组成；每行至多一条语句
- 空行、纯注释行均合法，不产生语句
- 语句由换行结束；`;` 仅引入行注释，词法层在产生 `NEWLINE` 前丢弃其后内容
- `if`/`while` 块内语句须满足 §4.7.2 的缩进规则
- **范式隔离**：`while` 块内**严格禁止** `goto` 与 `label`（见 §4.8.4）

### 4.2 变量定义（`var`）

#### 4.2.1 语法形态（已收敛）

`var` **声明时必须初始化**。合法且唯一的定义形态为：

```text
var <identifier>: <type> = <rhs>
```

| 项目 | 规则 |
| ---- | ---- |
| 初始化器 | **强制**。`=` 与 `<rhs>` 不可省略 |
| 非法形态 | `var <identifier>: <type>`（无 RHS）→ 静态错误 `TC_ERR_VAR_MISSING_INIT` |
| RHS | 字面量、已定义标识符、单个运算、`cast` 或 `bitcast`（见 §4.5）；须与声明类型一致 |
| 再赋值 | 首次引入之后，修改写作 `<identifier> = <rhs>`，**不再**写 `var`（§4.4） |

> **设计收敛说明**：可选初始化（「先 `var x: T`，后某处首次赋值」）**已废止**。`read` **不能**代替声明处的初始化器——目标变量必须事先以带 RHS 的 `var` 定义，`read` 仅覆盖其当前值（§9.3）。附录 A 的 `variable_def` 产生式与此一致，无可选分支。

#### 4.2.2 语义与检查

- 当且仅当该条 `var` 语句**执行成功**后，变量持有 RHS 求得的有效值（全局与块内均在定义行写入预先建立的固定槽位）。
- 编译器对每条 `var` 做**语法/形态检查**：缺 `=` RHS → **`TC_ERR_VAR_MISSING_INIT`**（打印名 `VarMissingInitializer`，§11.0 / §11.4）。此检查与控制流无关，亦不依赖数据流分析；**不得**仅报笼统 `TC_ERR_SYNTAX`。
- 同一作用域内不可重复定义同名变量（作用域包括全局、`if`/`while` 块，以及 `then`/`else` 各自的互斥子块，见 §3.8、§4.7.5、§4.8.3）。跨类型赋值须先 `cast`。
- 声明带 RHS 不等于该语句在所有路径上均已执行；完整 CFG 的确定初始化规则见 §3.9 与 §4.9.4。

```text
var a: int32 = 10                 ; ✅ 声明并初始化
var b: int32 = add(int32, a, 20)  ; ✅
var c: int8 = cast(int8, truncate, a)
var flag: bool = true
var pi: float64 = 3.14
var e: float32 = 2.71828f

var d: int32                      ; ❌ TC_ERR_VAR_MISSING_INIT
; d = 1                           ; 即便其后赋值，仍不能补救无初始化器的声明
```

> **作用域补充**：若 `var` 出现在 `if` / `else` / `while` 缩进块内部，则该变量具有**块级局部作用域**，仅在本块（从定义处到对应的 `end`）内可见。块外任何对该变量的引用均为"未定义标识符错误"。

### 4.3 常量定义（`let`）

使用 `let` 声明 **不可变常量**：

- RHS **必须** 提供
- RHS 须为 **单层编译期常量表达式**（见下文定义）
- 常量在编译期求值；使用处**直接内联**编译期结果，**不生成运行时算术指令，亦不分配运行时变量槽**（与 §3.9、附录 B 一致）。源文件中 `let` 语句仍占一行，仅作为编译期声明
- 常量 **不可** 作为赋值语句的左值
- **在同一作用域内**，常量名不可与变量名或其他常量名冲突。

#### 编译期常量表达式

`let` 的 RHS 可包含以下任一形式：

**原子表达式**：
- 整数字面量（`42`、`-128`、`0xFFu`）
- 浮点字面量（`3.14`、`2.71828f`、`1.0e-5`）
- 特殊浮点值（`inf`、`-inf`、`nan`）
- 布尔字面量（`true`、`false`）
- 此前已定义的 `let` 常量引用（`PI`、`MASK`）

**单个调用表达式**（每个操作数只能是上述原子表达式）：
- 算术运算：`add`/`sub`/`mul`/`div`/`mod`/`abs`/`neg`，可使用 §5 允许的 `wrap` 或 `ieee` 模式
- 位运算：`and`/`or`/`xor`/`not`（按位）、`shl`/`shr`（移位）
- 比较运算：`eq`/`ne`/`lt`/`le`/`gt`/`ge`（浮点比较允许但不推荐，因浮点精度）
- 逻辑运算：`and`/`or`/`not`（布尔）
- 转换：严格 `cast`、整数 `cast(..., truncate, ...)` 与等宽 `bitcast`

**约束**：
- 常量表达式**不可** 引用任何 `var` 变量
- 常量表达式**不可** 引用自身或尚未定义的 `let`
- 操作调用不可嵌套；复合计算须拆分为多条按源序定义的 `let`
- 由于只允许引用此前的 `let`，多常量循环依赖在语法可见性上不可形成，0.0.31 不再定义常量循环依赖错误
- 有符号严格常量算术运算或 `shl` 的溢出，在编译期视为 **常量溢出错误** 并终止编译
- 严格浮点常量算术按 §5 的异常优先级求值：无效操作 → `TC_ERR_CONSTANT_EXPRESSION`，除零 → `TC_ERR_CONSTANT_DIV_ZERO`，上溢或下溢 → `TC_ERR_CONSTANT_OVERFLOW`
- 常量表达式中整数 `div`/`mod` 的除数为 0，或浮点严格 `div` 的最高优先级异常为除零，视为 **常量除零错误** 并终止编译
- 常量移位、转换、NaN 与模式合法性完全复用 §5～§8 的运行时规则

#### 静态布尔条件与三态判定

“静态布尔条件”是结果类型为 `bool`，且整体符合本节“编译期常量表达式”定义的单层 RHS。其原子操作数只能是字面量，或在当前词法作用域可见、定义语句位于使用点之前且已成功求值的 `let`；不得把 `var` 的当前值或跨语句运行时结果当作常量传播。

名称和类型检查完成后，编译器对 `if`/`while` 条件执行以下三态判定：

- **静态 `true`**：RHS 合法且按本节共享常量语义求值为 `true`；CFG 仅保留真边。
- **静态 `false`**：RHS 合法且求值为 `false`；CFG 仅保留假边。
- **`unknown`**：RHS 不是合法的单层常量表达式，或含 `var` 等不能静态解析的操作数；CFG 同时保留真、假边。

静态布尔判定只复用本节的表达式定义和 §5～§8 的共享语义，不建立第二套运算符白名单。布尔字面量、可见的更早 `let bool`、结果为 `bool` 的整数/浮点比较、布尔逻辑运算，以及目标为 `bool` 的合法严格 `cast` 均因此自然受支持；`bitcast` 不允许 `bool`，整数 `truncate` 也不能产生合法布尔条件。

前向 `let`、不可见 `let`、嵌套调用或其他非法形态在内部静态判定中不得被折叠；它们仍分别由更早的名称、语法或类型阶段报告错误，因此不会进入 CFG。若表达式形态合法但求值发生整数/浮点溢出、除零、无效浮点操作或严格转换失败，必须按本节既有映射报告 `TC_ERR_CONSTANT_OVERFLOW`、`TC_ERR_CONSTANT_DIV_ZERO`、`TC_ERR_CONSTANT_EXPRESSION` 或 `TC_ERR_CONSTANT_CAST_OVERFLOW` 并终止编译；不得降级为 `unknown`。

#### 浮点常量算术的精度规则

常量表达式中的浮点算术遵循以下规则：
1. 每条指令均按其显式类型参数执行：`float32` 使用 binary32，`float64` 使用 binary64。
2. 每条指令结束时立即按 roundTiesToEven 舍入；不得统一提升为 `float64` 中间精度。
3. strict / `ieee` 的结果与同一指令在运行时执行完全一致。
4. 常量求值器不得使用 FMA、扩展精度、FTZ 或 DAZ。

```text
let PI: float64 = 3.141592653589793
let PI_SQ: float64 = mul(float64, PI, PI)          ; binary64
let F32_A: float32 = add(float32, 16777216.0f, 1.0f) ; binary32，结果仍为 16777216
let INF_OK: float32 = div(float32, ieee, 1.0f, 0.0f)   ; inf
; let ERR: float64 = div(float64, 1.0, 0.0)            ; 常量除零
```

#### 示例

```text
; 基础常量
let PI: float64 = 3.141592653589793
let E: float32 = 2.71828f
let MASK: uint32 = 0xFFFF_0000u
let DEBUG: bool = true

; 编译期整数算术
let ZERO: int32 = sub(int32, 10, 10)          ; 0
let OFFSET: int32 = add(int32, 1000, 100)     ; 1100

; 编译期浮点算术
let PI_SQUARED: float64 = mul(float64, PI, PI)   ; 9.869604401089358
let CIRCUMFERENCE: float64 = mul(float64, PI, 2.0)  ; 6.283185307179586

; 编译期比较与逻辑
let IS_POSITIVE: bool = gt(float64, PI, 0.0)  ; true
let RESULT: bool = and(bool, IS_POSITIVE, true)  ; true

; 静态布尔条件：完整合法单层 RHS 均参与 CFG 剪枝
let TEN: int32 = 10
let FIVE: int32 = 5
let FLAG_A: bool = true
let FLAG_B: bool = false
if gt(int32, TEN, FIVE) then                 ; 静态 true
    writeln(bool, %t, true)
end
while and(bool, FLAG_A, FLAG_B) then         ; 静态 false，循环体不可达
    writeln(bool, %t, true)
end

; var 不做值推测：即使运行时初值为 true，静态判定仍为 unknown
var runtime_flag: bool = true
if runtime_flag then
    writeln(bool, %t, runtime_flag)
end

; 编译期转换
let FLAG_INT: int32 = cast(int32, DEBUG)      ; 1
let PI_INT: int32 = cast(int32, PI)           ; 3
let PI_U32: uint32 = cast(uint32, PI_INT)      ; 3（嵌套调用拆为两条 let）
let PI_BITS: uint64 = bitcast(uint64, PI)      ; 等宽位重解释

; ========== 错误示例 ==========
; let ERR1: int32 = add(int32, a, 10)        ; 错误：a 是 var 变量
; let ERR2: int32 = add(int32, mul(int32, 2, 3), 4) ; 错误：调用嵌套
; let ERR3: float64 = div(float64, 1.0, 0.0)  ; 错误：常量除零
; let ERR4: int32 = bitcast(int32, PI)        ; 错误：位宽不匹配
```

> **作用域补充**：同 `var`，`let` 若定义在块内，其作用域限定于当前 `if`/`while` 块（到 `end` 为止）。常量在编译期求值，但可见性仍受块级作用域约束。

### 4.4 赋值语句

对已定义变量重新赋值时 **省略** `var`。右侧结果类型须与变量已声明类型 **完全一致**。首次引入该名必须已通过 §4.2 的 `var … = …` 完成，不存在「无初始化器的声明 + 本赋值作为首次写入」写法。

```text
var c: int32 = add(int32, a, b)
c = mul(int32, a, b)        ; 合法
c = cast(int32, d)          ; 跨类型须先 cast

var f: float64 = 3.14
f = mul(float64, f, 2.0)    ; 合法
f = cast(float64, 42)       ; 合法（整数 → 浮点）

var flag: bool = false
flag = true                 ; 合法
flag = lt(float64, f, 10.0) ; 合法
flag = and(bool, flag, true) ; 合法
```

**约束**：
- 赋值左侧不可为 `let` 定义的常量。
- 赋值目标必须在当前 CFG 点确定初始化；赋值不能补救被控制流绕过的声明初始化（§3.9）。

### 4.5 右值表达式（RHS）

| 产生式               | 结果类型           | 说明                            |
| -------------------- | ------------------ | ------------------------------- |
| `identifier`         | 标识符已声明类型   | 须已定义；受源序可见性约束      |
| `integer_literal`    | 由左侧变量类型决定 | 须满足字面量符号性及范围约束    |
| `float_literal`      | 由 `f`/`F` 后缀决定 | 无后缀为 `float64`，有后缀为 `float32`；须与上下文一致 |
| `float_special`      | 由左侧变量类型决定 | `inf`/`-inf`/`nan`              |
| `bool_literal`       | `bool`             | `true` 或 `false`               |
| `binary_arith_expr`  | 第一参数类型       | 五种双目算术运算（含浮点）      |
| `unary_arith_expr`   | 第一参数类型       | 两种单目算术运算（含浮点）      |
| `bitwise_expr`       | 第一参数类型       | 按位 `and`/`or`/`xor`（仅整数） |
| `unary_bitwise_expr` | 第一参数类型       | 按位 `not`（仅整数）            |
| `shift_expr`         | 第一参数类型       | `shl`/`shr` 移位运算（仅整数）  |
| `compare_expr`       | `bool`             | 六种比较运算（整数+浮点）       |
| `binary_logic_expr`  | `bool`             | `and`/`or` 逻辑运算（布尔）     |
| `unary_logic_expr`   | `bool`             | `not` 逻辑运算（布尔）          |
| `cast_expr`          | 第一参数类型       | 类型转换                        |
| `bitcast_expr`       | 第一参数类型       | 等宽位重解释                    |

### 4.6 操作数（Operand）

| 形式              | 约束                                                         |
| ----------------- | ------------------------------------------------------------ |
| *identifier*      | 已定义，类型与运算类型一致，且在当前 CFG 点确定初始化（§3.9） |
| *integer_literal* | 须满足字面量约束                                             |
| *float_literal*   | 须满足字面量约束                                             |
| *float_special*   | `inf`/`-inf`/`nan`，仅用于浮点类型上下文                     |
| *bool_literal*    | 仅用于 `bool` 类型上下文                                     |

**不支持** 嵌套：操作数不可为算术表达式、`cast` 或 `bitcast` 调用。复合计算须拆分为多条语句。

### 4.7 控制流语句

TC 提供 **Pascal 风格** 的 `if-then-else` 条件分支语句，采用 **缩进敏感** 的块结构，并以 **`end`** 关键字作为块的强制结束标志。

#### 4.7.1 `if` 语句语法

**强制语法形式**（行结构敏感）：

```text
if <bool_rhs> then
    语句序列
[else
    语句序列]
end
```

- `if`、`then`、`else`、`end` 为保留关键字。
- `<bool_rhs>` 为任何可产生 `bool` 类型结果的右值表达式（参见 §4.5）。
- `then` 关键字必须位于 `if` 行的末尾（其后必须换行），不可在同一行写其他内容（行末注释 `;` 除外）。
- `else` 关键字**必须单独另起一行**，缩进级别必须与对应的 `if` 行**完全一致**。
- `end` 关键字**必须单独另起一行**，缩进级别必须与对应的 `if` 行**完全一致**。`end` 是 `if` 语句的强制结束标志，不可省略。
- **不支持 `else if` 组合关键字**。多分支通过在 `else` 块内嵌套 `if` 实现（见下文）。
- `语句序列` 由零条或多条语句组成，每条语句独占一行，且**必须比所在的 `if` / `else` 行缩进更多**（至少一个缩进级别）。块内允许包含 `var` / `let` 定义、赋值、运算调用、I/O 语句、`label` 定义、`goto` 语句以及嵌套的 `if` 或 `while` 语句。

**重要**：缩进是**强制性的语法规则**。块内所有语句的缩进必须严格大于当前块级缩进。`end` 行的缩进必须等于当前块级缩进。若缩进不符合规则，或缺少 `end`，均视为静态错误，程序不编译。

#### 4.7.2 强制性缩进规则

| 规则编号 | 规则说明                                                     |
| -------- | ------------------------------------------------------------ |
| R1       | 缩进使用**空格**或**制表符**，但同一文件中**必须保持一致**（不可混用）。 |
| R2       | 一个缩进级别必须恰好为 **4 个空格**或 **1 个制表符**。整个文件选定一种后必须统一，不得混用或改变每级宽度。 |
| R3       | `if`/`while`、`else` 和 `end` 行的缩进级别必须**完全一致**，称为**当前块级缩进**。 |
| R4       | `if` / `else` / `while` 的直接块内语句必须比当前块级缩进**恰好多一级**。过少、无对应嵌套语句却多于一级，或同一语句序列内宽度不一致，均为缩进错误。 |
| R5       | `end` 行缩进必须等于当前块级缩进；若缩进不足或过多，均为静态错误。 |
| R6       | 嵌套 `if`/`while` 时，内层块行缩进必须比外层块内语句的缩进再多一级（即外层缩进 + 2 个级别）。内层块的块内语句再比内层块多一级，其 `end` 与内层块对齐。 |
| R7       | `label` **不产生**新的缩进层级、不开启代码块。`label` 行缩进与同作用域其它普通语句一致；其后仍属同一作用域、且非新子块内的语句，缩进必须与该 `label` 行**完全一致**。仅当后续语句本身开启 `if`/`while` 子块时，子块内部才按 R3–R6 增加缩进（详见 §4.9.2）。 |

#### 4.7.3 多分支写法（链式 `if-else`）

由于 `else if` 不允许写在同一行，链式分支的标准写法为：

```text
if <cond1> then
    语句序列1
else
    if <cond2> then
        语句序列2
    else
        if <cond3> then
            语句序列3
        else
            语句序列4
        end
    end
end
```

每个 `if` 都必须有对应的 `end`，且缩进逐级递增。

#### 4.7.4 执行语义

1. 计算 `<bool_rhs>` 的值。
2. 若为 `true`，执行 `then` 块内的所有语句，执行完后跳过 `else` 部分，直接跳至对应的 `end` 之后继续执行外层后续语句。
3. 若为 `false`，若存在 `else` 分支，则进入 `else` 块执行其内部语句（若 `else` 块内包含 `if`，则按 `if` 规则递归执行）。执行完 `else` 块后，遇到 `end` 继续执行外层后续语句。
4. 若条件为 `false` 且无 `else`，则直接跳至 `end` 之后执行。

#### 4.7.5 作用域与变量定义

- 整个 `if` 语句（从 `if` 行到对应的 `end`）构成一个**块级作用域**。
- `then` 与 `else` 各自构成**互斥子作用域**：同一 `if` 内，`then` 侧定义的局部标识符在 `else` 侧不可见，反之亦然；因执行路径互斥，允许在 `then` 与 `else` 中分别定义**同名**局部变量（静态分析视为不同绑定）。
- 块内允许使用 `var` 和 `let` 定义新的变量/常量；`end` 之后无法访问该 `if` 块内定义的任何局部标识符。
- 嵌套 `if` 时，内层块可访问外层块已定义的变量，除非被内层同名局部变量屏蔽。

#### 4.7.6 与 `let` 常量的交互

`let` 常量可在 `if` 块内定义，作用域同样限于该块。若 `if` 条件中引用 `let` 常量，其值是编译期确定的。

#### 4.7.7 扩展预留

未来版本将增加函数定义（`func`/`return`），但当前版本仅提供 `if` 条件分支、`while` 循环和 `goto` 跳转。

### 4.8 循环语句（`while`）

TC 提供 **Pascal 风格** 的 `while` 结构化循环语句，采用 **缩进敏感** 的块结构，并以 **`end`** 关键字作为块的强制结束标志。

#### 4.8.1 语法定义

**强制语法形式**（行结构敏感）：

```text
while <bool_rhs> then
    语句序列
end
```

- `while`、`then`、`end` 为保留关键字。
- `<bool_rhs>` 为任何可产生 `bool` 类型结果的右值表达式（参见 §4.5）。
- `then` 关键字必须位于 `while` 行的末尾（其后必须换行），不可在同一行写其他内容（行末注释 `;` 除外）。
- `end` 关键字**必须单独另起一行**，缩进级别必须与对应的 `while` 行**完全一致**。`end` 是 `while` 语句的强制结束标志，不可省略。
- `语句序列` 由零条或多条语句组成，每条语句独占一行，且**必须比 `while` 行缩进更多**（至少一个缩进级别）。块内允许包含 `var` / `let` 定义、赋值、运算调用、I/O 语句、`break`、`continue` 以及嵌套的 `if` 或 `while` 语句。

**缩进规则** 与 `if` 语句完全一致（参见 §4.7.2），即 `while`、`then`（隐式）、`end` 行缩进必须相等，块内语句缩进更大。

#### 4.8.2 执行语义

1. 计算 `<bool_rhs>` 的值。
2. 若为 `true`，执行 `then` 块内的所有语句（顺序执行）。
   - 若执行过程中遇到 `break`，立即退出整个 `while` 循环，跳转到对应的 `end` 之后继续执行。
   - 若遇到 `continue`，立即跳过当前迭代的剩余语句，跳转到步骤 1（重新计算条件）。
   - 若正常执行完块内所有语句（未遇 `break`/`continue`），则返回步骤 1，开始下一次迭代。
3. 若为 `false`，直接跳转到对应的 `end` 之后，循环终止。

> **无限循环说明**：当 `<bool_rhs>` 恒为 `true`（如 `while true then`）时，循环仅能通过 `break` 终止（因 `while` 内禁止 `goto`），否则为无限循环。

#### 4.8.3 作用域与变量定义

- 整个 `while` 语句（从 `while` 行到对应的 `end`）构成一个**块级作用域**。
- 块内使用 `var` 和 `let` 定义的新变量/常量，在 `end` 之后不可访问。
- 嵌套循环时，内层循环可访问外层变量，除非被同名局部变量屏蔽。
- `break` 和 `continue` 作用于**最内层**包含它们的 `while` 循环。它们不能跨循环作用（即 `break` 不能跳出嵌套的外层循环，只能跳出直接包围它的循环）。

#### 4.8.4 范式隔离原则（与 `goto` 的交互）

TC 提供两种实现循环的手段，二者属于**互斥范式**，不得混合使用：

1. **结构化循环（`while`）**：适用于逻辑清晰、入口/出口明确的场景。`while` 循环体内部**严格禁止**出现 `goto` 和 `label` 定义（因其破坏结构化块的结构完整性）。循环控制仅通过 `break`（退出）和 `continue`（跳回条件）实现。
2. **非结构化循环（`if` + `goto`）**：适用于状态机、复杂退出条件或底层汇编模拟场景。程序员使用 `if` 条件判断结合 `goto` 向前/向后跳转，自行构建循环入口和出口（详见 §4.9）。此模式下不涉及 `while` 关键字。

**编译器约束**：
- 若 `goto` 的词法祖先中包含 `while` 块 → 静态错误 `TC_ERR_GOTO_INSIDE_LOOP`。
- 若 `label` 的词法祖先中包含 `while` 块 → 静态错误 `TC_ERR_LABEL_INSIDE_LOOP`。

> **设计原理**：`while` 是结构化编程的语法糖，强制程序员写出可预测、可验证的代码（受限的入口和出口）。`if` + `goto` 还原了汇编级的原始跳转灵活性。TC 不允许在同一段循环逻辑中混用两种范式，以保持代码意图清晰。

#### 4.8.5 与初始化相关的静态分析

循环相关的静态分析必须覆盖：

- `break` / `continue` 是否处于合法的 `while` 上下文（§4.10）；
- 范式隔离：`while` 体内无 `goto` / `label`（§4.8.4）；
- 以条件失败、正常回边、`continue` 与 `break` 构造完整 CFG；
- 按 §3.9 在全部可达路径上计算确定初始化集合。

除 §3.9 能在编译期证明条件恒为 `true` 外，循环体均可能零次执行，因此仅在循环体中执行的 `var` 不会自动进入循环后的确定初始化集合。恒真 `while` 不产生条件失败出口，仅可达 `break` 边参与循环后的会合；恒假 `while` 的循环体不可达。所有符合 0.0.31 的实现必须得到相同的分析结果。

### 4.9 受限 `goto` 语句

TC 提供受限的 `goto` 跳转语句，在保留汇编级灵活性的同时维护结构化块的安全边界。`goto` 是构建**非结构化循环**（`if` + `goto`）的基础工具。

#### 4.9.1 设计原则

1. **作用域约束（只出不进）**：仅允许跳转到当前作用域的**祖先作用域**（向外）或**自身作用域**（平级）。
2. **位置自由**：支持向前跳转（跳至后续代码）和向后跳转（跳至先前代码），无位置限制。
3. **禁止侵入**：严禁跳入任何子作用域（嵌套块）或兄弟作用域（如 `then` ↔ `else`）。
4. **范式隔离**：`goto` **不得**出现在 `while` 循环体内（见 §4.8.4）。若需在循环逻辑中使用跳转，应使用 `if` + `goto` 构建非结构化循环（`while` 不参与）。
5. **标签唯一性**：在同一块级作用域内禁止定义同名标签；不同块可定义同名标签（与 `var` 规则一致）。`label` **不得**定义在 `while` 循环体内。
6. **标签命名空间独立**：标签名与变量名、常量名属于不同命名空间，允许在同一作用域内同时存在同名标签和变量，互不干扰。
7. **初始化约束完备统一**：`goto` 边进入完整 CFG；变量是否可用只由 §3.9 的确定初始化分析判定，不再使用同层源序扫描近似。

#### 4.9.2 语法定义

**标签定义**：
- 关键字 `label` + 标识符 + `:`，**必须单独占一行**（行末可带注释）。
- **缩进规则**（与 §4.7.2 R7 一致）：
  1. `label` **不产生**新的缩进层级，也**不开启**代码块或作用域。
  2. `label` 行本身视为**普通语句**：在全局作用域时缩进与其它顶层语句一致（通常为 0）；在 `if`/`else` 块内时，缩进必须**严格大于**当前块级缩进（与同块内赋值、`goto` 等对齐）。
  3. 紧随 `label` 之后、仍属于同一作用域、且**不是**因 `if`/`while` 等开启的新子块内的语句，其缩进必须与该 `label` 行**完全一致**（不得因出现 `label` 而额外多缩进一级，也不得比 `label` 更少）。
  4. 仅当后续语句自身开启子块（如 `if` … `then` / `while` … `then`）时，子块内部语句才按 §4.7.2 R3–R6 增加缩进；该子块的 `if`/`while`/`else`/`end` 行缩进仍与其外围同级语句（含前述 `label`）对齐。
- **执行语义**：标签本身不产生任何执行动作（即零成本标记）。`goto` 跳转后，控制权转移到该标签后的**下一条语句**。

```text
if gt(int32, a, 0) then          ; 块级缩进为 0
    label skip:                  ; 与块内普通语句同级（缩进 1）
    var x: int32 = 10            ; 与 label 同缩进（非新子块）
    writeln(int32, %d, x)        ; 与 label 同缩进
end
```

**跳转语句**：
- 关键字 `goto` + 标识符（标签名），单独占一行。
- `goto` 属于普通语句，缩进规则与同作用域内其它语句一致：在 `if` 块内时，缩进级别必须**严格大于**当前块级缩进（与块内赋值、`break`/`continue`、`label` 等相同，**不得**与对应的 `if`/`end` 行对齐）；在全局作用域时，缩进须与其它顶层语句一致（通常为 0）。

```text
var i: int32 = 0
label loop_start:                ; 全局：缩进 0；不产生新缩进
i = add(int32, i, 1)             ; 与 label 同缩进
if lt(int32, i, 10) then         ; 与 label 同缩进；if 才开启子块
    goto loop_start              ; ✅ 子块内多一级；平级向后跳（非结构化循环）
end

if gt(int32, x, 0) then
    goto after_if                ; ✅ 向 if 块外跳转
end
label after_if:                  ; 全局：缩进 0
writeln(int32, %d, x)            ; 与 label 同缩进
```

#### 4.9.3 作用域跳转的形式化规则

设 `Src` 为 `goto` 语句所在的最内层代码块（`if` 块或全局作用域），`Tgt` 为目标标签定义所在的最内层代码块。

> **前提条件**：若 `Src` 的任何祖先中包含 `while` 块，则 `goto` 本身已被判定为非法（`TC_ERR_GOTO_INSIDE_LOOP`），以下规则不再适用。

| 判定条件                                                     | 结果               | 错误码                         |
| :----------------------------------------------------------- | :----------------- | :----------------------------- |
| `Tgt` 与 `Src` 相同                                          | ✅ 合法（平级跳）   | —                              |
| `Tgt` 是 `Src` 的严格祖先（外层）                            | ✅ 合法（向外跳）   | —                              |
| `Tgt` 是 `Src` 的严格后代（内层）                            | ❌ 非法（跳入子块） | `TC_ERR_JUMP_INTO_BLOCK`       |
| `Tgt` 与 `Src` 为兄弟（同父不同子）                          | ❌ 非法（跨分支）   | `TC_ERR_JUMP_TO_SIBLING_BLOCK` |
| 在当前作用域及所有祖先作用域中均**未找到**目标标签           | ❌ 非法             | `TC_ERR_LABEL_NOT_FOUND`       |
| 在当前作用域内**重复定义**同名标签（即同一块内出现两个相同 `label` 名） | ❌ 非法             | `TC_ERR_DUPLICATE_LABEL`       |

> **注**：由于不同块级作用域允许定义同名标签，`TC_ERR_DUPLICATE_LABEL` 仅在**同一最内层代码块**内触发，跨块重名不视为错误。

> **实现注记**：判定算法为自 `Src` 向上遍历父级作用域链，若 `Tgt` 出现在链中（含自身），则合法；否则非法。

#### 4.9.4 语义规则

1. **执行流转移**：`goto` 无条件将控制权转移至目标标签后的第一条语句。
2. **变量生命周期**：若跳转跳出某作用域（如 `then` 块），该块内的局部变量按 §3.8 / §3.9 **立即销毁**（与 `end` / `break` 离开块等价）；后续任何对该块内变量的引用，静态分析会报"未定义标识符"。
3. **CFG 边**：每条合法 `goto` 都必须形成从跳转语句到目标标签后第一条语句的显式 CFG 边；向前、向后、同层与向外跳转使用同一表示。
4. **确定初始化**：编译器按 §3.9 在包含该边的完整 CFG 上求固定点。任何变量读取、赋值目标或 `read` 目标若不在当前 `IN` 集合中，触发静态错误 `TC_ERR_UNINITIALIZED_VARIABLE`。
5. **精确拒绝**：仅仅在源序上越过 `var` 不自动构成错误；若被越过的绑定在离开其作用域前从未使用，程序仍可合法。反之，无论被越过的 `var` 位于同层还是祖先作用域，只要存在可达未初始化使用就必须拒绝。
6. **反向跳转**：再次执行同一 `var` 时重新初始化同一词法槽；不重复分配槽位。
7. **跳过 `end`**：允许 `goto` 跳出 `if` 块而无需经过 `end`，执行流正常继续。
8. **不修改值**：`goto` 本身不改变任何仍存活变量的值，仅转移控制流。

**示例**：
```text
; ❌ 非法：同层前跳后读取未确定初始化变量
goto label_skip
var x: int32 = 10
label label_skip:
writeln(int32, %d, x)   ; TC_ERR_UNINITIALIZED_VARIABLE

; ✅ 合法：跳过未使用的 var
goto after_unused
var unused: int32 = 10
label after_unused:
writeln(int32, %d, 0)

; ✅ 合法：向后跳转重新初始化同一槽位
label loop:
var y: int32 = 1
...
goto loop

; ✅ 合法：向前跳转越过 let 常量定义（let 不生成运行时存储）
goto label_const
let PI: float64 = 3.14
label label_const:
writeln(float64, %f, PI)  ; 合法，PI 编译期内联

; ❌ 外跳前跳越过祖先作用域 var，完整 CFG 同样拒绝
if true then
    goto after
end
var skipped: int32 = 42
label after:
writeln(int32, %d, skipped)  ; TC_ERR_UNINITIALIZED_VARIABLE
```

#### 4.9.5 合法示例

**示例 1：非结构化循环（`if` + `goto` 模拟 `while`）**
```text
var i: int32 = 0
label loop_start:
i = add(int32, i, 1)
if lt(int32, i, 10) then
    goto loop_start       ; ✅ 同一全局作用域平级向后跳（非结构化循环）
end
label loop_end:
writeln(int32, %d, i)      ; 10
```

**示例 2：向外跳转**
```text
var x: int32 = 0

if gt(int32, x, 0) then
    x = add(int32, x, 10)
    goto after_if          ; ✅ 跳出 if 块到全局作用域
end

label after_if:
writeln(int32, %d, x)
```

**示例 3：跨多层向外跳转**
```text
label global_target:       ; 全局标签

if eq(int32, a, 0) then
    if eq(int32, b, 0) then
        goto global_target ; ✅ 从内层 if 跳转到全局外层
    end
end
```

**示例 4：跨块同名标签（合法，类比同名局部变量）**
```text
if gt(int32, a, 0) then
    label done:                ; 分支 A 中的标签
    writeln(int32, %d, a)
else
    label done:                ; 分支 B 中的同名标签（合法，互斥作用域）
    writeln(int32, %d, b)
end
; 此处不可引用内部的 done 标签（超出作用域）
```

**示例 5：嵌套块引用外部标签（合法）**
```text
var i: int32 = 0
label outer:                   ; 全局作用域标签

if lt(int32, i, 10) then
    i = add(int32, i, 1)
    goto outer                 ; ✅ 内部块引用外部标签（向外跳转）
end
```

#### 4.9.6 非法示例

**示例 1：跳入子块**
```text
goto inner                 ; ❌ 错误：跳入 if 块内部

if gt(int32, a, 0) then
    label inner:           ; 标签在内层子块
    writeln(int32, %d, a)
end
```

**示例 2：跳入兄弟分支**
```text
if gt(int32, a, 0) then
    goto else_branch       ; ❌ 错误：试图跳入 else 兄弟块
else
    label else_branch:
    writeln(int32, %d, a)
end
```

**示例 3：跨多层嵌套向里跳**
```text
if gt(int32, a, 0) then
    goto deep              ; ❌ 错误：标签在更深层的嵌套 if 内部
    if gt(int32, b, 0) then
        label deep:
        writeln(int32, %d, b)
    end
end
```

**示例 4：`goto` 在 `while` 循环体内（范式隔离违规）**
```text
while lt(int32, i, 10) then
    i = add(int32, i, 1)
    goto after_loop        ; ❌ 错误：TC_ERR_GOTO_INSIDE_LOOP
end
label after_loop:
```

**示例 5：向前跳转导致未初始化读取（非法）**
```text
goto skip_init
var init: int32 = 42
label skip_init:
writeln(int32, %d, init) ; ❌ TC_ERR_UNINITIALIZED_VARIABLE
```

#### 4.9.7 受限 `goto`：统一保证

受限 `goto` 同时受到块作用域、标签解析、范式隔离和确定初始化四类强制规则：

| 类别 | 规则摘要 | 错误码 / 依据 |
| ---- | -------- | ------------- |
| 作用域 | 禁止跳入子块、禁止跳入兄弟块；仅平级或向外 | `TC_ERR_JUMP_INTO_BLOCK` / `TC_ERR_JUMP_TO_SIBLING_BLOCK` |
| 标签 | 同块内标签唯一；祖先链上可解析；`while` 内禁止 `goto`/`label` | `TC_ERR_DUPLICATE_LABEL` / `TC_ERR_LABEL_NOT_FOUND` / `TC_ERR_GOTO_INSIDE_LOOP` / `TC_ERR_LABEL_INSIDE_LOOP` |
| 声明形态 | `var` 声明必须带 RHS | `TC_ERR_VAR_MISSING_INIT` |
| 路径安全 | 每次可达读取、赋值或 `read` 前，绑定均确定初始化 | `TC_ERR_UNINITIALIZED_VARIABLE` / §3.9 |

以上规则共同构成 0.0.31 的完整要求。规范不再保留“最低保证”“已知漏检”或“实现可选加强”层级。实现可以采用不同的内部算法，但不得接受存在可达未初始化使用的程序，也不得仅因源序上越过一个从未使用的 `var` 而拒绝程序。

---

### 4.10 `break` 与 `continue` 语句

#### 4.10.1 语法定义

`break` 和 `continue` 各为一条独立语句，独占一行，无额外参数。

```text
break
continue
```

**缩进规则**：它们属于普通语句，必须遵循所在块（`while`、`if` 或嵌套块）的缩进规则，即缩进级别必须严格大于当前块级缩进。

#### 4.10.2 语义规则

| 语句       | 行为                                                         |
| ---------- | ------------------------------------------------------------ |
| `break`    | 立即终止**最内层**包含它的 `while` 循环，控制流跳转到该循环的 `end` 之后；离开该 `while` 块时，块内局部变量按 §3.8 / §3.9 销毁。 |
| `continue` | 立即结束当前迭代，控制流跳转到**最内层**包含它的 `while` 的条件求值处：重新计算 `<bool_rhs>`；若为 `true`，则从循环体**首条语句**起按源序重新执行（再次执行到的块内 `var` 会重新初始化同一局部槽）；若为 `false`，则退出循环（效果同条件失败，局部按离开块规则销毁）。`continue` **不会**跳过条件求值而直接落到循环体中部。 |

**约束**：
- `break` 和 `continue` **必须**直接或间接位于某个 `while` 循环体内（即其所在的词法作用域链中至少有一个 `while` 块）。
- 若编译器静态分析发现 `break` 或 `continue` 不在任何 `while` 循环内，触发静态错误：
  - `TC_ERR_BREAK_OUTSIDE_LOOP`（针对 `break`）
  - `TC_ERR_CONTINUE_OUTSIDE_LOOP`（针对 `continue`）
- `break`/`continue` 可以出现在 `if` 块或其他嵌套块内，只要这些块本身位于 `while` 循环体内即可。
- **禁止**使用 `break`/`continue` 跳出 `while` 直接到外层（例如想跳出两层循环，需使用 `if` + `goto` 非结构化循环重构）。`break`/`continue` 的作用域目标是最内层 `while`，**而非**最内层 `if`。
- `break` 和 `continue` 的作用域目标仅为最内层包含它们的 `while` 循环，外围的 `if` 块不影响其合法性判定。

#### 4.10.3 与 `if` 的典型组合

实际编程中，`break`/`continue` 常配合 `if` 条件使用：

```text
while lt(int32, i, 10) then
    if eq(int32, i, 5) then
        break
    end
    i = add(int32, i, 1)
end
```

#### 4.10.4 与 `let` 常量的交互

`while` 条件中可以引用 `let` 常量。强制 RHS 不因恒真/恒假循环而改变；涉及 `goto` 时见 §4.9.7。

---

## 5. 算术运算

### 5.1 运算模式总览

0.0.31 以“操作 × 类型 × 模式”矩阵作为唯一权威来源：

| 运算 | 类型 | 合法模式 |
| ---- | ---- | -------- |
| `add`/`sub`/`mul`/`neg` | 有符号整数 | strict（无关键字）、`wrap` |
| `add`/`sub`/`mul`/`neg` | 无符号整数 | 固定模 `2^n`；不接受模式关键字 |
| `shl` | 全部整数 | strict（无关键字）、`wrap` |
| `div`/`mod`/`abs`/`shr` | 全部整数 | 无模式关键字 |
| `add`/`sub`/`mul`/`div` | 浮点 | strict（无关键字）、`ieee` |
| `abs`/`neg` | 浮点 | 无模式关键字；纯符号位操作 |
| 比较、逻辑、位运算 | 对应合法类型 | 无模式关键字 |

先在无模式的前提下检查运算是否支持所给类型；不支持时报 `TC_ERR_TYPE_MISMATCH`。类型本身受支持但模式不在上表时，报 `TC_ERR_MODE_MISMATCH`。0.0.31 不支持浮点 `wrap`，也不接受冗余的无符号 `wrap`。

### 5.2 严格模式（默认）

**整数严格模式**（参见 §5.3～§5.4）：
- 有符号 `add`/`sub`/`mul`/`neg` 结果超范围 → **整数溢出错误**
- `shl` 结果超范围 → **整数溢出错误**
- `div` 除数为 0 → **除零错误**；`div(INT_MIN, -1)` → **整数溢出错误**

**浮点严格模式**（IEEE 754 异常检测）：
- 上溢 → **浮点溢出错误**
- 下溢 → **浮点下溢错误**
- 除零 → **除零错误**
- 无效操作（0/0、∞−∞、0×∞） → **无效操作错误**
- `nan` 参与 `add`/`sub`/`mul`/`div` → **无效操作错误**

浮点执行环境固定如下：

- `float32` 每条指令按 IEEE 754 binary32 执行并立即舍入；`float64` 按 binary64 执行并立即舍入。
- 固定舍入模式为 roundTiesToEven；不提供运行时舍入模式切换。
- 禁止保留超出目标类型的中间精度，禁止将独立 `mul`/`add` 融合为 FMA。
- 必须保留非规格化数；禁止 FTZ（flush-to-zero）与 DAZ（denormals-are-zero）。
- 下溢定义为结果在舍入后微小且不精确；有限结果舍入为无穷时定义为上溢。
- 同一指令同时满足多个异常条件时，只报告最高优先级：`FloatInvalidOperation` → `DivisionByZero` → `FloatOverflow` → `FloatUnderflow`。

### 5.3 IEEE 754 标准模式（`ieee`）

**仅用于浮点 `add`/`sub`/`mul`/`div`**。执行精度、舍入方式、非规格化数与禁止 FMA 的规则同 §5.2，但异常不终止程序。

| 场景       | IEEE 754 行为                   | 返回值           |
| ---------- | ------------------------------- | ---------------- |
| 上溢       | 舍入到 ±∞                       | ±`inf`           |
| 下溢       | 返回 0 或非规格化数             | `0` 或非规格化值 |
| 除零       | 返回 ±∞                         | ±`inf`           |
| 无效操作   | 返回 canonical quiet NaN         | `nan`            |
| `nan` 传播 | 算术输入含 NaN 时返回 canonical quiet NaN | `nan`      |

```text
var big: float32 = 1.0e30f
var result: float32 = mul(float32, ieee, big, big)  ; inf
var zero: float32 = 0.0f
var nan_result: float32 = div(float32, ieee, zero, zero)  ; nan
```

`nan` 字面量与 `ieee` 算术产生的 NaN 使用固定 canonical quiet NaN：binary32 为 `0x7FC00000`，binary64 为 `0x7FF8000000000000`。通过 `bitcast` 构造的其他 NaN 位模式可保留其 payload；其参与 `ieee` 算术后结果仍规范化为 canonical quiet NaN。

### 5.4 整数回绕模式（`wrap`）

**整数 `wrap`**：
- 按模 `2^n` 回绕，不报错
- 仅用于有符号 `add`/`sub`/`mul`/`neg`，以及有符号或无符号 `shl`
- 无符号 `add`/`sub`/`mul`/`neg` 已固定采用模 `2^n` 语义，不接受冗余 `wrap`
- `div`/`mod`/`abs`/`shr` 不接受 `wrap`
- 浮点类型不接受 `wrap`；位模式混合须先 `bitcast` 为等宽整数

### 5.5 整数有符号运算（`int8`～`int64`）

| 指令                  | 语义                                                         |
| --------------------- | ------------------------------------------------------------ |
| `add` / `sub` / `mul` | 和 / 差 / 积；严格模式超范围报错，wrap 回绕                  |
| `div`                 | 商，**向零截断**（同 C 有符号 `/`）；除数为 0 → **除零错误**；商不可表示 → **整数溢出错误** |
| `mod`                 | 余数符号与 **被除数** 相同；除数为 0 → **除零错误**；**注**：`mod` 本身不会因值不可表示而报错，因为余数绝对值小于除数，必在类型范围内（除数为 0 已单独处理）。 |
| `abs`                 | 返回绝对值；`INT_MIN` 时严格模式报错                         |
| `neg`                 | 返回相反数；`INT_MIN` 时严格模式报错，wrap 回绕              |

**严格模式溢出检测**（`div`，有符号类型）：

1. 将操作数按 `<T>` 解码为 int64 有符号值
2. 除数为 0 → **除零错误**
3. 计算向零截断商 \(q\)；若 \(q\) 不在 `<T>` 可表示范围内 → **整数溢出错误**

**特殊情形**（\(b \neq 0\)）：

| 表达式                   | 结果 | 说明                                                        |
| ------------------------ | ---- | ----------------------------------------------------------- |
| `div(T, INT_MIN(T), −1)` | —    | **整数溢出错误**（数学商 \(+2^{n-1}\) 不可在 `<T>` 中表示） |
| `mod(T, INT_MIN(T), −1)` | `0`  | 余数为 0，**不报错**                                        |
| `abs(T, INT_MIN(T))`     | —    | **整数溢出错误**                                            |
| `neg(T, INT_MIN(T))`     | —    | 严格模式时 **整数溢出错误**                                 |

```text
var g: int32 = 7
var h: int32 = -3
var i: int32 = div(int32, g, h)    ; i = -2

var j: int32 = -7
var k: int32 = 3
var m: int32 = mod(int32, j, k)    ; m = -1

var n: int32 = -42
var o: int32 = abs(int32, n)       ; o = 42
```

### 5.6 整数无符号运算（`uint8`～`uint64`）

| 指令                  | 语义                                                |
| --------------------- | --------------------------------------------------- |
| `add` / `sub` / `mul` | 和 / 差 / 积；超范围 → 按模 2^n 回绕                |
| `div`                 | 数学整数商；除数为 0 → **除零错误**                 |
| `mod`                 | 余数满足 `0 ≤ 余数 < 除数`；除数为 0 → **除零错误** |
| `abs`                 | 恒等返回                                            |
| `neg`                 | 返回 `0 - 操作数`，按模 2^n 回绕                    |

无符号算术天然采用模 2^n 回绕，其位级结果与有符号 `wrap` 模式下的同宽算术一致。

```text
var a: uint8 = 200
var b: uint8 = 100
var c: uint8 = add(uint8, a, b)    ; c = 44
```

### 5.7 浮点运算（`float32`、`float64`）

| 指令                  | 语义                                                         |
| --------------------- | ------------------------------------------------------------ |
| `add` / `sub` / `mul` | 和 / 差 / 积；strict 检测异常，`ieee` 返回 inf/nan           |
| `div`                 | 商；strict 检测除零/无效操作，`ieee` 返回 ±inf/nan           |
| `abs`                 | 清除符号位；包括 NaN、无穷和零在内均不报错，不接受模式关键字 |
| `neg`                 | 翻转符号位；包括 NaN、无穷和零在内均不报错，不接受模式关键字 |

`abs`/`neg` 是纯位操作：仅修改符号位并保留其余位，包括通过 `bitcast` 构造的 NaN payload。它们不属于可能触发浮点异常的算术指令。

```text
var a: float64 = 3.5
var b: float64 = 2.0

var sum: float64 = add(float64, a, b)      ; 5.5（严格模式）
var prod: float64 = mul(float64, a, b)     ; 7.0
var quot: float64 = div(float64, a, b)     ; 1.75

; abs/neg 无模式且永远安全
var neg_val: float64 = -3.14
var pos_val: float64 = abs(float64, neg_val)   ; 3.14（无异常）

; IEEE 模式
var big: float32 = 1.0e30f
var inf_res: float32 = mul(float32, ieee, big, big)  ; inf
```

### 5.8 算术运算的模式有效范围

| 指令 | 无模式形式 | `ieee` | `wrap` |
| ---- | ---------- | ------ | ------ |
| `add`/`sub`/`mul` | ✅ 全部整数 + 浮点 | ✅ 仅浮点 | ✅ 仅有符号整数 |
| `div` | ✅ 全部整数 + 浮点 | ✅ 仅浮点 | ❌ |
| `mod` | ✅ 仅整数 | ❌ | ❌ |
| `abs` | ✅ 全部整数 + 浮点 | ❌ | ❌ |
| `neg` | ✅ 全部整数 + 浮点 | ❌ | ✅ 仅有符号整数 |
| `shl` | ✅ 仅整数 | ❌ | ✅ 仅整数 |
| `shr` | ✅ 仅整数 | ❌ | ❌ |

无符号 `add`/`sub`/`mul`/`neg` 的无模式形式采用固定模 `2^n` 语义；表中的有符号整数无模式形式为 strict。

### 5.9 算术运算的扩展框架

0.0.31 规范已包含 **整数算术**、**浮点算术**、**位运算** 和 **逻辑运算**。未来规范可逐步扩展：

| 运算类别     | 当前支持                                    | 未来扩展                                    |
| ------------ | ------------------------------------------- | ------------------------------------------- |
| **整数算术** | ✅ `add`/`sub`/`mul`/`div`/`mod`/`abs`/`neg` | 任意位宽整数                                |
| **浮点算术** | ✅ `add`/`sub`/`mul`/`div`/`abs`/`neg`       | `fmod`/`remainder`、`sqrt`、`sin`、`cos` 等 |
| **位运算**   | ✅ `and`/`or`/`xor`/`not`/`shl`/`shr`        | —                                           |
| **比较运算** | ✅ `eq`/`ne`/`lt`/`le`/`gt`/`ge`             | —                                           |
| **逻辑运算** | ✅ `and`/`or`/`not`（布尔）                  | —                                           |

---

## 6. 位运算

本章定义整数位级操作，包括按位逻辑运算与移位运算。位运算与布尔逻辑运算共享关键字 `and`、`or`、`not`，由**类型参数**区分：当类型参数为整数类型时执行按位操作；当类型参数为 `bool` 时执行逻辑运算（见第 7 章）。`xor` 仅用于整数按位运算，不支持布尔类型。

> **重要约束**：
> - 移位运算（`shl`/`shr`）中，**被移位数与移位计数必须为相同类型 `T`**。若需要不同位宽，必须先用 `cast` 统一类型。
> - 浮点类型 **不支持** 任何位运算，包括 `and`/`or`/`xor`/`not`/`shl`/`shr`。尝试对浮点类型使用位运算 → **类型错误**。

**设计说明：移位计数不掩码**

TC 对移位计数 `k` 的处理方式与主流语言不同，**不进行掩码**，直接取 `k` 的数学值。选择此语义主要基于以下原因：

- **教学清晰**：避免"计数被截断"这一隐含、非直观的行为
- **确定性**：`k >= n` 时行为明确定义（`shl` 严格模式报溢出，`shr` 得 `0`）
- **可验证性**：便于形式化证明和静态分析
- **显式编程**：若需要取模移位，程序员可显式使用 `mod` 运算

### 6.1 按位逻辑运算（`and`、`or`、`xor`、`not`）

#### 6.1.1 总览

| 运算  | 名称     | 操作数数量 | 适用类型                     | 语义                             |
| ----- | -------- | ---------- | ---------------------------- | -------------------------------- |
| `and` | 按位与   | 2          | 整数类型（`int8`～`uint64`） | 每位：两操作数均为 1 → 1，否则 0 |
| `or`  | 按位或   | 2          | 整数类型                     | 每位：任一操作数为 1 → 1，否则 0 |
| `xor` | 按位异或 | 2          | 整数类型                     | 每位：两操作数不同 → 1，相同 → 0 |
| `not` | 按位非   | 1          | 整数类型                     | 每位取反                         |

> **`xor` 与 `and`/`or`/`not` 的区别**：`and`/`or`/`not` 由类型参数 `T` 重载——`T` 为整数时执行按位运算，`T` 为 `bool` 时执行逻辑运算。`xor` **无** bool 重载，仅接受整数类型参数。

#### 6.1.2 语法形式

```text
var a: int8 = 0b1010_1100      ; -84
var b: int8 = 0b1111_0000      ; -16

var and_res: int8 = and(int8, a, b)   ; 0b1010_0000 -> -96
var or_res:  int8 = or(int8, a, b)    ; 0b1111_1100 -> -4
var xor_res: int8 = xor(int8, a, b)   ; 0b0101_1100 -> 92
var not_res: int8 = not(int8, a)      ; 0b0101_0011 -> 83
```

#### 6.1.3 语义规则

1. 将操作数视为 **无符号位模式**（有符号数按二进制补码解释）。
2. 对每一位执行对应的逻辑操作。
3. 结果按目标类型 `T` 解释（有符号按补码回译，无符号直接取值）。

**溢出**：按位运算结果天然限制在 `T` 的位宽内，**不会溢出**，因此**不支持** `wrap` 或 `truncate` 模式。

**操作数约束**：
- 两操作数必须与类型参数 `T` 一致（整数类型）。
- 若类型参数为 `bool`，则 `and`/`or`/`not` 会视为逻辑运算（参见第 7 章），而 `xor` 不允许 `bool` 类型参数，将触发 **类型错误**。
- 若类型参数为浮点类型，所有位运算均触发 **类型错误**。

### 6.2 移位运算（`shl`、`shr`）

移位运算仅适用于整数类型。**被移位数与移位计数必须为相同类型 `T`**。

#### 6.2.1 左移（`shl`）

- **适用类型**：整数类型（`int8`～`uint64`）。
- **调用形式**：
  - 严格模式（默认）：`shl(<T>, 被移位数, 移位计数)`
  - 回绕模式：`shl(<T>, wrap, 被移位数, 移位计数)`
- **语义**：
  - 将移位计数解释为无符号整数，取其数学值 `k`。**TC 不对 `k` 进行掩码操作**。
  - 若 `k >= n`（`n` 为 `T` 的位宽）：
    - 严格模式：视为 **整数溢出错误**。
    - 回绕模式：所有位被移出，结果为 `0`。
  - **严格模式**（`k < n`）：
    - 计算数学结果：`result_math = 被移位数 * 2^k`
    - 若 `result_math` 超出 `T` 的可表示范围 → **整数溢出错误**。
    - 否则存储结果。
  - **回绕模式**（`k < n`）：
    - 取 `(被移位数 * 2^k)` 的低 `n` 位位模式，按 `T` 解释。**不报错**。

```text
var x: int8 = 0b0111_1111   ; 127
var r1: int8 = shl(int8, x, 2)        ; 127*4=508 -> 溢出错误
var r2: int8 = shl(int8, wrap, x, 2)  ; 508 mod 256 = 0b1111_1100 -> -4

var y: uint8 = 0b1111_1111u
var r3: uint8 = shl(uint8, wrap, y, 4)  ; 255*16 mod 256 = 0b1111_0000 -> 240u
var r4: uint8 = shl(uint8, wrap, y, 8)  ; k=8 >=8 -> 0
```

#### 6.2.2 右移（`shr`）

- **适用类型**：整数类型。
- **调用形式**：`shr(<T>, 被移位数, 移位计数)` —— **无模式关键字**（永不溢出）。
- **语义**：
  - 若 `k >= n`，结果为 `0`。
  - **有符号类型**（`int*`）：执行 **算术右移**——高位补符号位。
  - **无符号类型**（`uint*`）：执行 **逻辑右移**——高位补 `0`。

```text
var neg: int8 = 0b1000_0000   ; -128
var r5: int8 = shr(int8, neg, 1)   ; -64 (算术右移)
var r6: int8 = shr(int8, neg, 8)   ; k=8 >=8 -> 0
```

### 6.3 位运算与常量表达式

`let` 常量表达式允许使用全部合法位运算。`shl` 可使用 strict 或 `wrap`；strict 溢出映射为常量溢出错误，`wrap` 与运行时一样保留低位。其余规则与 §4.3、§6.1、§6.2 完全一致。

---

## 7. 比较运算与逻辑运算

### 7.1 比较运算

#### 7.1.1 比较运算总览

所有比较运算统一支持整数和浮点类型：

- 接受 **两个同类型操作数**（整数或浮点）
- 返回 `bool` 类型值
- **整数比较**不涉及 `wrap` / `truncate` 模式关键字
- **浮点比较**使用 IEEE 754 普通比较谓词（NaN 为 unordered），不使用 `totalOrder`；仅接受无模式形式。
- **不产生溢出**（比较本身无算术溢出风险）

| 运算 | 名称     | 语义     |
| ---- | -------- | -------- |
| `eq` | 等于     | `a == b` |
| `ne` | 不等于   | `a != b` |
| `lt` | 小于     | `a < b`  |
| `le` | 小于等于 | `a <= b` |
| `gt` | 大于     | `a > b`  |
| `ge` | 大于等于 | `a >= b` |

#### 7.1.2 语法形式

```text
; 整数比较
var eq_result: bool = eq(int32, a, b)   ; false
var lt_result: bool = lt(int32, a, b)   ; true

; 浮点比较
var f_gt: bool = gt(float64, 3.14, 2.71)  ; true
; 注意：不支持 gt(float64, ieee, 3.14, 2.71) 形式

; nan 比较（IEEE 754）
var nan_val: float64 = nan
var eq_nan: bool = eq(float64, nan_val, nan_val)  ; false
var ne_nan: bool = ne(float64, nan_val, nan_val)  ; true
```

#### 7.1.3 语义规则

**整数比较**：
- 有符号类型按二进制补码解释后的数学值比较
- 无符号类型按非负整数值比较
- 混合符号性比较须先 `cast` 统一类型

**浮点比较**：
- `eq(+0.0, -0.0)` 为 `true`，`ne(+0.0, -0.0)` 为 `false`
- `nan` 与任何值（包括自身）执行 `eq`/`lt`/`le`/`gt`/`ge` 均返回 `false`，执行 `ne` 返回 `true`
- 本组指令不区分 NaN payload，也不提供总序；若需比较位模式，先 `bitcast` 为等宽整数

#### nan 检测的惯用写法

根据 IEEE 754 普通比较谓词，`nan` 与任何值（包括自身）比较均为 unordered。检测 `nan` 的推荐写法：

```text
var x: float64 = nan
var is_nan: bool = ne(float64, x, x)    ; true（仅 nan 满足 x != x）
var not_nan: bool = eq(float64, x, x)   ; false
```

**注意**：
- 由于 `nan != nan` 为 `true`，`ne(float64, x, x)` 可可靠检测 `nan`
- 不能使用 `eq(float64, x, nan)` 检测，因为 `nan` 字面量本身也是 `nan`，比较结果仍为 `false`
- 未来版本将提供 `isnan` 内置函数作为更直观的替代

### 7.2 逻辑运算

#### 7.2.1 逻辑运算总览

所有逻辑运算：
- 接受 **`bool` 类型操作数**
- 返回 `bool` 类型值
- **不涉及** `wrap` / `truncate` 模式
- **不产生溢出**

| 运算  | 名称   | 操作数数量 | 语义                                            |
| ----- | ------ | ---------- | ----------------------------------------------- |
| `and` | 逻辑与 | 2          | 两操作数均为 `true` 时返回 `true`，否则 `false` |
| `or`  | 逻辑或 | 2          | 任一操作数为 `true` 时返回 `true`，否则 `false` |
| `not` | 逻辑非 | 1          | 操作数为 `true` 时返回 `false`，否则 `true`     |

#### 7.2.2 短路求值

**`and` 短路规则**：
- 左操作数为 `false` 时，**不计算** 右操作数，直接返回 `false`
- 左操作数为 `true` 时，计算右操作数并返回其值

**`or` 短路规则**：
- 左操作数为 `true` 时，**不计算** 右操作数，直接返回 `true`
- 左操作数为 `false` 时，计算右操作数并返回其值

确定初始化分析必须为短路求值建立操作数级的可达边。当左操作数是布尔字面量，或是在当前词法作用域可见、定义语句位于使用点之前且已求值的 `let bool` 时，使用其编译期值删除不可能的右操作数读取边：`false and RHS` 与 `true or RHS` 均不记录 RHS 的变量读取。左操作数为 `var` 时不做值推测，保守认为右操作数可达。

该裁剪只影响 CFG/DFA 的确定初始化读边，不跳过前序静态阶段。被短路排除的 RHS 不触发 `TC_ERR_UNINITIALIZED_VARIABLE`，但仍必须通过词法、语法、名称、类型、操作数数量与模式检查；前向或不可见 `let` 仍按名称解析规则报 `TC_ERR_UNDEFINED_VARIABLE`。

```text
let FALSE: bool = false
let TRUE: bool = true
goto use
var pending: bool = true
label use:
var a: bool = and(bool, FALSE, pending) ; 合法：pending 的读取边被裁剪
var b: bool = or(bool, TRUE, pending)   ; 合法：pending 的读取边被裁剪

var runtime_false: bool = false
var c: bool = and(bool, runtime_false, pending)
; 上一行仍报 TC_ERR_UNINITIALIZED_VARIABLE：var 的值不参与静态推测

var d: bool = and(bool, FALSE, missing)
; 上一行仍报 TC_ERR_UNDEFINED_VARIABLE：名称检查不被短路绕过
```

#### 7.2.3 语法形式

```text
var and_result: bool = and(bool, a, b)   ; a && b
var or_result: bool = or(bool, a, b)     ; a || b
var not_result: bool = not(bool, a)      ; !a
```

---

## 8. 类型转换

### 8.1 三种转换形式

| 形式 | 语义 | 类型约束 |
| ---- | ---- | -------- |
| `cast(T, operand)` | 数值转换 | 整数、浮点、`bool`；按本章做范围检查与舍入 |
| `cast(T, truncate, operand)` | 保留低位的整数缩窄 | 仅整数→更窄整数 |
| `bitcast(T, operand)` | 位模式不变的等宽重解释 | 等宽整数/浮点；`bool` 不参与 |

三种形式互不替代：数值 `cast` 不静默丢弃整数高位；`truncate` 不接受浮点；`bitcast` 不做数值转换。

#### 8.1.1 字面量操作数的源类型

标识符操作数的源类型是其声明类型。字面量作为转换操作数时按下列规则唯一确定源类型：

| 上下文 | 字面量 | 源类型规则 |
| ------ | ------ | ---------- |
| `cast` | 无后缀整数 | `int64`；数值必须先在 `int64` 范围内 |
| `cast` | `u`/`U` 整数 | `uint64` |
| `cast` | 浮点 | `f`/`F` 后缀为 `float32`，无后缀为 `float64`；`inf`/`-inf`/`nan` 默认为 `float64` |
| `cast` | 布尔 | `bool` |
| `bitcast` | 整数 | 源位宽取目标位宽，后缀决定有/无符号；字面量必须能由该同宽整数类型表示 |
| `bitcast` | 普通浮点 | `f`/`F` 后缀为 `float32`，无后缀为 `float64` |
| `bitcast` | `inf`/`-inf`/`nan` | 目标为 32 位时源为 `float32`，目标为 64 位时源为 `float64` |

因此 `cast(float32, 1.0)` 是从 `float64` 到 `float32` 的数值转换，而 `bitcast(float32, 0x40490FDBu)` 将后者按同宽 `uint32` 字面量解释。`bitcast(float32, 1.0)` 因源为 `float64` 而报 `TC_ERR_BITCAST_WIDTH`；需写成 `1.0f`。`bitcast` 仍不接受布尔字面量。非 `let` 语句中的严格 `cast` 即使操作数是字面量，也按运行时指令处理转换溢出；`let` 中的同一情形映射为 `TC_ERR_CONSTANT_CAST_OVERFLOW`。

### 8.2 整数严格转换规则（strict）

严格整数转换统一依据数学值判定，与源/目标位宽关系无关：

1. 将源位模式按源类型解释为数学整数 `v`。
2. 若 `min(T) ≤ v ≤ max(T)`，结果为目标类型中表示同一数学值的位模式。
3. 否则报 `TC_ERR_CAST_OVERFLOW`。

因此无符号缩窄同样检查范围；例如已有 `var wide: uint16 = 256u` 时，`cast(uint8, wide)` 必须报错。需要显式保留低位时使用 §8.3 的 `truncate`。

### 8.3 整数截断转换规则（`truncate`）

设源、目标位宽分别为 `n`、`m`：

- 源与目标必须均为整数类型，且必须满足 `m < n`。
- 将源视为 `n` 位位模式 `bits`，结果位模式为 `bits mod 2^m`，再按目标整数类型解释。
- 不执行范围检查，不报溢出。
- `m >= n` 时为静态错误 `TC_ERR_MODE_MISMATCH`；加宽应使用严格 `cast`，等宽位重解释应使用 `bitcast`。

```text
var big: int32 = 1000
var trunc: int8 = cast(int8, truncate, big) ; 低 8 位 0xE8 → -24
```

### 8.4 浮点转换规则

| 转换方向              | 规则                                                         |
| --------------------- | ------------------------------------------------------------ |
| 整数 → 浮点           | 按 roundTiesToEven 转为目标浮点；允许精度舍入；有限整数若转换为无穷 → `TC_ERR_CAST_OVERFLOW` |
| 浮点 → 整数           | 向零舍入；结果超出目标整数范围，或源为 `nan`/`inf` → `TC_ERR_CAST_OVERFLOW` |
| `float64` → `float32` | 按 roundTiesToEven 舍入；有限源值若转换为无穷 → `TC_ERR_CAST_OVERFLOW`；舍入为非规格化数或零则合法 |
| `float32` → `float64` | 有限值无损精度提升（总是合法）                               |

浮点数值转换还须遵守：正负零保留符号，正负无穷保留符号；源为 NaN 且目标仍为浮点时，结果为目标类型的 canonical quiet NaN。同类型 `cast` 为恒等操作并保留完整位模式；若需在等宽不同类型间保留 NaN payload，必须使用 `bitcast`。

```text
var i: int32 = 42
var f: float64 = cast(float64, i)        ; 42.0
var pi_f: float64 = 3.14159
var pi_int: int32 = cast(int32, pi_f)    ; 3（向零截断）
var huge: float64 = 1.0e100
; var err: int32 = cast(int32, huge)    ; 转换溢出错误
```

### 8.5 `bool` ↔ 整数/浮点转换

| 方向               | 规则                                           |
| ------------------ | ---------------------------------------------- |
| `bool` → 整数/浮点 | `true` → `1`/`1.0`，`false` → `0`/`0.0`        |
| 整数/浮点 → `bool` | `0`/`0.0` → `false`，非零 → `true`（含 `nan`） |

```text
var flag: bool = true
var as_int: int32 = cast(int32, flag)      ; 1
var as_float: float64 = cast(float64, flag) ; 1.0
var back: bool = cast(bool, 3.14)          ; true
```

### 8.6 等宽位重解释（`bitcast`）与速查

`bitcast(TargetType, operand)` 保持源位模式的全部位不变，仅改用目标类型解释：

- 源与目标必须为整数或浮点类型，且位宽完全相同。
- `bool` 不允许参与，否则报 `TC_ERR_TYPE_MISMATCH`。
- 源、目标类型相同时是保持位模式不变的合法恒等操作。
- 位宽不一致报 `TC_ERR_BITCAST_WIDTH`。
- `bitcast` 不触发数值溢出，也不规范化 NaN payload。

```text
var f: float32 = -1.0f
var bits: uint32 = bitcast(uint32, f)       ; 0xBF800000u
var f2: float32 = bitcast(float32, bits)    ; -1.0f
; var bad: float64 = bitcast(float64, bits) ; TC_ERR_BITCAST_WIDTH
```

| 源 ↓ / 目标 → | 整数 | 浮点 | `bool` |
| ------------- | ---- | ---- | ------ |
| **整数** | strict `cast` 检查数学范围；更窄目标可显式 `truncate` | strict `cast` 做数值转换；等宽 `bitcast` 做位重解释 | 零为 `false`，非零为 `true` |
| **浮点** | strict `cast` 向零舍入并检查；等宽 `bitcast` 做位重解释 | strict `cast` 做精度转换；不同位宽禁止 `bitcast` | `±0.0` 为 `false`，其他值（含 NaN）为 `true` |
| **`bool`** | `false`→0，`true`→1 | `false`→0.0，`true`→1.0 | 恒等 |

---

# 第三部分：I/O 与交互

---

## 9. 标准 I/O

### 9.1 `write` — 不换行输出

**形式一：无格式输出**

| 项目       | 说明                                                         |
| ---------- | ------------------------------------------------------------ |
| 语法       | `write(int32, x)`、`write(float64, pi)`、`write(bool, flag)` |
| 功能       | 将操作数的值按类型解释写入 stdout，**不换行**                |
| 操作数规则 | 在当前 CFG 点确定初始化的变量，或字面量                   |
| 类型约束   | 变量类型与 `type` 一致；字面量满足 §3.6 约束                 |
| 输出格式   | 整数：十进制；浮点：与 `%g` 相同的 6 位有效数字；布尔：`true`/`false` |

**形式二：格式化输出**

| 项目       | 说明                                                         |
| ---------- | ------------------------------------------------------------ |
| 语法       | `write(int32, %d, x)`、`write(float64, %f, pi)`、`write(bool, %t, flag)` |
| 功能       | 按格式化符号输出操作数的值，**不换行**                       |
| 操作数规则 | 变量或字面量；**恰好一个**操作数                             |
| 类型约束   | 操作数类型与 `type` 一致，且与格式化符号兼容                 |

### 9.2 `writeln` — 换行输出

与 `write` 相同，但在输出末尾追加一个 LF 字节（`\n`），不随主机平台改写为其他行结束序列。`write`/`writeln` 向 stdout 写入失败时报 `TC_ERR_IO`。

### 9.3 `read` — 标准输入

| 项目     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 语法     | `read(int32, x)`、`read(float64, radius)`、`read(bool, flag)` |
| 功能     | 从 stdin 读取一个值，**覆盖**已定义变量的当前值              |
| 目标变量 | 必须已通过带 RHS 的 `var` 声明、在当前 CFG 点确定初始化，且类型与 `type` 一致 |
| 与初始化 | **`read` 不是初始化器**，不能代替 `var … = …` 或补救被控制流绕过的初始化 |
| 输入格式 | 见下表；跳过前导 ASCII 空白，并读取一个以 ASCII 空白或 EOF 结束的完整 Token |
| 错误处理 | 非法输入/数值超范围/非预期 EOF → **I/O 错误**                |

**输入规则**：

| 目标类型            | 输入格式                                      |
| ------------------- | --------------------------------------------- |
| `int*`              | 可选 `-` 前缀 + 连续十进制数字，至少一位      |
| `uint*`             | 连续十进制数字，至少一位；**不接受** `-` 前缀 |
| `float32`/`float64` | 可选 `-` 前缀 + 十进制浮点/科学计数法，或 `inf`/`-inf`/`nan` |
| `bool`              | `true` 或 `false`（大小写敏感，均为小写）     |

本节的 ASCII 空白仅指 U+0009～U+000D 与 U+0020，不受 locale 影响。输入必须完整匹配目标格式；例如 `12abc` 整体非法，不得只接受前缀 `12`。

有限浮点输入先解析为精确十进制数学值，再按 roundTiesToEven 舍入到目标 binary32/binary64；不得先通过另一浮点类型中转。显式 `-0.0` 保留负零符号。非零有限值舍入为零、有限值舍入为无穷，或整数超出目标范围，均报 `TC_ERR_IO`；可表示的非规格化数合法。`nan` 输入生成 §5.3 规定的 canonical quiet NaN。非法 Token 与意外 EOF 同样报 `TC_ERR_IO`。

### 9.4 格式化符号定义

| 符号 | 含义                     | 适用类型            | 示例（值 42/3.14159265） |
| ---- | ------------------------ | ------------------- | ------------------ |
| `%d` | 有符号十进制             | `int8`～`int64`     | `42`               |
| `%i` | 有符号十进制（同 `%d`）  | `int8`～`int64`     | `42`               |
| `%u` | 无符号十进制             | `uint8`～`uint64`   | `42`               |
| `%x` | 十六进制（小写 a-f）     | 任一整数类型        | `2a`               |
| `%X` | 十六进制（大写 A-F）     | 任一整数类型        | `2A`               |
| `%o` | 八进制                   | 任一整数类型        | `52`               |
| `%b` | 二进制                   | 任一整数类型        | `101010`           |
| `%t` | 布尔值文本               | `bool`              | `true`             |
| `%f` | 浮点十进制（固定小数）   | `float32`/`float64` | `3.141593`         |
| `%e` | 科学计数法（小写 e）     | `float32`/`float64` | `3.141593e+00`     |
| `%E` | 科学计数法（大写 E）     | `float32`/`float64` | `3.141593E+00`     |
| `%g` | 紧凑格式（较短）         | `float32`/`float64` | `3.14159`          |
| `%G` | 紧凑格式（较短，大写 E） | `float32`/`float64` | `3.14159`          |

**确定性格式规则**：

- 格式化不受进程 locale 影响，小数点固定为 `.`。
- `%f`/`%e`/`%E` 固定输出小数点后 6 位；`%g`/`%G` 固定输出 6 位有效数字。
- 十进制输出以源 binary32/binary64 值的精确数学值为输入，按 roundTiesToEven 舍入到所需十进制位数；不得先经过宿主的更低精度类型。
- `%e`/`%E` 在小数点前保留恰好一位非零数字（零值为 `0`），指数始终含 `+` 或 `-`，且至少两位十进制数字。
- `%g`/`%G` 先以 6 位有效数字舍入。设舍入后值的十进制指数为 `e`（即科学计数法中小数点前恰好一位时的指数）：当 `e < -4` 或 `e >= 6` 时使用 `%e`/`%E` 形式，否则使用定点形式。小数部分末尾的 `0` 全部删除；若小数部分为空，同时删除小数点。
- `%d`/`%i` 按有符号数学值输出，`%u` 按无符号数学值输出。
- `%x`/`%X`/`%o`/`%b` 对任意整数输出其固定宽度二进制补码位模式，不带进制前缀。零恰好输出 `0`；其他非负值省略前导零；负值保留足以表示完整类型位宽的全部数字。例如 `int8` 值 `-1` 分别输出 `ff`、`FF`、`377`、`11111111`。
- `%e` 使用小写指数标记，`%E` 使用大写指数标记；`%G` 的指数标记与特殊值使用大写。
- 有限负零保留符号，例如 `%f` 输出 `-0.000000`，`%g` 输出 `-0`。
- 特殊值固定输出 `inf`、`-inf`、`nan`；在 `%E`/`%G` 下输出 `INF`、`-INF`、`NAN`。NaN payload 不通过文本 I/O 暴露。

### 9.5 类型兼容性

| 符号                             | 可接受操作数类型     |
| -------------------------------- | -------------------- |
| `%d` / `%i`                      | `int8`～`int64`      |
| `%u`                             | `uint8`～`uint64`    |
| `%x` / `%X` / `%o` / `%b`        | 任一整数类型         |
| `%t`                             | `bool`               |
| `%f` / `%e` / `%E` / `%g` / `%G` | `float32`、`float64` |

### 9.6 格式化输出扩展预留

0.0.31 规范包含 §9.4 所列 **13 种**格式化符号（含 `%f`/`%e`/`%E`/`%g`/`%G`）。以下为后续规范的扩展方向：

> **扩展预留**：
> - `%c`：字符
> - `%s`：字符串
> - `%p`：指针
> - 宽度、精度、左对齐等修饰符（如 `%10.2f`）
>
> 上述符号与修饰符尚未纳入当前语法；使用前须等待对应语言版本发布。

---

# 第四部分：完整示例与规范

---

## 10. 完整示例集

### 10.1 变量定义与初始化

```text
var a: int32 = 42
var b: int8 = -128
var c: int32 = 0          ; 必须初始化
var d: uint8 = 0u

let MAX_U8: uint8 = 255u
let MASK_32: uint32 = 0xFFFF_FFFFu
let PI: float64 = 3.141592653589793
let E: float32 = 2.71828f
let DEBUG: bool = true
```

### 10.2 字面量后缀与符号性

```text
var a: int32 = 42
var b: int8 = -128
var c: int16 = 0x7FFF

var d: uint8 = 255u
var e: uint32 = 0xFFFF_FFFFu
var f: uint16 = 0b1111_1111_1111_1111u

var g: uint8 = 255          ; 合法
; var h: uint8 = 256        ; 字面量范围错误

var pi: float64 = 3.14159   ; 合法
var e32: float32 = 2.71828f ; 合法
var inf_val: float64 = inf  ; 合法
; var err: int32 = 3.14     ; 字面量类型错误

var flag: bool = true
; var err: int32 = true    ; 字面量类型错误
```

### 10.3 整数算术运算

```text
var a: int32 = 10
var b: int32 = 20
var sum:  int32 = add(int32, a, b)
var diff: int32 = sub(int32, a, b)
var prod: int32 = mul(int32, a, b)
var quot: int32 = div(int32, b, a)
var rem:  int32 = mod(int32, b, a)
```

### 10.4 浮点算术运算

```text
var x: float64 = 3.5
var y: float64 = 2.0
var fsum: float64 = add(float64, x, y)      ; 5.5
var fdiff: float64 = sub(float64, x, y)     ; 1.5
var fprod: float64 = mul(float64, x, y)     ; 7.0
var fquot: float64 = div(float64, x, y)     ; 1.75
var fabs: float64 = abs(float64, -3.14)     ; 3.14
var fneg: float64 = neg(float64, 3.14)      ; -3.14

; var fmod: float64 = mod(float64, x, y)    ; 类型错误：mod 不支持浮点
```

### 10.5 整数算术回绕

```text
var a: int8 = 127
var b: int8 = 1
var wrap_result: int8 = add(int8, wrap, a, b)    ; -128

var big: int16 = 30000
var small: int16 = 30000
var prod: int16 = mul(int16, wrap, big, small)   ; -5888
```

### 10.6 浮点两种模式对比

```text
var big: float32 = 1.0e30f
var zero: float32 = 0.0f

; 严格模式（默认）
; var err1: float32 = mul(float32, big, big)     ; 浮点溢出错误
; var err2: float32 = div(float32, big, zero)    ; 除零错误

; IEEE 754 标准模式
var ieee_inf: float32 = mul(float32, ieee, big, big)  ; inf
var ieee_nan: float32 = div(float32, ieee, zero, zero) ; nan

writeln(float32, %g, ieee_inf)   ; "inf"
writeln(float32, %g, ieee_nan)   ; "nan"
```

### 10.7 比较运算

```text
var a: int32 = 10
var b: int32 = 20
var eq_res: bool = eq(int32, a, b)   ; false
var lt_res: bool = lt(int32, a, b)   ; true

; 浮点比较
var x: float64 = 3.14
var y: float64 = 2.71
var f_gt: bool = gt(float64, x, y)   ; true
var f_eq: bool = eq(float64, x, x)   ; true

; nan 比较
var nan_val: float64 = nan
var nan_eq: bool = eq(float64, nan_val, nan_val)  ; false
var nan_ne: bool = ne(float64, nan_val, nan_val)  ; true
```

### 10.8 逻辑运算

```text
var a: bool = true
var b: bool = false
var and_ab: bool = and(bool, a, b)        ; false
var or_ab: bool = or(bool, a, b)          ; true
var not_a: bool = not(bool, a)            ; false
```

### 10.9 短路求值

```text
var flag: bool = false
var init_val: bool = true

; 短路：左操作数为 false 时不求值右操作数（TC 操作数不可嵌套，无副作用）
var result: bool = and(bool, flag, init_val)   ; false
```

### 10.10 类型转换

```text
; 整数 ↔ 整数
var x: int32 = 50
var y: uint8 = 200u
var y32: int32 = cast(int32, y)          ; 200

; 整数 ↔ 浮点
var i: int32 = 42
var f: float64 = cast(float64, i)        ; 42.0
var pi_f: float64 = 3.14159
var pi_i: int32 = cast(int32, pi_f)      ; 3

; 浮点 ↔ 浮点
var double: float64 = 1.2345678901234567
var single: float32 = cast(float32, double)  ; 1.2345679f

; 按位重解释
var neg_f: float32 = -1.0f
var bits: uint32 = bitcast(uint32, neg_f)  ; 0xBF800000
var pi_from_bits: float32 = bitcast(float32, 0x40490FDBu)  ; 3.1415927

; 整数截断
var big: int32 = 1000
var truncated: int8 = cast(int8, truncate, big)  ; -24

; bool ↔ 整数/浮点
var flag: bool = cast(bool, x)          ; true
var as_int: int32 = cast(int32, flag)   ; 1
var as_float: float64 = cast(float64, flag) ; 1.0
```

### 10.11 复合计算

```text
; (a + b) * c
var a: int16 = 3
var b: int16 = 4
var c: int16 = 5
var t: int16 = add(int16, a, b)
var result: int16 = mul(int16, t, c)     ; 35

; 浮点混合计算：圆面积
var radius: float64 = 5.0
let PI: float64 = 3.141592653589793
var radius_sq: float64 = mul(float64, radius, radius)
var area: float64 = mul(float64, PI, radius_sq)  ; 78.53981633974483
```

### 10.12 I/O 格式化输出

```text
var a: int32 = 42
var pi: float64 = 3.141592653589793
var flag: bool = true

writeln(int32, %d, a)              ; "42"
writeln(float64, %f, pi)           ; "3.141593"
writeln(float64, %e, pi)           ; "3.141593e+00"
writeln(float64, %g, pi)           ; "3.14159265358979"
writeln(bool, %t, flag)            ; "true"

; 特殊值输出
writeln(float64, %f, inf)          ; "inf"
writeln(float64, %f, -inf)         ; "-inf"
writeln(float64, %f, nan)          ; "nan"
```

### 10.13 if-else 条件分支

```text
var score: int32 = 85
var grade: int8 = 0

if ge(int32, score, 90) then
    grade = 1
else
    if ge(int32, score, 80) then
        grade = 2
    else
        if ge(int32, score, 70) then
            grade = 3
        else
            grade = 4
        end
    end
end
writeln(int8, %d, grade)   ; 2
```

### 10.14 while 结构化循环

**示例 1：简单 while 循环（1 到 10 求和）**

```text
var sum: int32 = 0
var i: int32 = 1

while le(int32, i, 10) then
    sum = add(int32, sum, i)
    i = add(int32, i, 1)
end

writeln(int32, %d, sum)   ; 55
```

**示例 2：使用 break 提前退出**

```text
var i: int32 = 0
while lt(int32, i, 100) then
    i = add(int32, i, 1)
    if gt(int32, i, 50) then
        break
    end
end
writeln(int32, %d, i)   ; 51
```

**示例 3：使用 continue 跳过偶数**

```text
var i: int32 = 0
while lt(int32, i, 10) then
    i = add(int32, i, 1)
    if eq(int32, mod(int32, i, 2), 0) then
        continue
    end
    writeln(int32, %d, i)   ; 输出 1,3,5,7,9（每行一个）
end
```

**示例 4：嵌套循环与 break/continue 作用于最内层**

```text
var outer: int32 = 0
while lt(int32, outer, 3) then
    var inner: int32 = 0
    while lt(int32, inner, 5) then
        inner = add(int32, inner, 1)
        if eq(int32, inner, 3) then
            continue   ; 跳过 inner=3 的输出
        end
        if gt(int32, inner, 4) then
            break      ; 退出内层循环
        end
        writeln(int32, %d, inner)   ; 输出 1,2,4
    end
    outer = add(int32, outer, 1)
end
```

**示例 5：循环外误用 break/continue（静态错误）**

```text
; break
var i: int32 = 0
if eq(int32, i, 0) then
    break          ; ❌ 静态错误：TC_ERR_BREAK_OUTSIDE_LOOP
end

; continue
while lt(int32, i, 10) then
    i = add(int32, i, 1)
    if gt(int32, i, 5) then
        continue   ; ✅ 合法，在循环内
    end
end
continue             ; ❌ 静态错误：TC_ERR_CONTINUE_OUTSIDE_LOOP
```

### 10.15 非结构化循环（`if` + `goto`）

> **设计说明**：非结构化循环完全使用 `if` + `goto` 构建，不涉及 `while` 关键字。此范式适用于状态机、复杂退出条件或需要直接从任意深度跳出的场景。

**示例 1：`if` + `goto` 模拟 `while` 循环**

> **缩进说明**（§4.9.2）：`label` 不产生新缩进；`sum_goto` / `j` / `if` / `writeln` 与 `label loop_start:` 同为全局缩进 0；仅 `if` 子块内的 `goto` 多一级。

```text
; 非结构化循环：计算 1 到 10 的和
var sum_goto: int32 = 0
var j: int32 = 1

label loop_start:
sum_goto = add(int32, sum_goto, j)
j = add(int32, j, 1)
if le(int32, j, 10) then
    goto loop_start
end

writeln(int32, %d, sum_goto)          ; 55
```

**示例 2：`if` + `goto` 实现复杂状态机**

```text
; 状态机：累乘直到结果 ≥ 100
var state: int32 = 0
var result: int32 = 1

label process:
if eq(int32, state, 0) then
    result = mul(int32, result, 2)
    state = 1
    goto process
end

if eq(int32, state, 1) then
    result = add(int32, result, 5)
    state = 2
    goto process
end

if eq(int32, state, 2) then
    if lt(int32, result, 100) then
        state = 0
        goto process
    end
end

writeln(int32, %d, result)     ; 最终结果 ≥ 100
```

**示例 3：`if` + `goto` 跳出深层嵌套**

```text
; 从深层 if 嵌套中直接跳出到结构之后的全局标签
var x: int32 = 0
var y: int32 = 0
var found: bool = false

if lt(int32, x, 100) then
    x = add(int32, x, 10)
    if gt(int32, x, 50) then
        y = add(int32, y, 5)
        if gt(int32, y, 20) then
            found = true
            goto exit          ; ✅ 直接从深层跳出到结构之后
        end
    end
end

label exit:
writeln(bool, %t, found)       ; true
```

**示例 4：非法示例——`while` 内使用 `goto`（范式隔离违规）**

```text
; ❌ 错误：结构化循环体内禁止 goto
var i: int32 = 0
while lt(int32, i, 10) then
    i = add(int32, i, 1)
    if gt(int32, i, 5) then
        goto after_loop        ; 静态错误：TC_ERR_GOTO_INSIDE_LOOP
    end
end
label after_loop:
```

### 10.16 `goto` 跳转（非循环场景）

**示例 1：向 if 块外跳转**
```text
var x: int32 = 5

if gt(int32, x, 0) then
    x = mul(int32, x, 2)
    goto after_if
end

label after_if:
writeln(int32, %d, x)              ; 10（与 label 同缩进）
```

**示例 2：非法跳入子块（静态错误）**
```text
goto inner                     ; 错误：跳入子块

if gt(int32, a, 0) then
    label inner:
    writeln(int32, %d, a)
end
```

**示例 3：向前跳转导致未初始化读取（静态错误）**
```text
goto skip_init
var init: int32 = 42
label skip_init:
writeln(int32, %d, init)       ; TC_ERR_UNINITIALIZED_VARIABLE
```

### 10.17 综合示例：浮点计算与分支

```text
; 计算圆的面积，输入半径
; radius / area 必须在声明处给出初值；read 仅覆盖 radius
var radius: float64 = 0.0
var area: float64 = 0.0

read(float64, radius)

if ge(float64, radius, 0.0) then
    let PI: float64 = 3.141592653589793
    var radius_sq: float64 = mul(float64, radius, radius)
    area = mul(float64, PI, radius_sq)
    writeln(float64, %g, area)
else
    writeln(float64, %g, -1.0)
end
```

### 10.18 综合示例：浮点位模式哈希

浮点类型 **不支持** 位运算（§6）。位级混合须先将浮点按位重解释为整数，再使用整数 `xor`：

```text
; 浮点位模式哈希：bitcast 等宽重解释 → 整数 xor
var f1: float32 = 3.14f
var f2: float32 = 2.71f
var f3: float32 = 1.41f

var bits1: uint32 = bitcast(uint32, f1)
var bits2: uint32 = bitcast(uint32, f2)
var bits3: uint32 = bitcast(uint32, f3)

var hash: uint32 = xor(uint32, bits1, bits2)
hash = xor(uint32, hash, bits3)

writeln(uint32, %X, hash)
```

浮点类型不提供 `wrap`。所有位级混合都必须显式 `bitcast` 为等宽整数后完成；需要恢复浮点解释时再 `bitcast` 回原位宽浮点类型。

---

## 11. 错误与警告

遇到 **第一个** 错误即终止程序（fail-fast）。错误按发生阶段分为 **静态**（词法/语法/静态分析）与 **运行时** 两类。

### 11.0 确定性诊断阶段与初始化错误分工

0.0.31 采用确定的静态处理顺序：

1. 源字节的 UTF-8 解码（文件打开失败属于实现/API 层）；
2. 词法与缩进扫描；
3. 语法解析；
4. 名称解析与作用域检查；
5. 类型、操作数数量与模式检查；
6. `let` 常量求值；
7. 按 §4.3 执行 `if`/`while` 静态布尔三态判定，并按 §7.2.2 判定逻辑 RHS 读边；
8. CFG 构建与不可达边裁剪；
9. 在最终 CFG 上执行确定初始化数据流固定点分析（DFA）。

阶段按上述顺序完整执行；只有前一阶段无错时才进入后一阶段。同一阶段存在多个错误时，按源文件行、列顺序报告第一个。若同一 Token 位置同时违反多个类型/模式规则，固定按“操作数数量 → 运算支持的类型类别 → 模式合法性 → `bitcast` 位宽 → 字面量范围/类型 → 格式符与其他操作数类型”的顺序选择诊断。计算操作数数量时，紧跟类型参数的 `wrap`/`ieee`/`truncate` 先识别为候选模式位，不计为普通操作数；若非模式指令写了数量正确的候选模式形式，因而稳定映射为 `TC_ERR_MODE_MISMATCH`。

非法 UTF-8 映射为 `TC_ERR_SYNTAX`；缩进与语法阶段已有专用错误码的情形优先使用专用码，其余才使用 `TC_ERR_SYNTAX`。`let` RHS 中的嵌套调用在语法阶段专门映射为 `TC_ERR_CONSTANT_EXPRESSION`；自引用和前向引用在名称阶段映射为 `TC_ERR_UNDEFINED_VARIABLE`；已解析为 `var` 的引用在常量求值阶段映射为 `TC_ERR_CONSTANT_EXPRESSION`。静态布尔判定仅在名称和类型检查以及全部源序更早的 `let` 求值成功后运行；合法常量形态的求值错误按 §4.3 映射并在 CFG/DFA 前终止，不得以“条件未知”吞掉。不可达代码仍完成词法、语法、名称和类型检查，只有 CFG 裁剪后不再可达的变量读取免于未初始化错误。初始化相关错误按下表严格分工：

| 层次 | 违规形态 | 错误码 | 检查性质 | 规范依据 |
| ---- | -------- | ------ | -------- | -------- |
| **语法层** | `var` 声明缺少 `= <rhs>` | `TC_ERR_VAR_MISSING_INIT` | 纯形态；与控制流无关 | §4.2 |
| **名称层** | 标识符从未声明、尚未源序可见或已超出词法作用域 | `TC_ERR_UNDEFINED_VARIABLE` | 名称解析 | §3.8 |
| **数据流层** | 声明存在，但在可达使用点不满足确定初始化 | `TC_ERR_UNINITIALIZED_VARIABLE` | 完整 CFG 固定点分析 | §3.9、§4.9.4 |

`TC_ERR_VAR_MISSING_INIT` 不得降级为通用语法错误；`TC_ERR_UNINITIALIZED_VARIABLE` 不得替代名称解析错误。0.0.31 不定义 `TC_ERR_GOTO_SKIPS_VAR_INIT`，因为未初始化是跨全部控制流种类的通用性质，不应绑定到单一语句。

**实现状态说明（非规范性）**：本文编写时现行实现为 v0.0.26。本文只定义 0.0.31 目标语义，不声明错误码枚举或处理阶段已在现行实现中完成迁移。

### 11.1 静态错误

| 错误类型                 | 触发条件                                                     |
| ------------------------ | ------------------------------------------------------------ |
| 词法错误                 | 非法字符、整数字面量格式非法、浮点字面量格式非法等           |
| 语法错误                 | token 序列不符合附录 A 的产生式（**不含**已单列的「变量缺少初始化器」；该情况见下行） |
| 未定义标识符错误         | 使用未 `var`/`let` 声明的标识符；或源序/块作用域下尚不可见（命名解析失败） |
| 重复定义错误             | 同一作用域重复 `var`/`let` 同名标识符                        |
| **变量缺少初始化器**     | **`var` 未写成 `var <名>: <类型> = <rhs>`（缺少 `=` 与 RHS）。声明时必须初始化；与控制流无关（§4.2、§11.0）** |
| 类型错误                 | 运算或赋值时类型不一致；例如：逻辑运算操作数非 `bool`；`xor` 用于 `bool` 类型；位运算用于浮点类型；`mod` 用于浮点；`ieee` 用于整数类型等 |
| 字面量范围错误           | 整数字面量数值超出上下文类型可表示范围；浮点字面量超出 `float32`/`float64` 范围 |
| 字面量类型错误           | `u` 后缀字面量用于有符号上下文；浮点字面量用于整数类型；浮点后缀决定的类型与显式上下文不一致；`true`/`false` 用于非 `bool` 类型；整数用于 `bool` 类型（未通过 `cast`） |
| 模式不匹配错误           | 操作、类型与 `wrap`/`ieee`/`truncate` 组合不在 §5.1 或 §8 的合法矩阵中 |
| 常量赋值错误             | 对 `let` 定义的常量进行赋值                                  |
| 常量表达式错误           | `let` 的 RHS 含 `var`、嵌套调用、其他不符合 §4.3 的形式，或浮点严格常量算术触发无效操作；自引用/前向引用属于未定义标识符 |
| 常量溢出错误             | `let` 常量表达式求值过程中发生有符号算术溢出、浮点严格运算上溢/下溢，或 `shl` 严格模式溢出 |
| 常量除零错误             | `let` 中整数 `div`/`mod` 除数为 0，或浮点严格 `div` 触发除零 |
| 常量转换溢出错误         | `let` 常量表达式中严格 `cast` 的数学结果无法由目标类型表示         |
| 格式字符串错误           | 格式化符号不是 `%d`/`%i`/`%u`/`%x`/`%X`/`%o`/`%b`/`%t`/`%f`/`%e`/`%E`/`%g`/`%G` 之一 |
| 格式类型不匹配           | 操作数类型与格式化符号不兼容                                 |
| 操作数数量错误           | 格式化版本缺少操作数，或多于一个操作数；`and`/`or`/`xor` 操作数数量不为 2；`not` 操作数数量不为 1 |
| 比较类型不匹配           | 比较运算的两操作数类型不一致，或与类型参数不一致             |
| 缩进不一致               | 同一文件中混用空格和制表符                                   |
| 块内缩进层级错误         | 直接块内语句未比当前块级恰好多一级，或缩进宽度不对应已有层级 |
| `else`/`end` 缩进错      | `else` 或 `end` 的缩进与对应的 `if`/`while` 行不一致         |
| 缺少 `end`               | `if`/`while` 语句未以 `end` 关键字结束                       |
| `else` 位置错误          | `else` 前没有对应的 `if` 块（即 `else` 出现在非块结尾位置）  |
| 条件类型错误             | `if`/`while` 的条件 RHS 结果类型不是 `bool`                  |
| 位重解释宽度错误         | `bitcast` 的源、目标位宽不同                                 |
| 未初始化变量错误         | 变量声明存在，但当前 CFG 点并非所有可达路径均已执行其初始化 |
| 标签未找到错误           | `goto` 引用了未定义的标签（在当前作用域及所有祖先作用域中均未找到） |
| 标签重复定义错误         | 同一作用域内定义了同名标签（不同块允许重名）                 |
| 标签定义在循环内错误     | `label` 定义在 `while` 块的作用域内（见 §4.8.4）             |
| 跳入块错误               | 试图跳入内层子块（见 §4.9.3）                                |
| 跳入兄弟块错误           | 试图跳入 `then`/`else` 兄弟分支（见 §4.9.3）                 |
| **循环内 goto 错误**     | **`goto` 语句的词法祖先中包含 `while` 块（见 §4.8.4）**      |
| **循环外 break 错误**    | **`break` 语句不在任何 `while` 循环体内（含嵌套块）**        |
| **循环外 continue 错误** | **`continue` 语句不在任何 `while` 循环体内（含嵌套块）**     |

> **0.0.31 已删除的旧分类**：`TC_ERR_GOTO_SKIPS_VAR_INIT`、常量循环依赖错误、跨块引用专用错误和重复的 overflow-mode 错误；分别由确定初始化、源序可见性、`TC_ERR_UNDEFINED_VARIABLE` 与 `TC_ERR_MODE_MISMATCH` 统一承担。

### 11.2 运行时错误

| 错误类型         | 触发条件                                                     |
| ---------------- | ------------------------------------------------------------ |
| 除零错误         | `div`/`mod` 右操作数为 0（整数）；严格模式浮点 `div` 除数为 0 |
| 整数溢出错误     | 有符号类型严格模式下 `add`/`sub`/`mul`/`neg` 结果超范围；`shl` 严格模式结果超范围 |
| 浮点溢出错误     | 严格模式浮点运算上溢                                         |
| 浮点下溢错误     | 严格模式浮点运算下溢                                         |
| 浮点无效操作错误 | 严格 `add/sub/mul/div` 的输入含 NaN 或产生无效结果（如 0/0、∞−∞、0×∞）；`abs/neg` 除外 |
| 转换溢出错误     | 严格 `cast` 的数学结果无法在目标类型中表示，或浮点源为 NaN/无穷且目标为整数 |
| I/O 错误         | `read` 输入非法/超范围/EOF，标准输入读取失败，或 `write`/`writeln` 写入失败 |

### 11.3 编译警告

TC 语言**无编译警告**（无 `TcWarningKind` 承担初始化或控制流诊断）。

- 声明缺少初始化器 → **错误** `TC_ERR_VAR_MISSING_INIT`（§4.2 / §11.0），不得以警告放行。
- 任一可达路径上的未初始化使用 → **错误** `TC_ERR_UNINITIALIZED_VARIABLE`；不得以警告放行。
- 溢出、类型与其它控制流违规均以错误报告。
- 逻辑短路（§7.2.2）排除的右操作数不触发未初始化错误，但其名称、类型或语法违规仍是静态错误。

### 11.4 错误码对照表（`TcErrorKind`）

§11.1、§11.2 以**可观测语义**命名错误类型；TC 实现通过 `TcErrorKind` 枚举编码诊断结果。`tc_error_kind_name()` 返回的打印名用于日志与 API 输出。

本表为 **0.0.31 规范的权威枚举清单**。表中共有 42 个条目：41 个语言错误码（34 个静态、7 个运行时）与 1 个实现扩展码 `TC_ERR_OUT_OF_MEMORY`。语言错误码决定可观察诊断；实现扩展码不改变语言程序的合法性。

**实现专用错误码说明**

以下错误码表中，`TC_ERR_OUT_OF_MEMORY` 等带有"实现扩展"标注的错误码**不由用户程序直接触发**，而是在内部分配失败时产生。这些错误码仅供实现方参考，语言标准语义层面不涉及此类错误。用户程序编写时无需处理这些错误。

**交互式解释器（REPL，非规范性）**：0.0.31 只定义批量源文件语义。REPL 可以限制多行控制流，但该限制不得解释为 TC 语法规则，也不得用于判断一个批量程序是否符合 0.0.31。文件打开失败、OOM 等同样属于实现/API 层错误。

| 错误码                             | 打印名                       | 语言标准类型（§11.1 / §11.2） | 阶段        | 说明                                                         |
| ---------------------------------- | ---------------------------- | ----------------------------- | ----------- | ------------------------------------------------------------ |
| `TC_ERR_SYNTAX`                    | `SyntaxError`                | 词法错误、语法错误            | 静态        | 非法 Token 或不符合附录 A；不含文件打开失败、REPL 限制和缺初始化器 |
| `TC_ERR_UNDEFINED_VARIABLE`        | `UndefinedVariable`          | 未定义标识符错误              | 静态        | 命名解析失败：未声明、前向引用、初始化自引用、跨块不可见等；**不**表示缺 RHS |
| `TC_ERR_DUPLICATE_DEFINITION`      | `DuplicateDefinition`        | 重复定义错误                  | 静态        | 同一作用域重复 `var`/`let` 同名                              |
| **`TC_ERR_VAR_MISSING_INIT`**      | **`VarMissingInitializer`**  | **变量缺少初始化器**          | **静态**    | **声明时必须初始化（§4.2 / §11.0）；形态检查，与控制流无关** |
| **`TC_ERR_UNINITIALIZED_VARIABLE`** | **`UninitializedVariable`** | **未初始化变量错误**          | **静态**    | **声明存在，但当前 CFG 点不满足确定初始化（§3.9）**          |
| `TC_ERR_TYPE_MISMATCH`             | `TypeMismatch`               | 类型错误                      | 静态        | 运算/赋值类型不一致；逻辑运算操作数非 `bool`；位运算用于浮点等 |
| `TC_ERR_LITERAL_OUT_OF_RANGE`      | `LiteralOutOfRange`          | 字面量范围错误                | 静态        | 整数/浮点字面量超出上下文类型范围                            |
| `TC_ERR_LITERAL_TYPE`              | `LiteralTypeError`           | 字面量类型错误                | 静态        | `u` 后缀误用、浮点/整数/布尔类别误用，或浮点后缀类型与显式上下文不一致 |
| `TC_ERR_KEYWORD`                   | `KeywordError`               | 关键字错误                    | 静态        | 保留关键字出现在必须为标识符的位置；已进入模式参数位置的组合错误使用 `TC_ERR_MODE_MISMATCH` |
| `TC_ERR_CONSTANT_ASSIGNMENT`       | `ConstantAssignmentError`    | 常量赋值错误                  | 静态        | 对 `let` 常量赋值                                            |
| `TC_ERR_CONSTANT_EXPRESSION`       | `ConstantExpressionError`    | 常量表达式错误                | 静态        | RHS 含 `var`、嵌套调用、其他非法形式，或浮点严格常量算术触发无效操作；自引用/前向引用使用 `TC_ERR_UNDEFINED_VARIABLE` |
| `TC_ERR_CONSTANT_OVERFLOW`         | `ConstantOverflow`           | 常量溢出错误                  | 静态        | 编译期有符号整数或 `shl` 严格模式溢出，或浮点严格运算上溢/下溢 |
| `TC_ERR_CONSTANT_DIV_ZERO`         | `ConstantDivisionByZero`     | 常量除零错误                  | 静态        | 编译期整数 `div`/`mod` 除数为 0，或浮点严格 `div` 触发除零 |
| `TC_ERR_CONSTANT_CAST_OVERFLOW`    | `ConstantCastOverflow`       | 常量转换溢出错误              | 静态        | 编译期严格 `cast` 的数学结果无法由目标类型表示                 |
| `TC_ERR_COMPARISON_TYPE_MISMATCH`  | `ComparisonTypeMismatch`     | 比较类型不匹配                | 静态        | 比较运算两操作数类型不一致                                   |
| `TC_ERR_FORMAT_STRING`             | `FormatStringError`          | 格式字符串错误                | 静态        | 非法 `%` 格式化符号                                          |
| `TC_ERR_FORMAT_TYPE_MISMATCH`      | `FormatTypeMismatch`         | 格式类型不匹配                | 静态        | 操作数类型与格式化符号不兼容                                 |
| `TC_ERR_OPERAND_COUNT`             | `OperandCountError`          | 操作数数量错误                | 静态        | 格式化/I/O 或逻辑运算操作数个数非法                          |
| `TC_ERR_DIVISION_BY_ZERO`          | `DivisionByZero`             | 除零错误                      | 运行时      | `div`/`mod` 除数为 0                                         |
| `TC_ERR_INTEGER_OVERFLOW`          | `IntegerOverflow`            | 整数溢出错误                  | 运行时      | 严格模式整数算术或 `shl` 超范围                              |
| `TC_ERR_FLOAT_OVERFLOW`            | `FloatOverflow`              | 浮点溢出错误                  | 运行时      | 严格模式浮点运算上溢                                         |
| `TC_ERR_FLOAT_UNDERFLOW`           | `FloatUnderflow`             | 浮点下溢错误                  | 运行时      | 严格模式浮点运算下溢                                         |
| `TC_ERR_FLOAT_INVALID`             | `FloatInvalidOperation`      | 浮点无效操作错误              | 运行时      | 严格模式浮点运算产生 `nan`                                   |
| `TC_ERR_CAST_OVERFLOW`             | `CastOverflow`               | 转换溢出错误                  | 运行时      | 严格模式 `cast` 无法在目标类型表示                           |
| `TC_ERR_IO`                        | `IOError`                    | I/O 错误                      | 运行时      | `read` 输入非法、EOF、超范围，或标准输入/输出读写失败                 |
| `TC_ERR_OUT_OF_MEMORY`             | `OutOfMemory`                | —（实现扩展）                 | 静态/运行时 | **实现专用**：内部分配失败；诊断消息为 `memory allocation failed` |
| `TC_ERR_INDENT_MIXED`              | `IndentMixedError`           | 缩进不一致                    | 静态        | 同一文件混用空格与制表符                                     |
| `TC_ERR_INDENT_INSUFFICIENT`       | `IndentInsufficientError`    | 块内缩进层级错误              | 静态        | 直接块内语句未恰好多一级，一次意外增加多级，或回退宽度不在缩进栈中 |
| `TC_ERR_INDENT_ELSE_END`           | `IndentElseEndError`         | `else`/`end` 缩进错           | 静态        | `else` 或 `end` 与对应 `if`/`while` 缩进不一致               |
| `TC_ERR_MISSING_END`               | `MissingEndError`            | 缺少 `end`                    | 静态        | `if`/`while` 未以 `end` 结束                                 |
| `TC_ERR_ELSE_POSITION`             | `ElsePositionError`          | `else` 位置错误               | 静态        | `else` 前无对应 `if` 块                                      |
| `TC_ERR_CONDITION_TYPE`            | `ConditionTypeError`         | 条件类型错误                  | 静态        | `if`/`while` 条件 RHS 结果非 `bool`                          |
| `TC_ERR_MODE_MISMATCH`             | `ModeMismatch`               | 模式不匹配错误                | 静态        | 操作、类型与 `wrap`/`ieee`/`truncate` 不符合 §5.1 / §8      |
| **`TC_ERR_BITCAST_WIDTH`**         | **`BitcastWidthError`**      | **位重解释宽度错误**          | **静态**    | **`bitcast` 源、目标位宽不一致（§8.6）**                     |
| `TC_ERR_LABEL_NOT_FOUND`           | `LabelNotFound`              | 标签未找到错误                | 静态        | `goto` 引用了未定义的标签                                    |
| `TC_ERR_DUPLICATE_LABEL`           | `DuplicateLabel`             | 标签重复定义错误              | 静态        | 同一作用域内定义了同名标签                                   |
| `TC_ERR_LABEL_INSIDE_LOOP`         | `LabelInsideLoop`            | 标签定义在循环内错误          | 静态        | `label` 定义在 `while` 块作用域内                            |
| `TC_ERR_JUMP_INTO_BLOCK`           | `JumpIntoBlockError`         | 跳入块错误                    | 静态        | 试图跳入内层子块                                             |
| `TC_ERR_JUMP_TO_SIBLING_BLOCK`     | `JumpToSiblingBlockError`    | 跳入兄弟块错误                | 静态        | 试图跳入 `then`/`else` 兄弟分支                              |
| **`TC_ERR_GOTO_INSIDE_LOOP`**      | **`GotoInsideLoop`**         | **循环内 goto 错误**          | **静态**    | **`goto` 出现在 `while` 块作用域内**                         |
| **`TC_ERR_BREAK_OUTSIDE_LOOP`**    | **`BreakOutsideLoop`**       | **循环外 break 错误**         | **静态**    | **`break` 未出现在 `while` 内**                              |
| **`TC_ERR_CONTINUE_OUTSIDE_LOOP`** | **`ContinueOutsideLoop`**    | **循环外 continue 错误**      | **静态**    | **`continue` 未出现在 `while` 内**                           |

> **迁移说明**：0.0.31 删除 `TC_ERR_GOTO_SKIPS_VAR_INIT`、`TC_ERR_OVERFLOW_MODE`、`TC_ERR_CONSTANT_CIRCULAR`、`TC_ERR_FLOAT_CAST_OVERFLOW` 与 `TC_ERR_CROSS_BLOCK_REFERENCE`；新增/恢复 `TC_ERR_UNINITIALIZED_VARIABLE` 与 `TC_ERR_BITCAST_WIDTH`。实现扩展码不计入语言错误码集合。

---

# 第五部分：附录

---

## 附录 A：形式化语法（EBNF）

本附录为 TC 语言的 **权威语法定义**。语义规则以正文为准。

### A.1 符号约定

采用 **扩展巴科斯-瑙尔范式（EBNF）**：

| 元符号   | 含义              |
| -------- | ----------------- |
| `=`      | 产生式定义        |
| `\|`     | 择一              |
| `[ … ]`  | 可选（0 或 1 次） |
| `{ … }`  | 重复（0 或多次）  |
| `( … )`  | 分组              |
| `"…"`    | 终结符            |
| `"a" … "z"` | 包含两端的字符范围 |
| `? … ?`  | 用自然语言描述的词法特殊序列 |
| *斜体名* | 非终结符          |

### A.2 词法与缩进 Token

词法分析先按最长匹配生成 Token，再将完整标识符与关键字表比较。`varx` 因而是一个 `IDENTIFIER`，绝不能拆成 `VAR` + `IDENTIFIER`。除行首缩进外，Token 间的空格和制表符由词法层丢弃；语法层不使用可匹配零字符的 `ws` 充当关键字边界。

```ebnf
/* ── 基本字符类 ── */

letter     = "A" … "Z" | "a" … "z" | "_" ;
digit      = "0" … "9" ;
hex_digit  = digit | "A" … "F" | "a" … "f" ;

/* ── 标识符与字面量 ── */

identifier = letter , { letter | digit | "_" } ;

integer_literal
           = ( "-" , (
               dec_literal
             | hex_literal
             | bin_literal
             | oct_literal
           ) )
           | (
               dec_literal [ "u" | "U" ]
             | hex_literal [ "u" | "U" ]
             | bin_literal [ "u" | "U" ]
             | oct_literal [ "u" | "U" ]
           ) ;
/* 负号仅可与无后缀字面量组合；禁止 -42u 等形式（§2.3.1） */

float_literal = [ "-" ] , (
    dec_float_literal [ ( "e" | "E" ) , [ "+" | "-" ] , digit , { digit } ]
  | sci_int_float_literal
) , [ "f" | "F" ] ;

dec_float_literal = digit , { digit } , "." , digit , { digit } ;
sci_int_float_literal
           = digit , { digit } , ( "e" | "E" ) , [ "+" | "-" ] ,
             digit , { digit } ;
/* 允许 1e5 等无小数点的科学计数法（§2.4.2）；亦允许 1.2e5 */

float_special = "inf" | "-" , "inf" | "nan" ;
/* -inf 合法；-nan 非法 */

bool_literal = "true" | "false" ;

dec_literal = non_zero_digit , { [ "_" ] digit } | "0" ;
hex_literal = ( "0x" | "0X" ) , hex_digit , { [ "_" ] hex_digit } ;
bin_literal = ( "0b" | "0B" ) , ( "0" | "1" ) , { [ "_" ] ( "0" | "1" ) } ;
oct_literal = ( "0o" | "0O" ) , ( "0" … "7" ) , { [ "_" ] ( "0" … "7" ) } ;
non_zero_digit = "1" … "9" ;

/* ── 类型关键字 ── */

int_type   = "int8"   | "uint8"
           | "int16"  | "uint16"
           | "int32"  | "uint32"
           | "int64"  | "uint64" ;
float_type = "float32" | "float64" ;
bool_type  = "bool" ;
type       = int_type | float_type | bool_type ;

/* ── 运算与模式关键字 ── */

binary_arith_op = "add" | "sub" | "mul" | "div" | "mod" ;
unary_arith_op  = "abs" | "neg" ;
bitwise_op      = "and" | "or" | "xor" ;
unary_bitwise_op = "not" ;
shift_op        = "shl" | "shr" ;
compare_op = "eq" | "ne" | "lt" | "le" | "gt" | "ge" ;
mode = "ieee" | "wrap" ;  /* 严格模式：无关键字（默认） */
truncate_kw = "truncate" ;
bitcast_kw  = "bitcast" ;

/* ── 格式化符号 ── */

format_specifier
           = "%d" | "%i" | "%u" | "%x" | "%X" | "%o" | "%b" | "%t"
           | "%f" | "%e" | "%E" | "%g" | "%G" ;

/* ── 控制流关键字 ── */
if_kw      = "if" ;
then_kw    = "then" ;
else_kw    = "else" ;
end_kw     = "end" ;
while_kw   = "while" ;
break_kw   = "break" ;
continue_kw = "continue" ;
goto_kw    = "goto" ;
label_kw   = "label" ;

/* ── 注释 ── */

line_comment = ";" , { ? any character except newline ? } ;
```

**缩进 Token 化规则**：

1. 词法器维护缩进宽度栈，初始仅含顶层宽度 `0`。
2. 每个非空、非纯注释逻辑行开始时，将其缩进与栈顶比较：增加时必须恰好增加一级，压栈并生成一个 `INDENT`；一次增加多级为缩进错误。减少时逐项出栈并为每次出栈生成一个 `DEDENT`。减少后的宽度必须等于某个既有栈值，否则为缩进错误。
3. 一级缩进恒为 4 个空格或 1 个制表符；同一文件二者不可混用，每级宽度不可变化。
4. 空行和纯注释行只生成 `NEWLINE`，不改变缩进栈；它们的行首空白不参与缩进风格或宽度检查。
5. LF（`\n`）、CRLF（`\r\n`）或 CR（`\r`）均表示一个逻辑行结束，并生成一个 `NEWLINE`；文件结尾先补最后一个 `NEWLINE`（若缺少），再生成剩余 `DEDENT`，最后生成 `EOF`。
6. `INDENT`/`DEDENT` 只反映 `if`/`else`/`while` 语句体的实际缩进；`label` 是普通语句，不改变缩进栈。
7. 空格/制表符混用报 `TC_ERR_INDENT_MIXED`；`else`/`end` 与对应块头不对齐报 `TC_ERR_INDENT_ELSE_END`；其他违反上述缩进层级/栈规则的情形报 `TC_ERR_INDENT_INSUFFICIENT`。

### A.3 语法

本节作用于 A.2 输出的 Token 序列。`NEWLINE`、`INDENT`、`DEDENT`、`EOF` 是虚拟终结符；`identifier`、`integer_literal` 等名称引用 A.2 的完整 Token 类别。引号中的关键字或标点表示一个完整 Token，不是字符子串匹配。非行首空白与行末注释已由词法层移除。

```ebnf
/* ── 程序 ── */

program    = { NEWLINE | statement , NEWLINE } , EOF ;

statement  = simple_statement
           | if_stmt
           | while_stmt ;

simple_statement
           = variable_def
           | constant_def
           | assignment
           | write_stmt
           | writeln_stmt
           | read_stmt
           | break_stmt
           | continue_stmt
           | label_def
           | goto_stmt ;

/* ── 变量定义（声明时必须初始化；无可选 RHS） ── */

variable_def = "var" , identifier , ":" , type , "=" , rhs ;

/* ── 常量定义 ── */

constant_def = "let" , identifier , ":" , type , "=" , const_rhs ;

/* ── 赋值语句 ── */

assignment = identifier , "=" , rhs ;

/* ── if 语句（缩进敏感，end 必须） ── */

if_stmt    = "if" , rhs , "then" , NEWLINE , suite ,
             [ "else" , NEWLINE , suite ] ,
             "end" ;

/* ── while 语句（缩进敏感，end 必须） ── */

while_stmt = "while" , rhs , "then" , NEWLINE , suite ,
             "end" ;

/* ── break / continue 语句 ── */

break_stmt = "break" ;
continue_stmt = "continue" ;

suite      = { NEWLINE } , [ INDENT , block , DEDENT ] ;
block      = statement , NEWLINE ,
             { NEWLINE | statement , NEWLINE } ;
/* suite 不含 INDENT 时表示零条语句的空块；缩进约束见 §4.7.2 */

/* ── label 与 goto 语句 ── */

label_def  = "label" , identifier , ":" ;
/* label 不产生新缩进；同级后续语句与 label 缩进一致，见 §4.7.2 R7、§4.9.2 */
goto_stmt  = "goto" , identifier ;

/* ── 右值表达式 ── */

rhs        = identifier
           | integer_literal
           | float_literal
           | float_special
           | bool_literal
           | binary_arith_expr
           | unary_arith_expr
           | bitwise_expr
           | unary_bitwise_expr
           | shift_expr
           | compare_expr
           | binary_logic_expr
           | unary_logic_expr
           | cast_expr
           | bitcast_expr ;

/* ── 常量右值表达式 ── */

const_rhs  = integer_literal
           | float_literal
           | float_special
           | bool_literal
           | identifier
           | const_binary_arith_expr
           | const_unary_arith_expr
           | const_bitwise_expr
           | const_unary_bitwise_expr
           | const_shift_expr
           | const_compare_expr
           | const_binary_logic_expr
           | const_unary_logic_expr
           | const_cast_expr
           | const_bitcast_expr ;

/* ── 常量表达式（只允许单层调用） ── */

const_binary_arith_expr
           = binary_arith_op , "(" , type , "," ,
             [ mode , "," ] , const_operand , "," , const_operand , ")" ;

const_unary_arith_expr
           = unary_arith_op , "(" , type , "," ,
             [ mode , "," ] , const_operand , ")" ;

/* ── 常量位运算表达式 ── */

const_bitwise_expr
           = bitwise_op , "(" , int_type , "," ,
             const_operand , "," , const_operand , ")" ;

const_unary_bitwise_expr
           = "not" , "(" , int_type , "," , const_operand , ")" ;

const_shift_expr
           = shift_op , "(" , int_type , "," ,
             [ mode , "," ] , const_operand , "," , const_operand , ")" ;

/* ── 常量比较表达式 ── */

const_compare_expr
           = compare_op , "(" , ( int_type | float_type ) , "," ,
             const_operand , "," , const_operand , ")" ;

/* ── 常量逻辑表达式 ── */

const_binary_logic_expr
           = binary_logic_op , "(" , bool_type , "," ,
             const_operand , "," , const_operand , ")" ;

const_unary_logic_expr
           = "not" , "(" , bool_type , "," , const_operand , ")" ;

/* ── 常量转换表达式 ── */

const_cast_expr
           = "cast" , "(" , type , "," , const_operand , ")"
           | "cast" , "(" , int_type , "," , "truncate" , "," ,
             const_operand , ")" ;

const_bitcast_expr
           = "bitcast" , "(" , type , "," , const_operand , ")" ;

/* ── 常量操作数 ── */

const_operand
           = integer_literal
           | float_literal
           | float_special
           | bool_literal
           | identifier ;

/* ── 操作数 ── */

operand    = identifier | integer_literal | float_literal | float_special | bool_literal ;

/* ── 双目算术表达式 ── */

binary_arith_expr
           = strict_binary_arith_expr
           | mode_binary_arith_expr ;

strict_binary_arith_expr
           = binary_arith_op , "(" , type , "," ,
             operand , "," , operand , ")" ;

mode_binary_arith_expr
           = binary_arith_op , "(" , type , "," , mode , "," ,
             operand , "," , operand , ")" ;

/* ── 单目算术表达式 ── */

unary_arith_expr
           = strict_unary_arith_expr
           | mode_unary_arith_expr ;

strict_unary_arith_expr
           = unary_arith_op , "(" , type , "," , operand , ")" ;

mode_unary_arith_expr
           = unary_arith_op , "(" , type , "," , mode , "," ,
             operand , ")" ;

/* ── 按位逻辑表达式（仅整数） ── */

bitwise_expr
           = bitwise_op , "(" , int_type , "," ,
             operand , "," , operand , ")" ;

unary_bitwise_expr
           = "not" , "(" , int_type , "," , operand , ")" ;

/* ── 移位表达式（仅整数） ── */

shift_expr
           = strict_shift_expr
           | wrap_shift_expr ;

strict_shift_expr
           = shift_op , "(" , int_type , "," ,
             operand , "," , operand , ")" ;

wrap_shift_expr
           = "shl" , "(" , int_type , "," , "wrap" , "," ,
             operand , "," , operand , ")" ;

/* ── 比较表达式（不支持 ieee 模式） ── */

compare_expr
           = compare_op , "(" , ( int_type | float_type ) , "," ,
             operand , "," , operand , ")" ;

/* ── 双目逻辑表达式（布尔） ── */

binary_logic_expr
           = binary_logic_op , "(" , bool_type , "," ,
             operand , "," , operand , ")" ;

binary_logic_op = "and" | "or" ;

/* ── 单目逻辑表达式（布尔） ── */

unary_logic_expr
           = "not" , "(" , bool_type , "," , operand , ")" ;

/* ── 类型转换表达式 ── */

cast_expr  = strict_cast_expr | truncate_cast_expr ;

strict_cast_expr
           = "cast" , "(" , type , "," , operand , ")" ;

truncate_cast_expr
           = "cast" , "(" , int_type , "," , "truncate" , "," ,
             operand , ")" ;

bitcast_expr
           = "bitcast" , "(" , type , "," , operand , ")" ;

/* 所有运算类型/模式组合均受 §5.1 权威矩阵约束。
   truncate 仅允许整数窄化；bitcast 要求等位宽且两端非 bool（§8）。 */

/* ── I/O 语句 ── */

write_stmt = "write" , "(" , type , "," , operand , ")"
           | "write" , "(" , type , "," , format_specifier , "," ,
             operand , ")" ;

writeln_stmt = "writeln" , "(" , type , "," , operand , ")"
             | "writeln" , "(" , type , "," , format_specifier , "," ,
               operand , ")" ;

read_stmt  = "read" , "(" , type , "," , identifier , ")" ;
```

---

## 附录 B：与 C 语言标量语义对照

### B.1 类型与运算对照

| 类型        | TC 类型   | C 语言等价                     |
| ----------- | --------- | ------------------------------ |
| 8 位有符号  | `int8`    | `int8_t`                       |
| 8 位无符号  | `uint8`   | `uint8_t`                      |
| 16 位有符号 | `int16`   | `int16_t`                      |
| 16 位无符号 | `uint16`  | `uint16_t`                     |
| 32 位有符号 | `int32`   | `int32_t`                      |
| 32 位无符号 | `uint32`  | `uint32_t`                     |
| 64 位有符号 | `int64`   | `int64_t`                      |
| 64 位无符号 | `uint64`  | `uint64_t`                     |
| 单精度浮点  | `float32` | `float`                        |
| 双精度浮点  | `float64` | `double`                       |
| 布尔        | `bool`    | `bool`（C23） / `_Bool`（C99） |

| 运算              | TC                                    | C 等价                                         |
| ----------------- | ------------------------------------- | ---------------------------------------------- |
| 有符号字面量      | `42`                                  | `42`                                           |
| 无符号字面量      | `42u`                                 | `42u`                                          |
| 浮点字面量        | `3.14f` / `3.14`                      | `3.14f` / `3.14`                               |
| 加                | `add(int32, a, b)`                    | `(int32_t)a + (int32_t)b`                      |
| 加（回绕）        | `add(int32, wrap, a, b)`              | 同宽无符号运算后 reinterpret                   |
| 浮点加（严格）    | `add(float64, a, b)`（默认严格模式）  | `a + b`（C 不检测异常）                        |
| 浮点加（IEEE）    | `add(float64, ieee, a, b)`            | `a + b`（C 默认行为）                          |
| 整除 / 求余       | `div` / `mod`                         | `/` / `%`                                      |
| `div(INT_MIN,−1)` | `div` → **整数溢出错误**；`mod` → `0` | C 为 **UB**（Java/C# 定义 `div` 为 `INT_MIN`） |
| 取绝对值          | `abs(int32, a)`                       | `llabs(a)`                                     |
| 按位与            | `and(int32, a, b)`                    | `a & b`                                        |
| 左移（严格）      | `shl(int32, a, b)`                    | `a << b`（TC 检测溢出，C 为 UB）               |
| 左移（回绕）      | `shl(int32, wrap, a, b)`              | `(uint32_t)a << b` 后 reinterpret              |
| 右移（有符号）    | `shr(int32, a, b)`                    | `a >> b`（TC 固定算术右移，C 为实现定义）      |
| 比较              | `eq(int32, a, b)`                     | `a == b`                                       |
| 逻辑与            | `and(bool, a, b)`                     | `a && b`                                       |

| 转换                       | TC                              | C                                           |
| -------------------------- | ------------------------------- | ------------------------------------------- |
| 严格数值转换（默认）   | `cast(int8, a)`                 | `(int8_t)a`（TC 在不可表示时报错）       |
| 整数窄化（`truncate`） | `cast(int8, truncate, a)`       | 取源整数低 8 位后按目标符号性解释        |
| 等宽位重解释（`bitcast`） | `bitcast(float32, bits)`        | `memcpy` 到等宽目标类型（避免别名 UB）       |
| bool→int                   | `cast(int32, flag)`             | `flag ? 1 : 0`                              |
| int→bool                   | `cast(bool, x)`                 | `x != 0`                                    |

| I/O          | TC                     | C 等价             |
| ------------ | ---------------------- | ------------------ |
| 无格式输出   | `write(int32, x)`      | `printf("%d", x)`  |
| 有符号十进制 | `write(int32, %d, x)`  | `printf("%d", x)`  |
| 无符号十进制 | `write(uint32, %u, x)` | `printf("%u", x)`  |
| 十六进制小写 | `write(uint32, %x, x)` | `printf("%x", x)`  |
| 十六进制大写 | `write(uint32, %X, x)` | `printf("%X", x)`  |
| 八进制       | `write(uint32, %o, x)` | `printf("%o", x)`  |
| 二进制       | `write(uint32, %b, x)` | 无标准等价（扩展） |
| 布尔输出     | `write(bool, %t, x)`   | 无标准等价（扩展） |

**主要差异**：
- TC 严格模式下有符号溢出报错（C 为 UB）
- TC `div(INT_MIN, −1)` 报整数溢出错误（C 为 UB；Java/C# 将 `div` 定义为 `INT_MIN`）
- TC `cast` 对所有整数窄化和符号性变换做可表示性检查；C 的对应转换可能为实现定义行为
- TC **声明时必须初始化**（`var … = …`；缺省 → `TC_ERR_VAR_MISSING_INIT`，§4.2）。此外，编译器必须在完整 CFG 上证明每次变量读取前都已执行其当前生命周期的初始化；否则报 `TC_ERR_UNINITIALIZED_VARIABLE`。`goto` 跳过但未读取某变量本身不构成错误。C 读取未初始化对象可产生未定义行为
- TC 二进制 `%b` 和布尔 `%t` 为扩展格式
- TC 比较运算返回 `bool`（C 中比较返回 `int`）
- TC 逻辑运算严格操作 `bool`（C 中逻辑运算接受任意标量类型）
- TC 右移有符号固定算术右移，C 为实现定义

### B.2 `let` 常量与 C 语言 `const` 语义对照

| 特性               | TC `let`     | C `const`    | 说明                    |
| ------------------ | ------------ | ------------ | ----------------------- |
| 编译期求值         | ✅ 必须       | ❌ 不要求     | TC `let` 保证编译期求值 |
| 运行时存储         | ❌ 不分配     | 通常分配     | TC 常量不生成运行时数据 |
| 可用于数组大小     | 未来扩展     | ✅            | TC 预留，未来数组将支持 |
| 地址可取值         | ❌ 不可取     | ✅ 可取       | TC 常量无运行时地址     |
| RHS 可为算术表达式 | ✅            | ✅            | TC 常量支持编译期算术   |
| 溢出检查           | ✅ 编译期报错 | ❌ UB         | TC 在编译期捕获常量溢出 |
| 优化器可推导       | ✅ 恒为常量   | ⚠️ 视实现而定 | TC 语义保证             |

### B.3 与 C++ `constexpr` 语义对照

| 特性           | TC `let`                | C++ `constexpr` |
| -------------- | ----------------------- | --------------- |
| 编译期求值     | ✅ 必须                  | ✅ 必须          |
| 运行时存储     | ❌ 不分配                | 视上下文而定    |
| 支持条件表达式 | ❌ `let` 不含控制流      | ✅               |
| 支持循环       | ❌                       | ✅               |
| 支持函数调用   | ❌（未来函数扩展后支持） | ✅               |
| 溢出检查       | ✅ 编译期报错            | ❌ 通常 UB       |

---

## 附录 C：各类型边界常量速查

| 类型      | 范围                                                    | 十六进制边界字面量                                |
| --------- | ------------------------------------------------------- | ------------------------------------------------- |
| `int8`    | −128 ～ 127                                             | `-0x80`, `0x7F`                                   |
| `uint8`   | 0 ～ 255                                                | `0xFFu`                                           |
| `int16`   | −32,768 ～ 32,767                                       | `-0x8000`, `0x7FFF`                               |
| `uint16`  | 0 ～ 65,535                                             | `0xFFFFu`                                         |
| `int32`   | −2,147,483,648 ～ 2,147,483,647                         | `-0x8000_0000`, `0x7FFF_FFFF`                     |
| `uint32`  | 0 ～ 4,294,967,295                                      | `0xFFFF_FFFFu`                                    |
| `int64`   | −9,223,372,036,854,775,808 ～ 9,223,372,036,854,775,807 | `-0x8000_0000_0000_0000`, `0x7FFF_FFFF_FFFF_FFFF` |
| `uint64`  | 0 ～ 18,446,744,073,709,551,615                         | `0xFFFF_FFFF_FFFF_FFFFu`                          |
| `float32` | 有限值约 −3.4e38 ～ +3.4e38；最小正非零值约 1.4e-45 | —                                                 |
| `float64` | 有限值约 −1.8e308 ～ +1.8e308；最小正非零值约 5e-324 | —                                                 |
| `bool`    | `false` ～ `true`                                       | —                                                 |

---

## 附录 D：扩展路线图

本附录只描述后续扩展方向，**不改变** 0.0.31 正文的强制规则。“规范已规定”与“实现已交付”是两个独立状态；本文编写时的 v0.0.26 实现不得因本附录的路线图而声称符合 TC 0.0.31。

> **扩展集成原则**
>
> TC 的扩展遵循以下集成原则，确保语言框架的一致性：
> 1. **复合类型（`array`/`struct`/`ptr`）**：作为 `type` 产生式的扩展，复用现有语句框架（`var`/`let`/赋值/I/O），运算指令按需扩展（如 `index`、`field`）
> 2. **字符串（`string`）**：作为复合类型的特例，支持 `%s` 格式化输出和基本的长度/拼接操作
> 3. **函数（`func`/`return`）**：引入新的语句种类，参数传递和返回值均遵循 TC 的"显式类型"原则
> 4. **循环**：以正文 `while`/`break`/`continue`（§4.8、§4.10）为结构化路径；`if` + `goto`（§4.9）为非结构化路径，两者范式隔离。两种路径都必须纳入同一套完整 CFG 确定初始化分析。

所有扩展**必须**保持 TC 的核心原则：显式类型标注、逐条执行、默认安全、教学友好。

| 领域                               | 0.0.31 规范状态 | 实现交付状态          | 设计位置              |
| ---------------------------------- | ---------------- | ----------------------- | --------------------- |
| `while` / `break` / `continue`     | **强制**         | v0.0.26 未声明符合 0.0.31 | §4.8、§4.10           |
| 完整 CFG 确定初始化               | **强制**         | v0.0.26 未声明符合 0.0.31 | §3.9、§4.8.5、§4.9.7 |
| `bitcast` 等宽位重解释             | **强制**         | v0.0.26 未声明符合 0.0.31 | §8                    |
| `fmod`/`remainder` 浮点取模        | 未纳入          | 未交付                   | §5.7                  |
| 浮点舍入模式（运行时可选）         | 未纳入          | 未交付                   | §5                    |
| 浮点数学库（`sqrt`、`sin`、`cos`） | 未纳入          | 未交付                   | §5.9                  |
| 函数定义与调用                     | 未纳入          | 未交付                   | §4.7                  |
| 字符与字符串（`char`/`string`）    | 未纳入          | 未交付                   | §2.6、§3.1、§9.6      |
| 数组、指针、结构体                 | 未纳入          | 未交付                   | §3.1、§9.6           |

---

## 附录 E：文档修订记录

| 版本        | 日期           | 说明                                                         |
| ----------- | -------------- | ------------------------------------------------------------ |
| 0.0.01      | 2026-07-01     | 首版草案：整数类型、算术与 cast 语义                         |
| 0.0.02      | 2026-07-01     | 补充参考实现说明                                             |
| 0.0.03      | 2026-07-01     | 补充命令行界面参考                                           |
| 0.0.04      | 2026-07-01     | 新增三条 I/O 语句                                            |
| 0.0.05      | 2026-07-01     | 补充交互式解释器模式说明                                     |
| 0.0.06      | 2026-07-01     | 完善整数语义：运行时值模型、字面量/极值约束、作用域          |
| 0.0.07      | 2026-07-01     | 新增负数字面量支持                                           |
| 0.0.08      | 2026-07-01     | EBNF 完善：`program` 支持空白行                              |
| 0.0.09      | 2026-07-01     | 新增多进制字面量与数字分隔符                                 |
| 0.0.10      | 2026-07-01     | 结构优化：I/O 语句移至完整示例之前                           |
| 0.0.11      | 2026-07-02     | 规范补充：源序可见性、变量生命周期、测试覆盖说明             |
| 0.0.12      | 2026-07-02     | 字面量引入 `u` 后缀；`overflow` 拆分为 `wrap` 和 `truncate`  |
| 0.0.13      | 2026-07-02     | 新增变量初始化可选、常量定义（`let`）；澄清 cast/truncate 交互 |
| 0.0.14      | 2026-07-03     | 与实现对齐：`read` 视为变量初始化；自引用/前向引用说明       |
| 0.0.15      | 2026-07-03     | 新增单目运算：`abs` 和 `neg`                                 |
| 0.0.16      | 2026-07-03     | 新增 I/O 格式化输出（7 种格式化符号）                        |
| 0.0.17      | 2026-07-03     | 文档结构重构：按「基础→语句→I/O→示例→附录」重新编排；新增类型分类、运算扩展框架、转换扩展框架、扩展路线图附录 |
| 0.0.18      | 2026-07-03     | 整数除法语义：`div(INT_MIN, −1)` 改为整数溢出错误；`mod(INT_MIN, −1)` 仍为 0；附录 B 澄清与 C/Java/C# 的差异 |
| 0.0.19      | 2026-07-03     | 新增布尔类型、比较运算与逻辑运算：新增 `bool` 类型，`true`/`false` 字面量；新增 6 种比较运算；新增 3 种逻辑运算，支持短路求值；新增 `%t` 格式化符号；扩展 `cast` 支持整数 ↔ `bool` 转换 |
| 0.0.20      | 2026-07-03     | 澄清与优化：细化 `bool` 类型的内部表示描述，消除位宽歧义；确认 `read` 对无符号类型不接受负号前缀的规则，保持与语言"禁止隐式转换"原则一致 |
| 0.0.21      | 2026-07-03     | `let` 常量表达式增强：放宽 `let` RHS 从仅允许字面量到允许编译期常量表达式（算术/比较/逻辑/无损转换）；新增常量表达式错误类型（常量溢出/除零/循环依赖等）；更新 EBNF 语法定义；新增常量表达式综合示例；补充与 C `const`/C++ `constexpr` 的语义对照；统一全文专业术语 |
| 0.0.22      | 2026-07-05     | 新增位运算：增加 `xor`、`shl`、`shr` 运算；`and`/`or`/`not` 重载支持按位操作；明确左移支持 `wrap` 模式，右移无模式；澄清移位计数不掩码的语义规则；精简附录 C；更新 EBNF、错误类型、示例及附录 |
| 0.0.23      | 2026-07-05     | 修订细节：拆分 §8.5 类型转换速查表；补充移位约束框；补充八进制词法匹配；修订 EBNF 位运算产生式；澄清 `mod` 溢出描述；补充右移 `k>=n` 解释；附录 B 增加对照行；增加移位计数"不掩码"设计说明；补充赋值左侧变量可未初始化说明；增强截断模式澄清 |
| 0.0.24      | 2026-07-07     | 新增 Pascal 风格 `if-else` 控制流（`then`/`else`/`end`/缩进强制）；引入块级局部作用域；更新 EBNF、错误表、示例集和路线图 |
| 0.0.24-impl | 2026-07-07     | 实现交付 `if-then-else-end`、块级作用域、缩进引擎            |
| 0.0.24-rev2 | 2026-07-09     | 错误码表：新增 §11.4 `TcErrorKind` 全量对照（含 `TC_ERR_OUT_OF_MEMORY` 实现扩展） |
| 0.0.25      | 2026-07-13     | 浮点类型完整支持：① 新增 `float32`/`float64` 类型及字面量；② 完全复用现有指令（`add`/`sub`/`mul`/`div`/`abs`/`neg`）统一支持浮点；③ 三种运算模式（严格/`ieee`/`wrap`）；④ 浮点比较与 `nan` 语义；⑤ `cast` 扩展支持浮点转换与位重解释；⑥ I/O 新增 `%f`/`%e`/`%E`/`%g`/`%G` 格式化符号；⑦ 常量表达式支持浮点算术；⑧ 更新 EBNF、错误类型、示例集及附录 |
| 0.0.25-rev1 | 2026-07-13     | 文档一致性修订：§7.1.1 澄清整数/浮点比较的模式关键字规则；§5.4 位模式混合须经整数 `cast(truncate)`；§9.6 移除已实现的浮点格式符、更新扩展预留；§10.15 示例改为整数 `xor` 路径（与 §6 对齐） |
| 0.0.26      | 2026-07-14     | 安全与控制流增强：① 将"读取未初始化变量"从编译警告升级为静态编译错误，引入数据流分析确保所有路径均已初始化；② 新增受限 `goto` 语句，支持向外/平级跳转，严格禁止跳入子块或兄弟分支；③ 新增 5 类静态错误码（未初始化变量、标签未找到、标签重复定义、跳入块错误、跳入兄弟块错误）；④ 移除 `TC_WARN_UNINITIALIZED_VARIABLE` 警告码；⑤ 更新 EBNF 语法（新增 `label_def`、`goto_stmt` 产生式）；⑥ 补充 `goto` 合法/非法示例及完整示例集；⑦ 在 §1.1、§2.8、§4.1、§4.8 等章节全面集成新特性；⑧ 明确标签块级作用域（与 `var`/`let` 一致），修订缩进规则，完善标签查找与重复定义判定 |
| 0.0.27      | 2026-07-14     | 循环控制增强：新增 `while` 循环、`break` 与 `continue` 语句；补充循环结构对数据流分析的"零次迭代保守规则"及编译期恒真循环特殊处理；调整章节编号；更新 EBNF、错误表、示例集及附录（该版本条目随后并入 0.0.28 草案文件） |
| 0.0.28      | 2026-07-14     | 范式隔离与数据流闭环：① 确立结构化 / 非结构化循环范式隔离——`while` 内禁止 `goto`/`label`；② 规则 B 仅承认 `break` 出口；③ 新增 `TC_ERR_GOTO_INSIDE_LOOP`、`TC_ERR_LABEL_INSIDE_LOOP`；④ 章节：`while` §4.8、`goto` §4.9、`break`/`continue` §4.10；⑤ 补充非结构化循环示例与 §1.4 范式对照；⑥ 更新 EBNF、错误表及附录 D；⑦ 收敛：已初始化集合用「交集」表述、钉死编译期恒真/恒假判定（对齐 §4.3）、修正全局 `label` 后示例缩进、文件名与文内版本统一为 0.0.28 |
| 0.0.29      | 2026-07-14     | 强制变量初始化：① 修订 `var` 定义，**RHS 必须提供**，无 RHS 触发 `TC_ERR_VAR_MISSING_INIT`；② 移除所有未初始化变量相关描述；③ 简化数据流分析，删除 §4.8.5 复杂规则；④ 更新 §1.2 内存安全原则、§1.3 安全优先哲学；⑤ 修订示例集（移除未初始化声明）；⑥ 错误码表新增 `TC_ERR_VAR_MISSING_INIT`，移除 `TC_ERR_UNINITIALIZED_VARIABLE`；⑦ 更新 EBNF `variable_def` 产生式；⑧ 精简 §3.8、§3.9 生命周期说明；⑨ 版本升级至 v0.0.29 |
| 0.0.30      | 2026-07-14     | 关键修复与澄清：① 修复 `goto` 跳过 `var` 初始化的严重缺陷（新增 `TC_ERR_GOTO_SKIPS_VAR_INIT`，§4.9.4）；② 澄清同一作用域内常量命名冲突规则（§4.3）；③ 明确标签与变量/常量属于不同命名空间（§4.9.1）；④ 说明无符号缩窄在严格模式下不报错的设计意图（§8.2）；⑤ 移除冗余的浮点比较 `ieee` 模式（§7.1, 附录A）；⑥ 统一 `let` 常量运行时存储描述（§3.9, 附录B）；⑦ 细化 `break`/`continue` 作用于最内层 `while` 的说明（§4.10.2）；⑧ 补充常量 `shr` 在 `k>=n` 时的行为（§4.3）；⑨ 修正编译警告章节措辞（§11.3）；⑩ 修正全局标签示例缩进（§10.15） |
| 0.0.30-rev1 | 2026-07-14     | 评审建议项 S1–S5：① §3.8/§3.9：离开块（`end` / `break` / `goto`）即销毁局部；② §4.3 与 §3.9/附录 B 统一 `let` 无运行时槽；③ 修订 §1.3/§3.7 过时「未来浮点」表述，附录 B 改为强制初始化 + `GOTO_SKIPS_VAR_INIT`；④ §4.9.2 `goto` 缩进与块内普通语句对齐，§4.10.2 重写 `continue` 语义；⑤ §11.4 声明 REPL 一律拒绝控制流/`label`，§10.15 例3 将 `exit` 标签后置。 |
| **0.0.30-rev2** | **2026-07-14** | **钉死 `label` 缩进模型**：① `label` 不产生新缩进、不开启代码块；② 同作用域内、非新子块的后续语句与 `label` 缩进完全一致；③ 新增 §4.7.2 R7，重写 §4.9.2；④ 修正 §1.4 / §10.15 / §10.16 中错误描述与仍多缩进的示例。 |
| **0.0.30-rev3** | **2026-07-15** | **承认受限 `goto` 局限并收敛安全叙事**：① 新增 §4.9.7「保证范围与已知局限」（承诺面 / 外跳前跳缺口 / 与全路径 DFA 的差距）；② 重写 §4.9.4：删除「外跳已保证定义已执行」的错误豁免表述，明确同层前跳为最低保证；③ 收敛 §1.2 / §1.3 / §3.9 / §4.2 / §4.6 / §4.8.5 / §11.3 / 附录 B 中「总是安全 / 无需数据流」过满措辞；④ §4.5 与 EBNF `rhs` 补 `identifier`；⑤ EBNF：负号与 `u` 后缀互斥；浮点允许无小数点科学计数（如 `1e5`）；⑥ §2.5 布尔字面量改为区分大小写且仅小写；⑦ 附录 D 区分正文规定与实现滞后，登录局限与可选加强项。 |
| **0.0.30-rev4** | **2026-07-15** | **收敛 `var`：声明时必须初始化**：① §1.2 单列原则并废止「先声明后首次赋值」；② §4.2 重写为 4.2.1/4.2.2，钉死唯一语法形态与「可选初始化已废止」；③ §3.9 分层设计声明（语法 / 执行 / 跳转）；④ §9.3 明确 `read` 仅覆盖、不代替初始化器；⑤ 附录 A `variable_def` 注释对齐。 |
| **0.0.30-rev5** | **2026-07-15** | **对齐初始化相关错误设计**：① 新增 §11.0 分层错误模型（`VAR_MISSING_INIT` / `GOTO_SKIPS_VAR_INIT` / 命名解析分工）；② 明文废止 `TC_ERR_UNINITIALIZED_VARIABLE` 及未初始化警告主路径；③ §11.1/§11.3/§11.4 与 §4.2、§4.9.7 交叉对齐；④ 禁止缺初始化器降级为笼统 `SYNTAX`；⑤ 加强跳转检查仍优先映射 `GOTO_SKIPS_VAR_INIT`。 |
| **0.0.31** | **2026-07-15** | **规范收敛（仅规范）**：① 完整 CFG 确定初始化取代 `goto` 局部跳过检查，恢复 `TC_ERR_UNINITIALIZED_VARIABLE`；② 删除浮点 `wrap`，固定 IEEE 754 舍入、异常与 NaN 规则；③ 严格 `cast` 统一执行可表示性检查，`truncate` 限于整数窄化，新增等宽 `bitcast`；④ `let` 与运行时精度/模式对齐且禁止嵌套调用；⑤ EBNF 分离词法 Token 与缩进语法；⑥ 固定诊断阶段、I/O 和 REPL 的规范边界。实现版本仍为 v0.0.26。 |

---

## 附录 F：0.0.30 → 0.0.31 迁移指南

0.0.31 是有意的破坏性规范升级。本附录为迁移摘要；若与正文冲突，以正文为准。

| 0.0.30 形式/行为 | 0.0.31 要求 | 迁移方式 |
| ---------------- | ------------- | ---------- |
| `add(float32, wrap, a, b)` 等浮点 `wrap` | 非法的类型/模式组合 | 需数值 IEEE 行为时改用 `ieee`；需位运算时先 `bitcast` 为等宽整数，运算后再 `bitcast` 回浮点 |
| `cast(float32, truncate, bits)` 或 `cast(uint32, truncate, f)` 用于位重解释 | `truncate` 仅用于整数窄化 | 改为 `bitcast(float32, bits)` 或 `bitcast(uint32, f)`，两端必须等位宽且非 `bool` |
| 严格无符号窄化可静默保留低位 | 所有严格整数转换均检查数学值是否可表示 | 对依赖低位保留的代码显式使用 `cast(<更窄整数类型>, truncate, value)` |
| 遇到某些跳过 `var` 的前跳即报 `TC_ERR_GOTO_SKIPS_VAR_INIT` | 只在可达使用点不能证明已初始化时报 `TC_ERR_UNINITIALIZED_VARIABLE` | 跳过但不使用该变量无需改写；对可达未初始化使用重构控制流，使所有前驱路径都经过定义 |
| 后向 `goto` 可以为每次通过创建新的局部存储 | 变量槽在进入作用域时固定分配，再次执行 `var` 会重新初始化同一槽 | 不得依赖每次跳转创建新地址或保留旧值 |
| `let` 的浮点计算可由宿主高精度计算后一次舍入 | 每个操作均按声明的 `float32`/`float64` 精度舍入，与运行时同模式 | 拆分嵌套常量调用，仅引用字面量或源序中更早的 `let` |
| 不同实现可选拒绝更多 `goto` 程序 | 合法程序集与诊断阶段必须一致 | 实现完整 CFG 固定点分析，不再保留影响可接受性的可选加强开关 |

**错误码变更**：删除 `TC_ERR_GOTO_SKIPS_VAR_INIT`、`TC_ERR_OVERFLOW_MODE`、`TC_ERR_CONSTANT_CIRCULAR`、`TC_ERR_FLOAT_CAST_OVERFLOW`、`TC_ERR_CROSS_BLOCK_REFERENCE`；新增或恢复 `TC_ERR_UNINITIALIZED_VARIABLE`、`TC_ERR_BITCAST_WIDTH`。模式错误统一使用 `TC_ERR_MODE_MISMATCH`，转换溢出统一使用 `TC_ERR_CAST_OVERFLOW`。

**实现边界**：迁移指南不代表 libtc / TC-VM / TC-AOT 已实现上述行为。只有全部强制规则在各后端和常量求值器中一致通过后，实现才可标记为 0.0.31 兼容。

---

*— 文档结束 —*
